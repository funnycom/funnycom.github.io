---
title: "[TS] 제네릭 - 2"
date: 2025-10-14 10:30:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS, 제네릭, Generics]
pin: true
math: true
mermaid: true
published: false
---

> **학습 목표**
> - 제네릭이 무엇이고 왜 필요한지 이해한다
> - 제네릭 함수를 작성할 수 있다
> - 제네릭 인터페이스와 타입을 정의할 수 있다
> - 제네릭 제약조건을 사용할 수 있다
> - 실전에서 제네릭을 활용할 수 있다

---

## ✏️ 직접 해보기

### 과제 1: 제네릭 캐시 시스템

제네릭을 사용하여 캐시 시스템을 만들어보세요.

**요구사항:**
- 키-값 쌍을 저장하는 Cache 클래스
- set, get, has, delete, clear 메서드 구현
- TTL(Time To Live) 기능: 일정 시간 후 자동 삭제
- 통계 기능: 히트/미스 카운트

```typescript
// 여기를 완성하세요
```

---

#### 정답 1: 제네릭 캐시 시스템

<details>
<summary>정답 보기</summary>

```typescript
// 캐시 항목 인터페이스
interface CacheItem<T> {
  value: T;
  expiresAt?: number;  // 만료 시간 (밀리초)
}

// 캐시 통계 인터페이스
interface CacheStats {
  hits: number;      // 캐시 히트 (찾음)
  misses: number;    // 캐시 미스 (못 찾음)
  size: number;      // 현재 항목 수
}

// 제네릭 캐시 클래스
class Cache<K, V> {
  private storage = new Map<K, CacheItem<V>>();
  private stats: CacheStats = {
    hits: 0,
    misses: 0,
    size: 0
  };
  
  // 저장
  set(key: K, value: V, ttlSeconds?: number): void {
    const item: CacheItem<V> = {
      value,
      expiresAt: ttlSeconds 
        ? Date.now() + ttlSeconds * 1000 
        : undefined
    };
    
    this.storage.set(key, item);
    this.stats.size = this.storage.size;
    console.log(\`[Cache] 저장: \${String(key)}\`);
  }
  
  // 가져오기
  get(key: K): V | undefined {
    const item = this.storage.get(key);
    
    if (!item) {
      this.stats.misses++;
      console.log(\`[Cache] 미스: \${String(key)}\`);
      return undefined;
    }
    
    // 만료 확인
    if (item.expiresAt && Date.now() > item.expiresAt) {
      this.delete(key);
      this.stats.misses++;
      console.log(\`[Cache] 만료됨: \${String(key)}\`);
      return undefined;
    }
    
    this.stats.hits++;
    console.log(\`[Cache] 히트: \${String(key)}\`);
    return item.value;
  }
  
  // 존재 확인
  has(key: K): boolean {
    if (!this.storage.has(key)) {
      return false;
    }
    
    const item = this.storage.get(key)!;
    
    // 만료 확인
    if (item.expiresAt && Date.now() > item.expiresAt) {
      this.delete(key);
      return false;
    }
    
    return true;
  }
  
  // 삭제
  delete(key: K): boolean {
    const result = this.storage.delete(key);
    if (result) {
      this.stats.size = this.storage.size;
      console.log(\`[Cache] 삭제: \${String(key)}\`);
    }
    return result;
  }
  
  // 전체 삭제
  clear(): void {
    this.storage.clear();
    this.stats.size = 0;
    console.log("[Cache] 전체 삭제");
  }
  
  // 만료된 항목 정리
  cleanup(): number {
    let cleaned = 0;
    const now = Date.now();
    
    for (const [key, item] of this.storage.entries()) {
      if (item.expiresAt && now > item.expiresAt) {
        this.storage.delete(key);
        cleaned++;
      }
    }
    
    if (cleaned > 0) {
      this.stats.size = this.storage.size;
      console.log(\`[Cache] \${cleaned}개 항목 정리됨\`);
    }
    
    return cleaned;
  }
  
  // 통계 가져오기
  getStats(): CacheStats {
    return { ...this.stats };
  }
  
  // 통계 출력
  printStats(): void {
    console.log("\n=== 캐시 통계 ===");
    console.log(\`히트: \${this.stats.hits}\`);
    console.log(\`미스: \${this.stats.misses}\`);
    console.log(\`현재 크기: \${this.stats.size}\`);
    
    const total = this.stats.hits + this.stats.misses;
    if (total > 0) {
      const hitRate = (this.stats.hits / total * 100).toFixed(2);
      console.log(\`히트율: \${hitRate}%\`);
    }
  }
  
  // 모든 항목 출력
  printAll(): void {
    console.log("\n=== 캐시 내용 ===");
    if (this.storage.size === 0) {
      console.log("(비어있음)");
      return;
    }
    
    for (const [key, item] of this.storage.entries()) {
      const expired = item.expiresAt && Date.now() > item.expiresAt;
      console.log(\`\${String(key)}: \${item.value} \${expired ? "(만료)" : ""}\`);
    }
  }
}

// 테스트
console.log("=== 캐시 시스템 테스트 ===\n");

// 문자열 키, 숫자 값
const numberCache = new Cache<string, number>();

numberCache.set("age", 25);
numberCache.set("score", 95, 2);  // 2초 후 만료

console.log("\n초기 상태:");
numberCache.printAll();

console.log("\n값 가져오기:");
console.log(numberCache.get("age"));    // 25
console.log(numberCache.get("score"));  // 95
console.log(numberCache.get("missing")); // undefined

numberCache.printStats();

// TTL 테스트
console.log("\n3초 대기 후...");
setTimeout(() => {
  console.log(numberCache.get("score"));  // undefined (만료됨)
  numberCache.printStats();
}, 3000);

// 객체 캐시
interface UserData {
  name: string;
  email: string;
}

const userCache = new Cache<number, UserData>();

userCache.set(1, { name: "홍길동", email: "hong@example.com" });
userCache.set(2, { name: "김철수", email: "kim@example.com" }, 5);

console.log("\n사용자 캐시:");
const user1 = userCache.get(1);
if (user1) {
  console.log(\`사용자 1: \${user1.name}\`);
}

// 정리
setTimeout(() => {
  console.log("\n만료된 항목 정리:");
  userCache.cleanup();
  userCache.printAll();
}, 6000);
```

**출력 예시:**
```
=== 캐시 시스템 테스트 ===

[Cache] 저장: age
[Cache] 저장: score

초기 상태:
=== 캐시 내용 ===
age: 25
score: 95

값 가져오기:
[Cache] 히트: age
25
[Cache] 히트: score
95
[Cache] 미스: missing
undefined

=== 캐시 통계 ===
히트: 2
미스: 1
현재 크기: 2
히트율: 66.67%

3초 대기 후...
[Cache] 만료됨: score
[Cache] 삭제: score
[Cache] 미스: score
undefined

=== 캐시 통계 ===
히트: 2
미스: 2
현재 크기: 1
히트율: 50.00%
```

</details>

---

### 과제 2: 제네릭 이벤트 시스템

제네릭을 활용한 이벤트 시스템을 만들어보세요.

**요구사항:**
- EventEmitter 클래스 구현
- on: 이벤트 리스너 등록
- off: 이벤트 리스너 제거
- emit: 이벤트 발생
- once: 한 번만 실행되는 리스너
- 타입 안전한 이벤트 데이터

```typescript
// 여기를 완성하세요
```

---

#### 정답 2: 제네릭 이벤트 시스템

<details>
<summary>정답 보기</summary>

```typescript
// 이벤트 리스너 타입
type EventListener<T> = (data: T) => void;

// 이벤트 맵 타입
type EventMap = {
  [key: string]: any;
};

// 제네릭 이벤트 이미터
class EventEmitter<Events extends EventMap> {
  private listeners: {
    [K in keyof Events]?: EventListener<Events[K]>[];
  } = {};
  
  // 이벤트 리스너 등록
  on<K extends keyof Events>(
    event: K,
    listener: EventListener<Events[K]>
  ): void {
    if (!this.listeners[event]) {
      this.listeners[event] = [];
    }
    this.listeners[event]!.push(listener);
    console.log(\`[EventEmitter] 리스너 등록: \${String(event)}\`);
  }
  
  // 한 번만 실행되는 리스너
  once<K extends keyof Events>(
    event: K,
    listener: EventListener<Events[K]>
  ): void {
    const onceListener: EventListener<Events[K]> = (data) => {
      listener(data);
      this.off(event, onceListener);
    };
    this.on(event, onceListener);
  }
  
  // 이벤트 리스너 제거
  off<K extends keyof Events>(
    event: K,
    listener: EventListener<Events[K]>
  ): void {
    const eventListeners = this.listeners[event];
    if (!eventListeners) return;
    
    const index = eventListeners.indexOf(listener);
    if (index !== -1) {
      eventListeners.splice(index, 1);
      console.log(\`[EventEmitter] 리스너 제거: \${String(event)}\`);
    }
  }
  
  // 이벤트 발생
  emit<K extends keyof Events>(event: K, data: Events[K]): void {
    const eventListeners = this.listeners[event];
    if (!eventListeners || eventListeners.length === 0) {
      console.log(\`[EventEmitter] 리스너 없음: \${String(event)}\`);
      return;
    }
    
    console.log(\`[EventEmitter] 이벤트 발생: \${String(event)}\`);
    eventListeners.forEach(listener => listener(data));
  }
  
  // 모든 리스너 제거
  removeAllListeners<K extends keyof Events>(event?: K): void {
    if (event) {
      delete this.listeners[event];
      console.log(\`[EventEmitter] 모든 리스너 제거: \${String(event)}\`);
    } else {
      this.listeners = {};
      console.log("[EventEmitter] 전체 리스너 제거");
    }
  }
  
  // 리스너 개수
  listenerCount<K extends keyof Events>(event: K): number {
    return this.listeners[event]?.length || 0;
  }
}

// 사용 예시: 사용자 이벤트
interface UserEvents {
  login: { username: string; timestamp: Date };
  logout: { username: string };
  profileUpdate: { username: string; changes: string[] };
}

const userEmitter = new EventEmitter<UserEvents>();

// 로그인 이벤트 리스너
userEmitter.on("login", (data) => {
  console.log(\`✅ \${data.username}님이 로그인했습니다.\`);
  console.log(\`   시간: \${data.timestamp.toLocaleString()}\`);
});

userEmitter.on("login", (data) => {
  console.log(\`📊 로그인 로그 기록: \${data.username}\`);
});

// 로그아웃 이벤트 리스너
userEmitter.on("logout", (data) => {
  console.log(\`👋 \${data.username}님이 로그아웃했습니다.\`);
});

// 프로필 업데이트 이벤트 (한 번만)
userEmitter.once("profileUpdate", (data) => {
  console.log(\`🔄 \${data.username}님의 프로필이 업데이트되었습니다.\`);
  console.log(\`   변경사항: \${data.changes.join(", ")}\`);
});

// 이벤트 발생
console.log("\n=== 이벤트 테스트 ===\n");

userEmitter.emit("login", {
  username: "홍길동",
  timestamp: new Date()
});

console.log();

userEmitter.emit("profileUpdate", {
  username: "홍길동",
  changes: ["이메일", "전화번호"]
});

console.log();

// 같은 이벤트를 다시 발생시켜도 once 리스너는 실행 안 됨
userEmitter.emit("profileUpdate", {
  username: "홍길동",
  changes: ["주소"]
});

console.log();

userEmitter.emit("logout", {
  username: "홍길동"
});

// 리스너 개수 확인
console.log(\`\n로그인 이벤트 리스너 수: \${userEmitter.listenerCount("login")}\`);

// 실전 예시: 주문 시스템
interface OrderEvents {
  orderCreated: {
    orderId: string;
    customerId: string;
    total: number;
  };
  orderShipped: {
    orderId: string;
    trackingNumber: string;
  };
  orderDelivered: {
    orderId: string;
    deliveredAt: Date;
  };
}

const orderEmitter = new EventEmitter<OrderEvents>();

// 주문 생성 시
orderEmitter.on("orderCreated", (data) => {
  console.log(\`\n📦 새 주문: \${data.orderId}\`);
  console.log(\`   고객: \${data.customerId}\`);
  console.log(\`   금액: \${data.total.toLocaleString()}원\`);
});

orderEmitter.on("orderCreated", (data) => {
  console.log(\`📧 주문 확인 이메일 발송: \${data.customerId}\`);
});

// 배송 시작 시
orderEmitter.on("orderShipped", (data) => {
  console.log(\`\n🚚 배송 시작: \${data.orderId}\`);
  console.log(\`   송장번호: \${data.trackingNumber}\`);
});

// 배송 완료 시
orderEmitter.on("orderDelivered", (data) => {
  console.log(\`\n✅ 배송 완료: \${data.orderId}\`);
  console.log(\`   완료 시간: \${data.deliveredAt.toLocaleString()}\`);
});

// 주문 처리 시뮬레이션
console.log("\n=== 주문 처리 시뮬레이션 ===");

orderEmitter.emit("orderCreated", {
  orderId: "ORD-001",
  customerId: "USER-123",
  total: 50000
});

setTimeout(() => {
  orderEmitter.emit("orderShipped", {
    orderId: "ORD-001",
    trackingNumber: "TRK-123456789"
  });
}, 1000);

setTimeout(() => {
  orderEmitter.emit("orderDelivered", {
    orderId: "ORD-001",
    deliveredAt: new Date()
  });
}, 2000);
```

</details>

---

### 과제 3: 제네릭 폼 빌더

제네릭을 활용하여 타입 안전한 폼 빌더를 만들어보세요.

**요구사항:**
- FormBuilder 클래스 구현
- 필드 추가 (텍스트, 숫자, 이메일 등)
- 유효성 검사
- 폼 데이터 가져오기
- 에러 메시지 관리

```typescript
// 여기를 완성하세요
```

---

#### 정답 3: 제네릭 폼 빌더

<details>
<summary>정답 보기</summary>

```typescript
// 필드 타입
type FieldType = "text" | "number" | "email" | "password";

// 유효성 검사 함수 타입
type Validator<T> = (value: T) => string | null;

// 필드 설정
interface FieldConfig<T> {
  type: FieldType;
  label: string;
  required?: boolean;
  validators?: Validator<T>[];
  defaultValue?: T;
}

// 필드 상태
interface FieldState<T> {
  value: T;
  error: string | null;
  touched: boolean;
}

// 폼 데이터 타입 추출
type FormData<T> = {
  [K in keyof T]: T[K] extends FieldConfig<infer U> ? U : never;
};

// 제네릭 폼 빌더
class FormBuilder<T extends Record<string, FieldConfig<any>>> {
  private config: T;
  private state: { [K in keyof T]: FieldState<any> } = {} as any;
  
  constructor(config: T) {
    this.config = config;
    this.initializeState();
  }
  
  // 상태 초기화
  private initializeState(): void {
    for (const key in this.config) {
      const field = this.config[key];
      this.state[key] = {
        value: field.defaultValue ?? this.getDefaultValue(field.type),
        error: null,
        touched: false
      };
    }
  }
  
  // 타입별 기본값
  private getDefaultValue(type: FieldType): any {
    switch (type) {
      case "text":
      case "email":
      case "password":
        return "";
      case "number":
        return 0;
      default:
        return "";
    }
  }
  
  // 필드 값 설정
  setValue<K extends keyof T>(
    field: K,
    value: FormData<T>[K]
  ): void {
    this.state[field].value = value;
    this.state[field].touched = true;
    this.validate(field);
  }
  
  // 필드 값 가져오기
  getValue<K extends keyof T>(field: K): FormData<T>[K] {
    return this.state[field].value;
  }
  
  // 필드 유효성 검사
  private validate<K extends keyof T>(field: K): void {
    const fieldConfig = this.config[field];
    const fieldState = this.state[field];
    const value = fieldState.value;
    
    // 필수 필드 검사
    if (fieldConfig.required && !value) {
      fieldState.error = \`\${fieldConfig.label}은(는) 필수 항목입니다.\`;
      return;
    }
    
    // 커스텀 유효성 검사
    if (fieldConfig.validators) {
      for (const validator of fieldConfig.validators) {
        const error = validator(value);
        if (error) {
          fieldState.error = error;
          return;
        }
      }
    }
    
    fieldState.error = null;
  }
  
  // 전체 유효성 검사
  validateAll(): boolean {
    let isValid = true;
    
    for (const key in this.config) {
      this.validate(key);
      if (this.state[key].error) {
        isValid = false;
      }
    }
    
    return isValid;
  }
  
  // 에러 메시지 가져오기
  getError<K extends keyof T>(field: K): string | null {
    return this.state[field].error;
  }
  
  // 모든 에러 가져오기
  getAllErrors(): { [K in keyof T]?: string } {
    const errors: any = {};
    
    for (const key in this.state) {
      if (this.state[key].error) {
        errors[key] = this.state[key].error;
      }
    }
    
    return errors;
  }
  
  // 폼 데이터 가져오기
  getData(): FormData<T> {
    const data: any = {};
    
    for (const key in this.state) {
      data[key] = this.state[key].value;
    }
    
    return data;
  }
  
  // 폼 리셋
  reset(): void {
    this.initializeState();
    console.log("폼이 초기화되었습니다.");
  }
  
  // 폼 출력
  print(): void {
    console.log("\n=== 폼 상태 ===");
    
    for (const key in this.config) {
      const config = this.config[key];
      const state = this.state[key];
      
      console.log(\`\n[\${config.label}]\`);
      console.log(\`  값: \${state.value}\`);
      console.log(\`  에러: \${state.error || "없음"}\`);
      console.log(\`  수정됨: \${state.touched ? "예" : "아니오"}\`);
    }
  }
}

// 유효성 검사 유틸리티
const Validators = {
  minLength: (min: number): Validator<string> => {
    return (value: string) => {
      if (value.length < min) {
        return \`최소 \${min}자 이상 입력해주세요.\`;
      }
      return null;
    };
  },
  
  maxLength: (max: number): Validator<string> => {
    return (value: string) => {
      if (value.length > max) {
        return \`최대 \${max}자까지 입력 가능합니다.\`;
      }
      return null;
    };
  },
  
  email: (): Validator<string> => {
    return (value: string) => {
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailRegex.test(value)) {
        return "올바른 이메일 형식이 아닙니다.";
      }
      return null;
    };
  },
  
  min: (min: number): Validator<number> => {
    return (value: number) => {
      if (value < min) {
        return \`\${min} 이상의 값을 입력해주세요.\`;
      }
      return null;
    };
  },
  
  max: (max: number): Validator<number> => {
    return (value: number) => {
      if (value > max) {
        return \`\${max} 이하의 값을 입력해주세요.\`;
      }
      return null;
    };
  }
};

// 사용 예시: 회원가입 폼
const signupForm = new FormBuilder({
  username: {
    type: "text",
    label: "사용자명",
    required: true,
    validators: [
      Validators.minLength(3),
      Validators.maxLength(20)
    ]
  },
  email: {
    type: "email",
    label: "이메일",
    required: true,
    validators: [Validators.email()]
  },
  password: {
    type: "password",
    label: "비밀번호",
    required: true,
    validators: [Validators.minLength(8)]
  },
  age: {
    type: "number",
    label: "나이",
    required: true,
    validators: [
      Validators.min(14),
      Validators.max(120)
    ],
    defaultValue: 20
  }
});

console.log("=== 회원가입 폼 테스트 ===");

// 초기 상태
signupForm.print();

// 값 입력
console.log("\n--- 값 입력 ---");
signupForm.setValue("username", "홍길동");
signupForm.setValue("email", "hong@example.com");
signupForm.setValue("password", "password123");
signupForm.setValue("age", 25);

// 현재 상태
signupForm.print();

// 유효성 검사
console.log("\n--- 유효성 검사 ---");
if (signupForm.validateAll()) {
  console.log("✅ 모든 필드가 유효합니다!");
  console.log("\n제출 데이터:", signupForm.getData());
} else {
  console.log("❌ 유효하지 않은 필드가 있습니다.");
  console.log("에러:", signupForm.getAllErrors());
}

// 잘못된 값으로 테스트
console.log("\n--- 잘못된 값 테스트 ---");
signupForm.setValue("username", "ab");  // 너무 짧음
signupForm.setValue("email", "invalid-email");  // 잘못된 형식
signupForm.setValue("password", "short");  // 너무 짧음
signupForm.setValue("age", 10);  // 너무 어림

signupForm.print();

if (!signupForm.validateAll()) {
  console.log("\n❌ 다음 에러들이 발견되었습니다:");
  const errors = signupForm.getAllErrors();
  for (const [field, error] of Object.entries(errors)) {
    console.log(\`  - \${field}: \${error}\`);
  }
}

// 폼 리셋
console.log("\n--- 폼 리셋 ---");
signupForm.reset();
signupForm.print();

// 실전 예시: 상품 등록 폼
const productForm = new FormBuilder({
  name: {
    type: "text",
    label: "상품명",
    required: true,
    validators: [Validators.minLength(2)]
  },
  price: {
    type: "number",
    label: "가격",
    required: true,
    validators: [Validators.min(0)]
  },
  stock: {
    type: "number",
    label: "재고",
    required: true,
    validators: [Validators.min(0)],
    defaultValue: 0
  },
  description: {
    type: "text",
    label: "설명",
    required: false,
    validators: [Validators.maxLength(500)]
  }
});

console.log("\n\n=== 상품 등록 폼 테스트 ===");

productForm.setValue("name", "노트북");
productForm.setValue("price", 1000000);
productForm.setValue("stock", 10);
productForm.setValue("description", "고성능 노트북입니다.");

if (productForm.validateAll()) {
  console.log("✅ 상품 정보가 유효합니다!");
  console.log("\n상품 데이터:", productForm.getData());
}
```

</details>

---

## ✅ 체크포인트

이번 챕터를 마치기 전에 다음 항목을 확인해보세요:

- [ ] 제네릭이 왜 필요한지 이해한다
- [ ] 제네릭 함수를 작성할 수 있다
- [ ] 여러 타입 매개변수를 사용할 수 있다
- [ ] 제네릭 인터페이스와 타입을 정의할 수 있다
- [ ] 제네릭 제약조건을 사용할 수 있다
- [ ] keyof를 사용하여 객체의 키로 제약할 수 있다
- [ ] 제네릭 클래스를 만들 수 있다
- [ ] 제네릭 유틸리티 함수를 작성할 수 있다
- [ ] 언제 제네릭을 사용해야 하는지 안다

### 자가 진단 퀴즈

**1. 다음 중 제네릭 함수의 올바른 정의는?**

```typescript
A. function identity<T>(value: T): T { return value; }
B. function identity(T)(value: T): T { return value; }
C. function identity<T: any>(value: T): T { return value; }
D. function identity[T](value: T): T { return value; }
```

<details>
<summary>정답 보기</summary>

**정답: A**

제네릭 함수는 함수 이름 뒤에 `<T>`를 붙여 정의합니다.

```typescript
function identity<T>(value: T): T {
  return value;
}
```

</details>

---

**2. 다음 코드의 출력 결과는?**

```typescript
function getFirst<T>(arr: T[]): T {
  return arr[0];
}

const result = getFirst([1, 2, 3]);
```

A. result의 타입은 any  
B. result의 타입은 number  
C. result의 타입은 T  
D. 컴파일 에러

<details>
<summary>정답 보기</summary>

**정답: B**

TypeScript가 배열의 타입을 보고 `T`를 `number`로 추론하므로, `result`의 타입은 `number`입니다.

</details>

---

**3. 제네릭 제약조건의 올바른 사용은?**

```typescript
A. function fn<T extends string>(value: T): T { }
B. function fn<T: string>(value: T): T { }
C. function fn<T inherits string>(value: T): T { }
D. function fn<T = string>(value: T): T { }
```

<details>
<summary>정답 보기</summary>

**정답: A**

제네릭 제약조건은 `extends` 키워드를 사용합니다.

```typescript
function fn<T extends string>(value: T): T {
  return value;
}
```

D는 기본 타입 지정이지 제약조건이 아닙니다.

</details>

---

**4. keyof를 사용한 제네릭의 의미는?**

```typescript
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key];
}
```

A. K는 아무 타입이나 가능  
B. K는 T의 키 중 하나여야 함  
C. K는 문자열이어야 함  
D. K는 T와 같은 타입이어야 함

<details>
<summary>정답 보기</summary>

**정답: B**

`K extends keyof T`는 K가 T 객체의 키 중 하나여야 한다는 의미입니다.

```typescript
const person = { name: "홍길동", age: 25 };
getProperty(person, "name");  // ✅ OK
getProperty(person, "email"); // ❌ 에러! email은 person의 키가 아님
```

</details>

---

**5. 다음 중 제네릭을 사용하지 않아도 되는 경우는?**

A. 여러 타입에서 동작하는 유틸리티 함수  
B. 타입 안전한 컬렉션 클래스  
C. 특정 타입만 다루는 단순 함수  
D. 재사용 가능한 컴포넌트

<details>
<summary>정답 보기</summary>

**정답: C**

특정 타입만 다루는 함수라면 제네릭이 필요하지 않습니다.

```typescript
// 제네릭 불필요
function addNumbers(a: number, b: number): number {
  return a + b;
}

// 제네릭 필요
function getFirst<T>(arr: T[]): T {
  return arr[0];
}
```

</details>

---

## 💡 학습 팁

### 제네릭 마스터하기

**1. 간단한 예제부터 시작하기**

제네릭이 처음에는 어려워 보일 수 있습니다. 간단한 예제부터 차근차근 연습하세요.

```typescript
// 1단계: 가장 간단한 제네릭
function identity<T>(value: T): T {
  return value;
}

// 2단계: 배열 다루기
function getFirst<T>(arr: T[]): T {
  return arr[0];
}

// 3단계: 여러 타입 매개변수
function pair<T, U>(first: T, second: U): [T, U] {
  return [first, second];
}

// 4단계: 제약조건 추가
function getLength<T extends { length: number }>(item: T): number {
  return item.length;
}
```

**2. 제네릭과 any의 차이 명확히 하기**

```typescript
// ❌ any 사용 - 타입 안정성 잃음
function badExample(value: any): any {
  return value;
}

const result1 = badExample("hello");
result1.toFixed();  // 컴파일 에러 없음, 런타임 에러!

// ✅ 제네릭 사용 - 타입 안정성 유지
function goodExample<T>(value: T): T {
  return value;
}

const result2 = goodExample("hello");
// result2.toFixed();  // ✅ 컴파일 에러! string에는 toFixed가 없음
```

**3. 실전 프로젝트에 적용하기**

배운 내용을 실제 프로젝트에 적용해보세요:
- API 응답 래퍼 만들기
- 상태 관리 시스템 만들기
- 컬렉션 유틸리티 만들기

**4. TypeScript 내장 제네릭 타입 활용하기**

TypeScript는 많은 유용한 제네릭 유틸리티 타입을 제공합니다.

```typescript
// Partial: 모든 속성을 선택적으로
interface User {
  name: string;
  age: number;
  email: string;
}

type PartialUser = Partial<User>;
// { name?: string; age?: number; email?: string; }

// Pick: 특정 속성만 선택
type UserBasic = Pick<User, "name" | "email">;
// { name: string; email: string; }

// Omit: 특정 속성 제외
type UserWithoutEmail = Omit<User, "email">;
// { name: string; age: number; }

// Readonly: 읽기 전용으로
type ReadonlyUser = Readonly<User>;
// { readonly name: string; readonly age: number; readonly email: string; }
```

### 일반적인 실수와 해결법

**실수 1: 불필요한 제네릭**

```typescript
// ❌ 제네릭이 불필요함
function add<T>(a: number, b: number): number {
  return a + b;
}

// ✅ 간단하게
function add(a: number, b: number): number {
  return a + b;
}
```

**실수 2: 제약조건 없이 속성 접근**

```typescript
// ❌ T에 length가 있다는 보장이 없음
function getLength<T>(item: T): number {
  // return item.length;  // 에러!
}

// ✅ 제약조건 추가
function getLength<T extends { length: number }>(item: T): number {
  return item.length;  // OK!
}
```

**실수 3: 너무 복잡한 제네릭**

```typescript
// ❌ 너무 복잡함
function complex<T, U, V, W, X>(a: T, b: U, c: V, d: W): X {
  // ...
}

// ✅ 간단하고 명확하게
function simple<Input, Output>(data: Input): Output {
  // ...
}
```

### AI 활용 팁

**효과적인 질문 방법:**

```
1. "제네릭을 사용한 배열 유틸리티 함수 예제를 보여줘"
   → 구체적인 예제 요청

2. "이 함수를 제네릭으로 변환해줘: [코드]"
   → 코드와 함께 요청

3. "제네릭 제약조건을 언제 사용해야 하나요?"
   → 사용 시점에 대한 질문

4. "keyof와 extends를 함께 사용하는 예제를 알려줘"
   → 구체적인 문법 조합 요청
```

---

## 🎯 다음 챕터 미리보기

이것으로 TypeScript Part의 모든 기초 챕터를 완료했습니다! 🎉

지금까지 배운 내용:
- Chapter 6: TypeScript 기초
- Chapter 7: 기본 타입 시스템
- Chapter 8: 함수 타입
- Chapter 9: 인터페이스와 타입 별칭
- Chapter 10: 제네릭

**다음 단계는?**

이제 여러분은 TypeScript의 핵심 개념을 모두 배웠습니다! 이 지식을 바탕으로:

1. **실전 프로젝트 만들기**
   - Todo 앱
   - 쇼핑몰
   - 블로그 시스템

2. **React와 TypeScript**
   - React 컴포넌트에 타입 적용
   - Props와 State 타이핑
   - 커스텀 훅 만들기

3. **고급 TypeScript**
   - 고급 타입 (Conditional Types, Mapped Types)
   - 데코레이터
   - 모듈 시스템

**미리 준비하기:**
- Chapter 10의 모든 과제를 완성하세요
- 지금까지 배운 모든 개념을 복습하세요
- 작은 프로젝트를 하나 만들어보세요

---

**축하합니다! Chapter 10을 완료했습니다! 🎉**

제네릭을 마스터했습니다! 이제 여러분은:
- ✅ 재사용 가능한 타입 안전한 코드를 작성할 수 있다
- ✅ 제네릭 함수, 인터페이스, 클래스를 만들 수 있다
- ✅ 제네릭 제약조건을 활용할 수 있다
- ✅ 복잡한 타입 시스템을 설계할 수 있다

TypeScript의 가장 강력한 기능 중 하나인 제네릭을 완전히 이해했습니다! 이제 여러분은 프로페셔널한 TypeScript 개발자로 한 걸음 더 나아갔습니다! 💪

**🎊 TypeScript 기초 과정을 모두 완료했습니다!**

이제 실전 프로젝트를 만들며 배운 내용을 활용해보세요! 화이팅! 🚀
