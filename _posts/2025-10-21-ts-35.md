---
title: "[TS] 유틸리티 타입 - 2"
date: 2025-10-21 15:50:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS, 유틸리티타입]
pin: true
math: true
mermaid: true
---


> **학습목표**
> - 유틸리티의 필요성을 이해한다
> - 타입을 추출하고 변형할 수 있다.
> - 유니온 타입을 조작할 수 있다.
> - 함수 타입을 다룰 수 있다.
> - 고급 패턴을 적용할 수 있다.


## 13.6 Omit<T, K>

### 개념

`Omit<T, K>`는 타입 T에서 **특정 속성 K를 제외**한 나머지로 새로운 타입을 만듭니다. `Pick`의 반대입니다.

**문법:**
```typescript
type Omit = Pick>;
```

> 💡 **읽는 방법**: "T에서 K에 해당하는 속성들을 제외"

### 기본 사용법
```typescript
interface User {
  id: number;
  name: string;
  email: string;
  password: string;
  age: number;
}

// password를 제외한 나머지
type UserWithoutPassword = Omit;

/*
결과:
type UserWithoutPassword = {
  id: number;
  name: string;
  email: string;
  age: number;
}
*/

const user: UserWithoutPassword = {
  id: 1,
  name: "홍길동",
  email: "hong@example.com",
  age: 30
}; // ✅ OK (password 없음)

// 여러 속성 제외
type UserPublic = Omit;

/*
결과:
type UserPublic = {
  id: number;
  name: string;
  age: number;
}
*/
```

### 실전 활용 예제

**1. API 응답에서 민감한 정보 제거**
```typescript
interface UserFromDB {
  id: number;
  username: string;
  email: string;
  password: string;
  salt: string;
  createdAt: Date;
  updatedAt: Date;
}

// 클라이언트에게 보낼 때: 민감한 정보 제거
type UserResponse = Omit;

function getUser(id: number): UserResponse {
  // DB에서 사용자 조회
  const userFromDB: UserFromDB = {
    id: 1,
    username: "hong",
    email: "hong@example.com",
    password: "hashed_password",
    salt: "random_salt",
    createdAt: new Date(),
    updatedAt: new Date()
  };
  
  // password와 salt 제거하여 반환
  const { password, salt, ...userResponse } = userFromDB;
  return userResponse;
}
```

**2. 생성 시 자동 생성 필드 제외**
```typescript
interface Article {
  id: number;
  title: string;
  content: string;
  authorId: number;
  createdAt: Date;
  updatedAt: Date;
  viewCount: number;
}

// 게시글 생성 시: 자동 생성되는 필드 제외
type CreateArticleInput = Omit<Article, 'id' | 'createdAt' | 'updatedAt' | 'viewCount'>;

/*
결과:
type CreateArticleInput = {
  title: string;
  content: string;
  authorId: number;
}
*/

function createArticle(input: CreateArticleInput): Article {
  return {
    id: Date.now(), // 자동 생성
    ...input,
    createdAt: new Date(), // 자동 생성
    updatedAt: new Date(), // 자동 생성
    viewCount: 0 // 초기값
  };
}

// 사용
const newArticle = createArticle({
  title: "TypeScript 입문",
  content: "TypeScript는...",
  authorId: 1
});
```

**3. 상속과 확장**

```typescript
interface BaseEntity {
  id: number;
  createdAt: Date;
  updatedAt: Date;
}

interface User extends BaseEntity {
  username: string;
  email: string;
  password: string;
}

// BaseEntity 필드를 제외한 User만의 필드
type UserOwnFields = Omit<User, keyof BaseEntity>;

/*
결과:
type UserOwnFields = {
  username: string;
  email: string;
  password: string;
}
*/

// 다른 엔티티에 User 필드 추가
interface Admin extends BaseEntity, UserOwnFields {
  role: 'admin' | 'super-admin';
}

const admin: Admin = {
  id: 1,
  createdAt: new Date(),
  updatedAt: new Date(),
  username: "admin",
  email: "admin@example.com",
  password: "hashed",
  role: "admin"
};
```

**4. 컴포넌트 Props 확장**

```typescript
// 기본 버튼 Props
interface ButtonProps {
  onClick: () => void;
  disabled: boolean;
  type: 'button' | 'submit' | 'reset';
  children: string;
}

// 링크 버튼: onClick 대신 href 사용
interface LinkButtonProps extends Omit<ButtonProps, 'onClick' | 'type'> {
  href: string;
  target?: '_blank' | '_self';
}

function LinkButton(props: LinkButtonProps) {
  return `<a href="${props.href}" target="${props.target || '_self'}">${props.children}</a>`;
}

// 사용
const linkBtn: LinkButtonProps = {
  href: "https://example.com",
  disabled: false,
  children: "Click me"
  // onClick과 type은 없음!
};
Pick vs Omit: 언제 무엇을 사용할까?
typescriptinterface User {
  id: number;
  name: string;
  email: string;
  password: string;
  age: number;
  address: string;
  phone: string;
}

// 시나리오 1: 필요한 필드가 적을 때 → Pick 사용
type UserSummary = Pick<User, 'id' | 'name'>; // 2개만 선택

// 시나리오 2: 제외할 필드가 적을 때 → Omit 사용
type UserWithoutPassword = Omit<User, 'password'>; // 1개만 제외

// 👎 비효율적
type UserSummaryBad = Omit<User, 'email' | 'password' | 'age' | 'address' | 'phone'>; // 5개 제외
// 👍 더 효율적
type UserSummaryGood = Pick<User, 'id' | 'name'>; // 2개 선택

// 💡 규칙: 선택할 것이 적으면 Pick, 제외할 것이 적으면 Omit
```

## 13.7 Record<K, T>

### 개념

Record<K, T>는 키 K와 값 T의 타입을 가진 객체 타입을 만듭니다. 객체의 키-값 쌍을 타입으로 정의할 때 유용합니다.

**문법:**
```
typescripttype Record<K extends keyof any, T> = {
  [P in K]: T;
};
```

**💡 읽는 방법:** "K 타입의 키를 가지고, 각 키의 값은 T 타입인 객체"

### 기본 사용법
```typescript
// 문자열 키, 숫자 값
type NumberRecord = Record<string, number>;

const scores: NumberRecord = {
  alice: 90,
  bob: 85,
  charlie: 95
}; // ✅ OK

scores.david = "excellent"; // ❌ 에러! 값은 number여야 함

// 특정 문자열 리터럴 키
type Grades = Record<'math' | 'english' | 'science', number>;

const myGrades: Grades = {
  math: 90,
  english: 85,
  science: 88
}; // ✅ OK

const myGrades2: Grades = {
  math: 90,
  english: 85
  // ❌ 에러! science가 없음
};
```

### 실전 활용 예제

**1. 설정 객체** 
```typescript
type Language = 'ko' | 'en' | 'ja' | 'zh';

// 각 언어별 번역
type Translations = Record<Language, string>;

const welcomeMessage: Translations = {
  ko: "환영합니다",
  en: "Welcome",
  ja: "ようこそ",
  zh: "欢迎"
}; // ✅ 모든 언어가 있어야 함

function getWelcomeMessage(lang: Language): string {
  return welcomeMessage[lang];
}

console.log(getWelcomeMessage('ko')); // "환영합니다"
```

**2. API 엔드포인트 정의**
```typescript
type HttpMethod = 'GET' | 'POST' | 'PUT' | 'DELETE';

interface EndpointConfig {
  url: string;
  auth: boolean;
}

// 각 HTTP 메서드별 엔드포인트 설정
type ApiEndpoints = Record<HttpMethod, EndpointConfig>;

const userEndpoints: ApiEndpoints = {
  GET: { url: '/api/users', auth: true },
  POST: { url: '/api/users', auth: true },
  PUT: { url: '/api/users/:id', auth: true },
  DELETE: { url: '/api/users/:id', auth: true }
};

function request(method: HttpMethod) {
  const config = userEndpoints[method];
  console.log(`${method} ${config.url} (Auth: ${config.auth})`);
}
3. 상태 관리
typescripttype Status = 'idle' | 'loading' | 'success' | 'error';

interface StatusInfo {
  message: string;
  canRetry: boolean;
}

// 각 상태별 정보
const statusMessages: Record<Status, StatusInfo> = {
  idle: { message: "대기 중", canRetry: false },
  loading: { message: "로딩 중...", canRetry: false },
  success: { message: "성공!", canRetry: false },
  error: { message: "오류 발생", canRetry: true }
};

function getStatusInfo(status: Status): StatusInfo {
  return statusMessages[status];
}
```

**4. 유효성 검증 규칙**
```typescript
interface ValidationRule {
  required: boolean;
  minLength?: number;
  maxLength?: number;
  pattern?: RegExp;
}

type FormFields = 'username' | 'email' | 'password' | 'confirmPassword';

// 각 필드별 유효성 검증 규칙
const validationRules: Record<FormFields, ValidationRule> = {
  username: {
    required: true,
    minLength: 3,
    maxLength: 20,
    pattern: /^[a-zA-Z0-9_]+$/
  },
  email: {
    required: true,
    pattern: /^[^\s@]+@[^\s@]+\.[^\s@]+$/
  },
  password: {
    required: true,
    minLength: 8
  },
  confirmPassword: {
    required: true,
    minLength: 8
  }
};

function validateField(field: FormFields, value: string): boolean {
  const rule = validationRules[field];
  
  if (rule.required && !value) {
    return false;
  }
  
  if (rule.minLength && value.length < rule.minLength) {
    return false;
  }
  
  if (rule.maxLength && value.length > rule.maxLength) {
    return false;
  }
  
  if (rule.pattern && !rule.pattern.test(value)) {
    return false;
  }
  
  return true;
}
```

**5. 에러 코드 매핑**
```typescript
type ErrorCode = 'AUTH_001' | 'AUTH_002' | 'DB_001' | 'DB_002' | 'NET_001';

interface ErrorDetail {
  message: string;
  statusCode: number;
  retryable: boolean;
}

const errorMessages: Record<ErrorCode, ErrorDetail> = {
  'AUTH_001': {
    message: "인증 실패",
    statusCode: 401,
    retryable: false
  },
  'AUTH_002': {
    message: "권한 없음",
    statusCode: 403,
    retryable: false
  },
  'DB_001': {
    message: "데이터베이스 연결 실패",
    statusCode: 500,
    retryable: true
  },
  'DB_002': {
    message: "쿼리 실행 실패",
    statusCode: 500,
    retryable: true
  },
  'NET_001': {
    message: "네트워크 오류",
    statusCode: 503,
    retryable: true
  }
};

function handleError(code: ErrorCode) {
  const error = errorMessages[code];
  console.error(`[${code}] ${error.message} (Status: ${error.statusCode})`);
  
  if (error.retryable) {
    console.log("재시도 가능");
  }
}
```

### Record vs Index Signature
```typescript
// Index Signature (인덱스 시그니처)
type IndexSigDict = {
  [key: string]: number;
};

const dict1: IndexSigDict = {
  a: 1,
  b: 2
}; // ✅ OK

dict1.c; // number | undefined (존재하지 않을 수 있음)

// Record
type RecordDict = Record<string, number>;

const dict2: RecordDict = {
  a: 1,
  b: 2
}; // ✅ OK

dict2.c; // number (항상 존재한다고 가정)

// 💡 차이점
// 1. 가독성: Record가 더 명확
// 2. 타입 안전성: 비슷함
// 3. 사용성: Record는 유틸리티 타입과 조합 가능

// 특정 키만 허용하려면 Record 사용
type SpecificKeys = Record<'a' | 'b' | 'c', number>;

const dict3: SpecificKeys = {
  a: 1,
  b: 2,
  c: 3
}; // ✅ 정확히 a, b, c만 가능
```
