---
title: "[TS] 인터페이스와 타입 별칭 - 2"
date: 2025-10-14 15:30:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS, 인터페이스, 타입별칭]
pin: true
math: true
mermaid: true
---

> **학습 목표**
> - 인터페이스로 객체 구조를 정의할 수 있다
> - 타입 별칭을 사용하여 타입에 이름을 붙일 수 있다
> - 인터페이스를 확장하고 병합할 수 있다
> - 인터페이스와 타입 별칭의 차이를 이해하고 적절히 선택할 수 있다

---

## ✏️ 직접 해보기

### 과제 1: 도서관 시스템 만들기

인터페이스를 사용하여 도서관 시스템의 타입을 정의해보세요.

**요구사항:**
- Book 인터페이스: 책 정보 (제목, 저자, ISBN, 출판년도)
- Member 인터페이스: 회원 정보 (ID, 이름, 이메일)
- Borrowing 인터페이스: 대출 정보 (책, 회원, 대출일, 반납일?)
- 책을 대출하는 함수
- 대출 목록을 출력하는 함수

```typescript
// 여기를 완성하세요
```

#### 정답 1: 도서관 시스템 만들기

```typescript
// 책 인터페이스
interface Book {
  isbn: string;
  title: string;
  author: string;
  publishYear: number;
  isAvailable: boolean;
}

// 회원 인터페이스
interface Member {
  id: string;
  name: string;
  email: string;
}

// 대출 정보 인터페이스
interface Borrowing {
  book: Book;
  member: Member;
  borrowDate: Date;
  returnDate?: Date;
}

// 도서관 시스템
class Library {
  private books: Book[] = [];
  private members: Member[] = [];
  private borrowings: Borrowing[] = [];
  
  // 책 추가
  addBook(book: Book): void {
    this.books.push(book);
    console.log(\`책이 추가되었습니다: \${book.title}\`);
  }
  
  // 회원 등록
  registerMember(member: Member): void {
    this.members.push(member);
    console.log(\`회원이 등록되었습니다: \${member.name}\`);
  }
  
  // 책 대출
  borrowBook(isbn: string, memberId: string): boolean {
    const book = this.books.find(b => b.isbn === isbn);
    const member = this.members.find(m => m.id === memberId);
    
    if (!book) {
      console.log("책을 찾을 수 없습니다.");
      return false;
    }
    
    if (!member) {
      console.log("회원을 찾을 수 없습니다.");
      return false;
    }
    
    if (!book.isAvailable) {
      console.log("이미 대출된 책입니다.");
      return false;
    }
    
    book.isAvailable = false;
    const borrowing: Borrowing = {
      book,
      member,
      borrowDate: new Date()
    };
    
    this.borrowings.push(borrowing);
    console.log(\`\${member.name}님이 "\${book.title}"을(를) 대출했습니다.\`);
    return true;
  }
  
  // 책 반납
  returnBook(isbn: string): boolean {
    const borrowingIndex = this.borrowings.findIndex(
      b => b.book.isbn === isbn && !b.returnDate
    );
    
    if (borrowingIndex === -1) {
      console.log("대출 기록을 찾을 수 없습니다.");
      return false;
    }
    
    const borrowing = this.borrowings[borrowingIndex];
    borrowing.returnDate = new Date();
    borrowing.book.isAvailable = true;
    
    console.log(\`"\${borrowing.book.title}"이(가) 반납되었습니다.\`);
    return true;
  }
  
  // 대출 목록 출력
  printBorrowings(): void {
    console.log("\n=== 현재 대출 목록 ===");
    
    const activeBorrowings = this.borrowings.filter(b => !b.returnDate);
    
    if (activeBorrowings.length === 0) {
      console.log("현재 대출 중인 책이 없습니다.");
      return;
    }
    
    activeBorrowings.forEach(b => {
      const days = Math.floor(
        (Date.now() - b.borrowDate.getTime()) / (1000 * 60 * 60 * 24)
      );
      console.log(\`- "\${b.book.title}" (저자: \${b.book.author})\`);
      console.log(\`  대출자: \${b.member.name}\`);
      console.log(\`  대출일: \${b.borrowDate.toLocaleDateString()}\`);
      console.log(\`  경과일: \${days}일\`);
    });
  }
  
  // 이용 가능한 책 목록
  printAvailableBooks(): void {
    console.log("\n=== 이용 가능한 책 ===");
    const available = this.books.filter(b => b.isAvailable);
    
    if (available.length === 0) {
      console.log("현재 이용 가능한 책이 없습니다.");
      return;
    }
    
    available.forEach(book => {
      console.log(\`- "\${book.title}" by \${book.author} (\${book.publishYear})\`);
    });
  }
}

// 테스트
const library = new Library();

// 책 추가
library.addBook({
  isbn: "978-0-12345-678-9",
  title: "TypeScript 완벽 가이드",
  author: "홍길동",
  publishYear: 2024,
  isAvailable: true
});

library.addBook({
  isbn: "978-0-98765-432-1",
  title: "JavaScript 마스터하기",
  author: "김철수",
  publishYear: 2023,
  isAvailable: true
});

// 회원 등록
library.registerMember({
  id: "M001",
  name: "이영희",
  email: "lee@example.com"
});

library.registerMember({
  id: "M002",
  name: "박민수",
  email: "park@example.com"
});

// 도서관 운영
library.printAvailableBooks();

library.borrowBook("978-0-12345-678-9", "M001");
library.borrowBook("978-0-98765-432-1", "M002");

library.printBorrowings();

library.returnBook("978-0-12345-678-9");

library.printBorrowings();
library.printAvailableBooks();
```

---

### 과제 2: 음식 주문 시스템

타입 별칭과 유니온 타입을 사용하여 음식 주문 시스템을 만들어보세요.

**요구사항:**
- MenuItem 타입: 메뉴 항목 (이름, 가격, 카테고리)
- 카테고리: "main", "side", "drink", "dessert" 중 하나
- Order 타입: 주문 정보 (항목들, 총액)
- 주문 생성 함수
- 주문 요약 출력 함수

```typescript
// 여기를 완성하세요
```

#### 정답 2: 음식 주문 시스템

```typescript
// 메뉴 카테고리 타입
type MenuCategory = "main" | "side" | "drink" | "dessert";

// 메뉴 항목 타입
type MenuItem = {
  id: string;
  name: string;
  price: number;
  category: MenuCategory;
  description?: string;
};

// 주문 항목 타입
type OrderItem = {
  menuItem: MenuItem;
  quantity: number;
};

// 주문 상태 타입
type OrderStatus = "pending" | "preparing" | "ready" | "delivered";

// 주문 타입
type Order = {
  orderId: string;
  items: OrderItem[];
  totalPrice: number;
  status: OrderStatus;
  createdAt: Date;
};

// 메뉴판
const menu: MenuItem[] = [
  {
    id: "M001",
    name: "불고기 버거",
    price: 8000,
    category: "main",
    description: "한국식 불고기가 들어간 버거"
  },
  {
    id: "M002",
    name: "치킨 버거",
    price: 7000,
    category: "main",
    description: "바삭한 치킨 패티 버거"
  },
  {
    id: "S001",
    name: "감자튀김",
    price: 3000,
    category: "side",
    description: "바삭한 감자튀김"
  },
  {
    id: "D001",
    name: "콜라",
    price: 2000,
    category: "drink"
  },
  {
    id: "D002",
    name: "아이스크림",
    price: 3500,
    category: "dessert"
  }
];

// 주문 생성 함수
function createOrder(items: OrderItem[]): Order {
  const totalPrice = items.reduce(
    (sum, item) => sum + item.menuItem.price * item.quantity,
    0
  );
  
  return {
    orderId: \`ORD-\${Date.now()}\`,
    items,
    totalPrice,
    status: "pending",
    createdAt: new Date()
  };
}

// 메뉴 찾기
function findMenuItem(id: string): MenuItem | undefined {
  return menu.find(item => item.id === id);
}

// 주문에 항목 추가
function addToOrder(
  items: OrderItem[],
  menuId: string,
  quantity: number
): boolean {
  const menuItem = findMenuItem(menuId);
  
  if (!menuItem) {
    console.log("메뉴를 찾을 수 없습니다.");
    return false;
  }
  
  // 이미 주문에 있는 항목인지 확인
  const existingItem = items.find(item => item.menuItem.id === menuId);
  
  if (existingItem) {
    existingItem.quantity += quantity;
    console.log(\`\${menuItem.name} 수량이 \${existingItem.quantity}개로 증가했습니다.\`);
  } else {
    items.push({ menuItem, quantity });
    console.log(\`\${menuItem.name} \${quantity}개가 추가되었습니다.\`);
  }
  
  return true;
}

// 주문 요약 출력
function printOrderSummary(order: Order): void {
  console.log(\`\n=== 주문 요약 (\${order.orderId}) ===\`);
  console.log(\`주문 시간: \${order.createdAt.toLocaleString()}\`);
  console.log(\`상태: \${getStatusText(order.status)}\n\`);
  
  // 카테고리별로 그룹화
  const categories: Record<MenuCategory, OrderItem[]> = {
    main: [],
    side: [],
    drink: [],
    dessert: []
  };
  
  order.items.forEach(item => {
    categories[item.menuItem.category].push(item);
  });
  
  // 카테고리별 출력
  const categoryNames = {
    main: "메인",
    side: "사이드",
    drink: "음료",
    dessert: "디저트"
  };
  
  Object.entries(categories).forEach(([category, items]) => {
    if (items.length > 0) {
      console.log(\`[\${categoryNames[category as MenuCategory]}]\`);
      items.forEach(item => {
        const subtotal = item.menuItem.price * item.quantity;
        console.log(
          \`  \${item.menuItem.name} × \${item.quantity}개 = \${subtotal.toLocaleString()}원\`
        );
      });
      console.log();
    }
  });
  
  console.log(\`총 금액: \${order.totalPrice.toLocaleString()}원\`);
  console.log("=".repeat(40));
}

// 상태 텍스트 변환
function getStatusText(status: OrderStatus): string {
  const statusTexts = {
    pending: "대기 중",
    preparing: "조리 중",
    ready: "완료",
    delivered: "전달 완료"
  };
  return statusTexts[status];
}

// 주문 상태 변경
function updateOrderStatus(order: Order, status: OrderStatus): void {
  order.status = status;
  console.log(\`\n주문 \${order.orderId}의 상태가 '\${getStatusText(status)}'(으)로 변경되었습니다.\`);
}

// 메뉴 출력
function printMenu(): void {
  console.log("\n=== 메뉴판 ===");
  
  const categories: MenuCategory[] = ["main", "side", "drink", "dessert"];
  const categoryNames = {
    main: "메인 메뉴",
    side: "사이드 메뉴",
    drink: "음료",
    dessert: "디저트"
  };
  
  categories.forEach(category => {
    const items = menu.filter(item => item.category === category);
    if (items.length > 0) {
      console.log(\`\n[\${categoryNames[category]}]\`);
      items.forEach(item => {
        console.log(\`  \${item.id}: \${item.name} - \${item.price.toLocaleString()}원\`);
        if (item.description) {
          console.log(\`      \${item.description}\`);
        }
      });
    }
  });
  console.log();
}

// 테스트
console.log("=== 레스토랑 주문 시스템 ===");

printMenu();

// 주문 생성
const orderItems: OrderItem[] = [];

addToOrder(orderItems, "M001", 2);  // 불고기 버거 2개
addToOrder(orderItems, "S001", 2);  // 감자튀김 2개
addToOrder(orderItems, "D001", 2);  // 콜라 2개
addToOrder(orderItems, "D002", 1);  // 아이스크림 1개

const order = createOrder(orderItems);
printOrderSummary(order);

// 주문 진행
updateOrderStatus(order, "preparing");
setTimeout(() => {
  updateOrderStatus(order, "ready");
}, 1000);
```

---

### 과제 3: 블로그 시스템

인터페이스 확장을 사용하여 블로그 시스템을 만들어보세요.

**요구사항:**
- Content 인터페이스: 모든 콘텐츠의 기본 (ID, 제목, 작성자, 날짜)
- Post 인터페이스: Content를 확장하여 본문, 태그 추가
- Comment 인터페이스: Content를 확장하여 댓글 내용, 게시글 ID 추가
- 게시글 작성 함수
- 댓글 작성 함수
- 게시글과 댓글 출력 함수

```typescript
// 여기를 완성하세요
```

#### 정답 3: 블로그 시스템

```typescript
// 기본 콘텐츠 인터페이스
interface Content {
  readonly id: string;
  title: string;
  author: string;
  createdAt: Date;
  updatedAt?: Date;
}

// 게시글 인터페이스 (Content 확장)
interface Post extends Content {
  body: string;
  tags: string[];
  viewCount: number;
  likeCount: number;
}

// 댓글 인터페이스 (Content 확장)
interface Comment extends Content {
  postId: string;
  content: string;
  parentCommentId?: string;  // 대댓글인 경우
}

// 사용자 인터페이스
interface User {
  id: string;
  username: string;
  email: string;
}

// 블로그 시스템 클래스
class BlogSystem {
  private posts: Post[] = [];
  private comments: Comment[] = [];
  private users: User[] = [];
  
  // 사용자 등록
  registerUser(user: User): void {
    this.users.push(user);
    console.log(\`사용자 등록: \${user.username}\`);
  }
  
  // 게시글 작성
  createPost(
    author: string,
    title: string,
    body: string,
    tags: string[] = []
  ): Post {
    const post: Post = {
      id: \`POST-\${Date.now()}\`,
      title,
      author,
      body,
      tags,
      viewCount: 0,
      likeCount: 0,
      createdAt: new Date()
    };
    
    this.posts.push(post);
    console.log(\`게시글 작성됨: "\${title}"\`);
    return post;
  }
  
  // 댓글 작성
  createComment(
    postId: string,
    author: string,
    content: string,
    parentCommentId?: string
  ): Comment | null {
    const post = this.posts.find(p => p.id === postId);
    
    if (!post) {
      console.log("게시글을 찾을 수 없습니다.");
      return null;
    }
    
    const comment: Comment = {
      id: \`COMMENT-\${Date.now()}\`,
      postId,
      title: \`Re: \${post.title}\`,
      author,
      content,
      parentCommentId,
      createdAt: new Date()
    };
    
    this.comments.push(comment);
    
    if (parentCommentId) {
      console.log(\`대댓글 작성됨 by \${author}\`);
    } else {
      console.log(\`댓글 작성됨 by \${author}\`);
    }
    
    return comment;
  }
  
  // 게시글 조회 (조회수 증가)
  viewPost(postId: string): Post | null {
    const post = this.posts.find(p => p.id === postId);
    
    if (!post) {
      console.log("게시글을 찾을 수 없습니다.");
      return null;
    }
    
    post.viewCount++;
    return post;
  }
  
  // 게시글 좋아요
  likePost(postId: string): boolean {
    const post = this.posts.find(p => p.id === postId);
    
    if (!post) {
      console.log("게시글을 찾을 수 없습니다.");
      return false;
    }
    
    post.likeCount++;
    console.log(\`"\${post.title}" 좋아요! (총 \${post.likeCount}개)\`);
    return true;
  }
  
  // 게시글 상세 출력
  printPost(postId: string): void {
    const post = this.viewPost(postId);
    
    if (!post) return;
    
    console.log("\n" + "=".repeat(60));
    console.log(\`제목: \${post.title}\`);
    console.log(\`작성자: \${post.author}\`);
    console.log(\`작성일: \${post.createdAt.toLocaleString()}\`);
    
    if (post.updatedAt) {
      console.log(\`수정일: \${post.updatedAt.toLocaleString()}\`);
    }
    
    console.log(\`조회수: \${post.viewCount} | 좋아요: \${post.likeCount}\`);
    
    if (post.tags.length > 0) {
      console.log(\`태그: \${post.tags.map(t => \`#\${t}\`).join(" ")}\`);
    }
    
    console.log("-".repeat(60));
    console.log(post.body);
    console.log("=".repeat(60));
    
    // 댓글 출력
    this.printComments(postId);
  }
  
  // 댓글 출력
  printComments(postId: string): void {
    const comments = this.comments.filter(c => c.postId === postId);
    
    if (comments.length === 0) {
      console.log("\n댓글이 없습니다.");
      return;
    }
    
    console.log(\`\n💬 댓글 (\${comments.length}개)\`);
    console.log("-".repeat(60));
    
    // 부모 댓글만 먼저 찾기
    const parentComments = comments.filter(c => !c.parentCommentId);
    
    parentComments.forEach(comment => {
      this.printComment(comment, 0);
      
      // 대댓글 찾기
      const replies = comments.filter(c => c.parentCommentId === comment.id);
      replies.forEach(reply => {
        this.printComment(reply, 1);
      });
    });
  }
  
  // 개별 댓글 출력
  private printComment(comment: Comment, depth: number): void {
    const indent = "  ".repeat(depth);
    const prefix = depth > 0 ? "↳ " : "";
    
    console.log(\`\${indent}\${prefix}\${comment.author}\`);
    console.log(\`\${indent}  \${comment.content}\`);
    console.log(\`\${indent}  \${comment.createdAt.toLocaleString()}\`);
    console.log();
  }
  
  // 태그로 게시글 검색
  searchByTag(tag: string): Post[] {
    return this.posts.filter(post => post.tags.includes(tag));
  }
  
  // 인기 게시글 (좋아요 순)
  getPopularPosts(limit: number = 5): Post[] {
    return [...this.posts]
      .sort((a, b) => b.likeCount - a.likeCount)
      .slice(0, limit);
  }
  
  // 게시글 목록 출력
  printPostList(): void {
    console.log("\n=== 전체 게시글 ===");
    
    if (this.posts.length === 0) {
      console.log("게시글이 없습니다.");
      return;
    }
    
    this.posts.forEach((post, index) => {
      const commentCount = this.comments.filter(c => c.postId === post.id).length;
      console.log(\`\${index + 1}. "\${post.title}" by \${post.author}\`);
      console.log(\`   조회 \${post.viewCount} | 좋아요 \${post.likeCount} | 댓글 \${commentCount}\`);
      if (post.tags.length > 0) {
        console.log(\`   \${post.tags.map(t => \`#\${t}\`).join(" ")}\`);
      }
    });
  }
}

// 테스트
const blog = new BlogSystem();

// 사용자 등록
blog.registerUser({
  id: "U001",
  username: "
홍길동",
  email: "hong@example.com"
});

blog.registerUser({
  id: "U002",
  username: "김철수",
  email: "kim@example.com"
});

blog.registerUser({
  id: "U003",
  username: "이영희",
  email: "lee@example.com"
});

// 게시글 작성
const post1 = blog.createPost(
  "홍길동",
  "TypeScript 인터페이스 완벽 가이드",
  "TypeScript의 인터페이스는 객체의 구조를 정의하는 강력한 도구입니다.\n" +
  "이 글에서는 인터페이스의 기본 사용법부터 고급 기능까지 모두 다룹니다.\n\n" +
  "1. 인터페이스란?\n" +
  "2. 인터페이스 확장\n" +
  "3. 선택적 속성\n" +
  "4. 읽기 전용 속성\n\n" +
  "인터페이스를 잘 활용하면 더 안전하고 유지보수하기 쉬운 코드를 작성할 수 있습니다.",
  ["typescript", "interface", "tutorial"]
);

const post2 = blog.createPost(
  "김철수",
  "JavaScript vs TypeScript",
  "JavaScript와 TypeScript의 차이점을 비교해봅시다.\n\n" +
  "JavaScript는 동적 타입 언어이고, TypeScript는 정적 타입 언어입니다.\n" +
  "TypeScript를 사용하면 개발 단계에서 더 많은 오류를 발견할 수 있습니다.",
  ["javascript", "typescript", "comparison"]
);

// 게시글 목록
blog.printPostList();

// 게시글 상세 보기
blog.printPost(post1.id);

// 좋아요
blog.likePost(post1.id);
blog.likePost(post1.id);
blog.likePost(post2.id);

// 댓글 작성
const comment1 = blog.createComment(
  post1.id,
  "김철수",
  "정말 유익한 글이네요! 인터페이스 확장 부분이 특히 도움이 되었습니다."
);

const comment2 = blog.createComment(
  post1.id,
  "이영희",
  "초보자인데 이해하기 쉽게 설명해주셨네요. 감사합니다!"
);

// 대댓글 작성
if (comment1) {
  blog.createComment(
    post1.id,
    "홍길동",
    "도움이 되셨다니 기쁩니다! 다음에는 타입 별칭에 대해서도 작성해볼게요.",
    comment1.id
  );
}

// 게시글 다시 보기 (댓글 포함)
blog.printPost(post1.id);

// 태그로 검색
console.log("\n=== 'typescript' 태그 게시글 ===");
const typescriptPosts = blog.searchByTag("typescript");
typescriptPosts.forEach(post => {
  console.log(\`- "\${post.title}" by \${post.author}\`);
});

// 인기 게시글
console.log("\n=== 인기 게시글 ===");
const popularPosts = blog.getPopularPosts(3);
popularPosts.forEach((post, index) => {
  console.log(\`\${index + 1}. "\${post.title}" (좋아요 \${post.likeCount}개)\`);
});
```

---

## ✅ 체크포인트

이번 챕터를 마치기 전에 다음 항목을 확인해보세요:

- [ ] 인터페이스로 객체 구조를 정의할 수 있다
- [ ] 선택적 속성과 읽기 전용 속성을 사용할 수 있다
- [ ] 인터페이스에 메서드를 정의할 수 있다
- [ ] 인터페이스를 확장할 수 있다
- [ ] 여러 인터페이스를 동시에 확장할 수 있다
- [ ] 타입 별칭을 정의할 수 있다
- [ ] 유니온 타입에 별칭을 붙일 수 있다
- [ ] 인터페이스와 타입 별칭의 차이를 이해한다
- [ ] 상황에 맞게 인터페이스와 타입 별칭을 선택할 수 있다
- [ ] 인터페이스 병합을 이해한다

### 자가 진단 퀴즈

**1. 다음 중 인터페이스 정의로 올바른 것은?**

```typescript
A. interface User = { name: string; age: number; }
B. interface User { name: string; age: number; }
C. type User interface { name: string; age: number; }
D. interface { name: string; age: number; }
```

**정답: B**

인터페이스는 `interface 이름 { ... }` 형식으로 정의합니다.

```typescript
interface User {
  name: string;
  age: number;
}
```

---

**2. 인터페이스 확장의 올바른 문법은?**

```typescript
A. interface Student : Person { ... }
B. interface Student extends Person { ... }
C. interface Student inherits Person { ... }
D. interface Student <- Person { ... }
```

**정답: B**

인터페이스는 `extends` 키워드로 확장합니다.

```typescript
interface Person {
  name: string;
}

interface Student extends Person {
  studentId: string;
}
```

---

**3. 다음 중 타입 별칭만 가능한 것은?**

A. 객체 타입 정의  
B. 유니온 타입에 이름 붙이기  
C. 인터페이스 확장  
D. 메서드 정의

**정답: B**

유니온 타입에 이름을 붙이는 것은 타입 별칭만 가능합니다.

```typescript
type Status = "pending" | "approved" | "rejected";  // ✅ 타입 별칭만 가능

// interface Status = "pending" | "approved" | "rejected";  // ❌ 불가능
```

---

**4. 인터페이스 병합에 대한 설명으로 올바른 것은?**

A. 타입 별칭도 병합이 가능하다  
B. 같은 이름의 인터페이스를 여러 번 선언하면 자동으로 병합된다  
C. 인터페이스 병합 시 같은 속성을 다른 타입으로 정의할 수 있다  
D. 인터페이스 병합은 명시적으로 merge 키워드를 사용해야 한다

**정답: B**

같은 이름의 인터페이스를 여러 번 선언하면 TypeScript가 자동으로 병합합니다.

```typescript
interface User {
  name: string;
}

interface User {
  age: number;
}

// 자동으로 병합됨
// User = { name: string; age: number; }
```

---

**5. 다음 중 선택적 속성을 나타내는 올바른 문법은?**

```typescript
A. email: string?
B. email?: string
C. email: string | undefined
D. optional email: string
```

**정답: B**

선택적 속성은 속성 이름 뒤에 `?`를 붙입니다. C도 비슷한 의미이지만, 명시적인 선택적 속성 문법은 B입니다.

```typescript
interface User {
  name: string;
  email?: string;  // 선택적 속성
}
```

---

## 💡 학습 팁

### 인터페이스와 타입 별칭 마스터하기

**1. 일관성 있게 사용하기**

프로젝트 내에서 일관된 스타일을 유지하세요. 팀과 함께 작업한다면 컨벤션을 정하는 것이 좋습니다.

```typescript
// 팀 컨벤션 예시:
// - 객체 구조는 interface 사용
// - 유니온/튜플은 type 사용

interface User {
  name: string;
  role: UserRole;  // type으로 정의된 유니온
}

type UserRole = "admin" | "user" | "guest";
```

**2. 작은 인터페이스로 시작하기**

처음부터 큰 인터페이스를 만들지 말고, 작은 인터페이스를 조합하세요.

```typescript
// ✅ 좋은 예: 작은 인터페이스들
interface Named {
  name: string;
}

interface Aged {
  age: number;
}

interface Person extends Named, Aged {
  email: string;
}

// ❌ 나쁜 예: 처음부터 큰 인터페이스
interface Person {
  name: string;
  age: number;
  email: string;
  phone: string;
  address: string;
  city: string;
  country: string;
  // ... 너무 많음
}
```

**3. 명확한 이름 사용하기**

인터페이스와 타입 별칭의 이름은 그 용도를 명확히 알 수 있어야 합니다.

```typescript
// ✅ 명확한 이름
interface UserProfile {
  // ...
}

type OrderStatus = "pending" | "shipped" | "delivered";

// ❌ 불명확한 이름
interface Data {
  // ...
}

type Status = string;  // 너무 일반적
```

**4. 실전 프로젝트에 적용하기**

배운 내용을 실제 프로젝트에 적용해보세요:
- Todo 앱의 타입 정의
- 쇼핑몰의 상품/주문 타입 정의
- 블로그 시스템의 게시글/댓글 타입 정의

### 일반적인 실수와 해결법

**실수 1: 인터페이스와 타입 별칭 혼동**

```typescript
// ❌ 유니온 타입을 인터페이스로
// interface Status = "pending" | "approved";  // 불가능!

// ✅ 유니온 타입은 type으로
type Status = "pending" | "approved";
```

**실수 2: 불필요한 선택적 속성**

```typescript
// ❌ 모든 속성을 선택적으로
interface User {
  name?: string;
  age?: number;
  email?: string;
}

// ✅ 필수 속성은 필수로
interface User {
  name: string;      // 필수
  age: number;       // 필수
  email?: string;    // 선택적 (진짜 선택적인 경우만)
}
```

**실수 3: 인터페이스 너무 크게 만들기**

```typescript
// ❌ 거대한 인터페이스
interface Everything {
  // 100개의 속성...
}

// ✅ 작은 인터페이스로 분리
interface BasicInfo {
  // ...
}

interface ContactInfo {
  // ...
}

interface User extends BasicInfo, ContactInfo {
  // 추가 속성만
}
```

### AI 활용 팁

**효과적인 질문 방법:**

```
1. "인터페이스와 타입 별칭의 차이를 실전 예제와 함께 설명해줘"
   → 비교와 예제 요청

2. "다음 객체에 맞는 인터페이스를 만들어줘: [객체]"
   → 구체적인 객체 제공

3. "여러 인터페이스를 확장하는 방법을 알려줘"
   → 구체적인 기능 질문

4. "이 인터페이스 설계가 올바른지 검토해줘: [코드]"
   → 코드 리뷰 요청
```

---

## 🎯 다음 챕터 미리보기

Chapter 10에서는 **제네릭(Generics)**을 배웁니다!
- 제네릭이란 무엇인가
- 제네릭 함수 작성하기
- 제네릭 인터페이스와 타입
- 제네릭 제약조건
- 실전 제네릭 활용

**미리 준비하기:**
- Chapter 9의 모든 과제를 완성하세요
- 인터페이스와 타입 별칭을 복습하세요
- 함수 타입 지정을 복습하세요

**미리 생각해보기:**

지금까지 배운 함수는 특정 타입만 처리할 수 있었습니다:

```typescript
function getFirstNumber(arr: number[]): number {
  return arr[0];
}

function getFirstString(arr: string[]): string {
  return arr[0];
}
```

같은 로직인데 타입만 다르다면, 중복 없이 하나의 함수로 만들 수는 없을까요? 🤔

다음 챕터에서는 이 문제를 해결하는 강력한 도구, 제네릭을 배워봅시다!

---

**축하합니다! Chapter 9를 완료했습니다! 🎉**

인터페이스와 타입 별칭을 마스터했습니다! 이제 여러분은:
- ✅ 재사용 가능한 타입을 정의할 수 있다
- ✅ 인터페이스를 확장하여 코드를 구조화할 수 있다
- ✅ 상황에 맞게 인터페이스와 타입 별칭을 선택할 수 있다
- ✅ 복잡한 타입 시스템을 설계할 수 있다

이제 타입스크립트의 타입 시스템을 제대로 활용할 수 있습니다! 다음 챕터에서는 더욱 강력한 제네릭을 배워봅시다! 💪

**🚀 Chapter 10에서 만나요!**
