---
title: "[TS] 타입스크립트 시작하기 - 2"
date: 2025-10-13 10:40:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS]
pin: true
math: true
mermaid: true
published: false
---

## 간단한 프로그램 작성하고 실행하기

가장 간단한 타입스크립트 프로그램부터 시작해봅시다.

### Hello World 작성하기

프로젝트 폴더에 `hello.ts` 파일을 만들고 다음 코드를 작성하세요:

```typescript
// hello.ts
const message: string = "Hello, TypeScript!";
console.log(message);

const name: string = "홍길동";
const age: number = 25;

console.log(\`안녕하세요, 저는 \${age}살 \${name}입니다.\`);
```

**컴파일하고 실행:**

```bash
tsc hello.ts
node hello.js
```

**출력:**
```
Hello, TypeScript!
안녕하세요, 저는 25살 홍길동입니다.
```

### 타입 에러 경험하기

이제 일부러 에러를 만들어봅시다. 타입스크립트가 어떻게 에러를 잡아주는지 확인할 수 있습니다.

```typescript
// error.ts
const name: string = "홍길동";
const age: number = "25";  // 에러!
```

**컴파일 시도:**

```bash
tsc error.ts
```

**에러 메시지:**

```
error TS2322: Type 'string' is not assignable to type 'number'.
```

타입스크립트가 코드를 실행하기 전에 에러를 발견했습니다! 이것이 타입스크립트의 가장 큰 장점입니다.

**에러 수정:**

```typescript
const name: string = "홍길동";
const age: number = 25;  // ✅ 올바른 코드
```

---

## 타입 표기 연습

### 변수에 타입 지정하기

다양한 타입을 연습해봅시다.

```typescript
// types-practice.ts

// 문자열
const firstName: string = "길동";
const lastName: string = "홍";
const fullName: string = lastName + firstName;
console.log(fullName);  // 홍길동

// 숫자
const age: number = 25;
const height: number = 175.5;
const year: number = 2024;

console.log(\`나이: \${age}세\`);
console.log(\`키: \${height}cm\`);

// 불린
const isStudent: boolean = true;
const isAdult: boolean = age >= 18;

console.log(\`학생입니까? \${isStudent}\`);
console.log(\`성인입니까? \${isAdult}\`);

// 배열
const numbers: number[] = [1, 2, 3, 4, 5];
const fruits: string[] = ["사과", "바나나", "오렌지"];

console.log(\`숫자 배열: \${numbers}\`);
console.log(\`과일 배열: \${fruits}\`);
```

**실행 결과:**

```
홍길동
나이: 25세
키: 175.5cm
학생입니까? true
성인입니까? true
숫자 배열: 1,2,3,4,5
과일 배열: 사과,바나나,오렌지
```

### 타입 추론 vs 타입 표기

타입스크립트는 값을 보고 자동으로 타입을 추론합니다.

```typescript
// 타입 추론 (Type Inference)
let count = 0;           // number로 자동 추론
let message = "Hello";   // string으로 자동 추론
let isOk = true;         // boolean으로 자동 추론

// 타입 표기 (Type Annotation)
let count2: number = 0;
let message2: string = "Hello";
let isOk2: boolean = true;

// 결과는 동일! 타입스크립트가 똑똑하게 추론함
```

**언제 타입을 명시해야 할까?**

```typescript
// ✅ 나중에 값을 할당할 때
let name: string;
name = "홍길동";

// ✅ 빈 배열로 시작할 때
let numbers: number[] = [];
numbers.push(1);
numbers.push(2);

// ✅ 여러 타입이 가능할 때 (나중에 배움)
let data: string | number;
data = "Hello";
data = 123;
```

---

## ✏️ 직접 해보기

### 과제 1: 자기소개 프로그램

TypeScript로 자신을 소개하는 프로그램을 만들어보세요.

**요구사항:**
- 변수 선언 시 타입 지정
- 이름, 나이, 거주 도시, 취미 저장
- 자기소개 문장 출력

**힌트:**

```typescript
// introduce.ts
const name: string = // 여기를 채우세요
const age: number = // 여기를 채우세요
const city: string = // 여기를 채우세요
const hobby: string = // 여기를 채우세요

console.log(// 자기소개 문장 작성);
```

#### 정답 1: 자기소개 프로그램

```typescript
// introduce.ts
const name: string = "홍길동";
const age: number = 25;
const city: string = "서울";
const hobby: string = "독서";

console.log(\`안녕하세요! 저는 \${name}입니다.\`);
console.log(\`나이는 \${age}세이고, \${city}에 살고 있습니다.\`);
console.log(\`취미는 \${hobby}입니다.\`);

// 또는 한 줄로
console.log(\`안녕하세요! 저는 \${age}세 \${name}입니다. \${city}에 살며 \${hobby}을(를) 좋아합니다.\`);
```

**출력:**
```
안녕하세요! 저는 홍길동입니다.
나이는 25세이고, 서울에 살고 있습니다.
취미는 독서입니다.
```

---

### 과제 2: 점수 계산하기

세 과목의 점수를 저장하고 총점과 평균을 계산해보세요.

**요구사항:**
- 국어, 수학, 영어 점수를 변수에 저장 (타입 지정)
- 총점 계산
- 평균 계산
- 결과 출력

**힌트:**

```typescript
// scores.ts
const korean: number = // 국어 점수
const math: number = // 수학 점수
const english: number = // 영어 점수

const total: number = // 총점 계산
const average: number = // 평균 계산

console.log(// 결과 출력);
```

#### 정답 2: 점수 계산하기

```typescript
// scores.ts
const korean: number = 85;
const math: number = 90;
const english: number = 88;

const total: number = korean + math + english;
const average: number = total / 3;

console.log("=== 성적표 ===");
console.log(\`국어: \${korean}점\`);
console.log(\`수학: \${math}점\`);
console.log(\`영어: \${english}점\`);
console.log(\`총점: \${total}점\`);
console.log(\`평균: \${average.toFixed(2)}점\`);
```

**출력:**
```
=== 성적표 ===
국어: 85점
수학: 90점
영어: 88점
총점: 263점
평균: 87.67점
```

**설명:**
- `.toFixed(2)`: 소수점 2자리까지 표시

---

### 과제 3: 쇼핑 목록 만들기

쇼핑 목록을 배열로 만들고 출력해보세요.

**요구사항:**
- 과일 이름을 문자열 배열로 저장
- 과일 가격을 숫자 배열로 저장
- 모든 과일과 가격 출력

**힌트:**

```typescript
// shopping.ts
const fruits: string[] = // 과일 이름들
const prices: number[] = // 과일 가격들

// 출력하기
```

#### 정답 3: 쇼핑 목록 만들기

```typescript
// shopping.ts
const fruits: string[] = ["사과", "바나나", "오렌지", "포도"];
const prices: number[] = [1000, 1500, 2000, 3000];

console.log("=== 쇼핑 목록 ===");

// 방법 1: 인덱스로 접근
console.log(\`\${fruits[0]}: \${prices[0]}원\`);
console.log(\`\${fruits[1]}: \${prices[1]}원\`);
console.log(\`\${fruits[2]}: \${prices[2]}원\`);
console.log(\`\${fruits[3]}: \${prices[3]}원\`);

// 방법 2: for문 사용 (Part 0에서 배운 내용)
console.log("\n=== for문으로 출력 ===");
for (let i = 0; i < fruits.length; i++) {
  console.log(\`\${fruits[i]}: \${prices[i]}원\`);
}

// 총 금액 계산
let total: number = 0;
for (let i = 0; i < prices.length; i++) {
  total = total + prices[i];
}
console.log(\`\n총 금액: \${total}원\`);
```

**출력:**
```
=== 쇼핑 목록 ===
사과: 1000원
바나나: 1500원
오렌지: 2000원
포도: 3000원

=== for문으로 출력 ===
사과: 1000원
바나나: 1500원
오렌지: 2000원
포도: 3000원

총 금액: 7500원
```

---

## ✅ 체크포인트

이번 챕터를 마치기 전에 다음 항목을 확인해보세요:

- [ ] 타입스크립트가 무엇인지 이해한다
- [ ] JavaScript의 문제점과 타입스크립트의 필요성을 안다
- [ ] 타입스크립트를 설치하고 실행할 수 있다
- [ ] 변수에 타입을 지정할 수 있다 (string, number, boolean)
- [ ] 배열에 타입을 지정할 수 있다
- [ ] 타입 에러가 발생했을 때 읽고 이해할 수 있다
- [ ] 타입 추론과 타입 표기의 차이를 안다

### 자가 진단 퀴즈

**1. 다음 중 TypeScript의 장점이 아닌 것은?**

A. 실행 전에 에러를 발견할 수 있다  
B. 자동 완성이 강력하다  
C. 실행 속도가 JavaScript보다 빠르다  
D. 코드의 의도가 명확해진다

**정답: C**

TypeScript는 컴파일 후 JavaScript로 변환되어 실행되므로, 실행 속도는 JavaScript와 동일합니다. TypeScript의 장점은 개발 단계에서 타입 체크를 통해 에러를 미리 발견하고, 개발 생산성을 높이는 것입니다.

---

**2. 다음 코드 중 올바른 것은?**

\`\`\`typescript
A. const name: string = "홍길동";
B. const age: number = "25";
C. const isOk: boolean = "true";
D. const count: string = 100;
\`\`\`

**정답: A**

- A: ✅ string 타입에 문자열 할당
- B: ❌ number 타입에 문자열 할당
- C: ❌ boolean 타입에 문자열 할당
- D: ❌ string 타입에 숫자 할당

---

**3. TypeScript 파일의 확장자는?**

A. .js  
B. .ts  
C. .txt  
D. .type

**정답: B**

TypeScript 파일은 `.ts` 확장자를 사용합니다. 컴파일하면 `.js` 파일이 생성됩니다.

---

**4. 다음 코드의 컴파일 결과는?**

```typescript
const message: string = "Hello";
console.log(message);
```

A. 컴파일 에러  
B. 경고만 표시  
C. 정상 컴파일  
D. 아무것도 일어나지 않음

**정답: C**

타입이 올바르게 지정되어 있으므로 정상적으로 컴파일됩니다.

---

**5. 다음 중 타입을 명시하지 않아도 되는 경우는?**

```typescript
A. let count = 0;
B. let data;
C. let numbers = [];
D. const name;
```

**정답: A**

`let count = 0;`은 타입스크립트가 자동으로 number로 추론하므로 타입을 명시하지 않아도 됩니다.

- B: 타입을 알 수 없으므로 명시 필요
- C: 빈 배열은 타입 명시 권장
- D: 문법 에러 (const는 선언 시 값 할당 필요)

---

## 💡 학습 팁

### TypeScript 마스터하기

**1. 에러 메시지를 두려워하지 마세요**

TypeScript의 에러 메시지는 친절합니다. 꼼꼼히 읽으면 문제를 쉽게 해결할 수 있습니다.

\`\`\`typescript
function greet(name: string): string {
  return name;
}

greet(123);
// Error: Argument of type 'number' is not assignable to parameter of type 'string'.
//        ↑ 무엇이 문제인지 명확하게 알려줌
\`\`\`

**2. 타입 추론을 활용하되, 명시가 필요할 때는 명시하세요**

\`\`\`typescript
// ✅ 추론 활용
let count = 0;  // number로 추론

// ✅ 명시 필요
let data: any;  // 나중에 할당
function greet(name: string) { }  // 매개변수는 항상 명시
\`\`\`

**3. 작은 프로젝트부터 시작하세요**

- 간단한 계산기
- Todo 리스트
- 학생 관리 시스템

작은 프로젝트를 완성하며 실력을 쌓으세요.

**4. JavaScript 코드를 TypeScript로 변환해보세요**

Part 0에서 작성한 JavaScript 코드에 타입을 추가하며 연습하세요.

\`\`\`javascript
// JavaScript
function add(a, b) {
  return a + b;
}
\`\`\`

\`\`\`typescript
// TypeScript
function add(a: number, b: number): number {
  return a + b;
}
\`\`\`

### AI 활용 팁

**효과적인 질문 방법:**

\`\`\`
1. "이 JavaScript 코드를 TypeScript로 변환해줘"
   → JavaScript 코드를 함께 제공

2. "이 타입 에러를 어떻게 해결하나요?"
   → 에러 메시지 전체를 복사해서 제공

3. "interface와 type의 차이를 예제와 함께 설명해줘"
   → 구체적인 예제 요청

4. "이 코드의 타입을 더 안전하게 만들려면?"
   → 개선하고 싶은 코드 제공
\`\`\`

---

## 🎯 다음 챕터 미리보기

Chapter 7에서는 **기본 타입 시스템**을 자세히 배웁니다!
- 원시 타입 (string, number, boolean 등)
- 배열과 튜플
- 객체 타입
- any, unknown, void
- 유니온 타입

**미리 준비하기:**
- Chapter 6의 과제를 모두 완성하세요
- 타입 에러를 읽고 이해하는 연습을 하세요
- Part 0의 JavaScript 코드를 복습하세요

---

**축하합니다! Chapter 6을 완료했습니다! 🎉**

TypeScript의 첫 걸음을 뗐습니다! 이제 여러분은:
- ✅ TypeScript가 무엇인지 안다
- ✅ 변수에 타입을 지정할 수 있다
- ✅ TypeScript 코드를 작성하고 실행할 수 있다

다음 챕터에서는 더 다양한 타입을 배워봅시다! 💪

**🚀 Chapter 7에서 만나요!**

---

**축하합니다! Chapter 6을 완료했습니다! 🎉**

TypeScript의 기초를 배웠습니다. 이제 여러분은 TypeScript가 무엇인지, 왜 사용하는지, 어떻게 설정하는지 알게 되었습니다.

다음 챕터에서는 TypeScript의 타입 시스템을 더 깊이 배워봅시다! 💪

