---
title: "[TS] 유틸리티 타입 - 1"
date: 2025-10-21 15:030:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS, 유틸리티타입]
pin: true
math: true
mermaid: true
---

TypeScript의 기본 문법과 객체지향 프로그래밍을 마스터했다면, 이제 TypeScript의 진정한 힘을 발휘할 시간입니다. 이제부터 타입 시스템의 고급 기능들을 다룹니다. 유틸리티 타입으로 기존 타입을 변형하고, 조건부 타입과 매핑 타입으로 동적인 타입을 생성하며, 모듈 시스템으로 코드를 체계적으로 관리하는 방법을 배웁니다. 데코레이터를 활용해 메타프로그래밍을 경험하고, tsconfig.json 설정을 최적화하여 프로젝트 전체의 타입 안전성과 성능을 극대화합니다. 이 Part를 마치면 TypeScript를 단순히 사용하는 수준을 넘어, TypeScript 타입 시스템의 모든 기능을 자유자재로 다루는 고급 개발자가 될 것입니다.

---

# 유틸리티 타입

> **학습목표**
> - 유틸리티의 필요성을 이해한다
> - 타입을 추출하고 변형할 수 있다.
> - 유니온 타입을 조작할 수 있다.
> - 함수 타입을 다룰 수 있다.
> - 고급 패턴을 적용할 수 있다.

## 왜 유틸리티 타입이 필요한가?

실제 개발을 하다 보면 기존에 정의한 타입을 **조금씩 변형**해야 하는 상황이 자주 발생합니다.

**흔한 상황들:**
```typescript
// 상황 1: 사용자 정보 타입
interface User {
  id: number;
  name: string;
  email: string;
  age: number;
}

// 회원가입 시에는 id가 없어요 (서버에서 자동 생성)
// 모든 필드를 선택적으로 만들고 싶어요

// 상황 2: 사용자 수정 시에는 일부 필드만 업데이트
// 특정 필드만 선택하고 싶어요

// 상황 3: 읽기 전용 사용자 정보
// 모든 필드를 수정 불가능하게 만들고 싶어요
```

**비효율적인 방법:**
```typescript
// 매번 새로운 인터페이스를 만들어야 해요
interface UserWithoutId {
  name: string;
  email: string;
  age: number;
}

interface UserOptional {
  id?: number;
  name?: string;
  email?: string;
  age?: number;
}

interface ReadonlyUser {
  readonly id: number;
  readonly name: string;
  readonly email: string;
  readonly age: number;
}

// 필드가 추가되면 모든 인터페이스를 수정해야 해요... 😱
```

**유틸리티 타입을 사용하면:**
```typescript
// ✅ 기존 타입을 재사용하며 변형!
type UserWithoutId = Omit<User, 'id'>;
type UserOptional = Partial<User>;
type ReadonlyUser = Readonly<User>;

// 필드가 추가되어도 자동으로 반영됩니다! 🎉
```

**유틸리티 타입의 장점:**
```
1. 코드 재사용성 ↑
   - 기존 타입을 변형하여 새로운 타입 생성
   
2. 유지보수성 ↑
   - 원본 타입만 수정하면 모든 변형 타입에 자동 반영
   
3. 타입 안전성 ↑
   - 컴파일러가 타입 관계를 정확히 추적
   
4. 개발 생산성 ↑
   - 반복적인 타입 정의 작업 감소
```

TypeScript는 이런 상황을 위해 **내장 유틸리티 타입**을 제공합니다. 이제 하나씩 배워봅시다!

---

## Partial<T>

### 개념

`Partial<T>`는 타입 T의 **모든 속성을 선택적(optional)**으로 만듭니다.

**문법:**
```typescript
type Partial<T> = {
  [P in keyof T]?: T[P];
};
```

> 💡 **읽는 방법**: "T의 모든 키 P에 대해, T[P] 타입을 가지되 선택적으로"

### 기본 사용법
```typescript
interface User {
  id: number;
  name: string;
  email: string;
  age: number;
}

// 모든 속성이 선택적이 됩니다
type PartialUser = Partial<User>;

/*
결과:
type PartialUser = {
  id?: number;
  name?: string;
  email?: string;
  age?: number;
}
*/

// 사용 예시
const user1: PartialUser = {}; // ✅ OK
const user2: PartialUser = { name: "홍길동" }; // ✅ OK
const user3: PartialUser = { id: 1, email: "hong@example.com" }; // ✅ OK
```

### 실전 활용 예제

**1. 사용자 정보 업데이트**
```typescript
interface User {
  id: number;
  name: string;
  email: string;
  age: number;
}

// 사용자 정보 업데이트 함수
function updateUser(id: number, updates: Partial<User>): User {
  // 기존 사용자 정보 (DB에서 가져왔다고 가정)
  const existingUser: User = {
    id: 1,
    name: "홍길동",
    email: "hong@example.com",
    age: 30
  };
  
  // 변경된 필드만 업데이트
  return {
    ...existingUser,
    ...updates
  };
}

// 사용
const updated1 = updateUser(1, { name: "김철수" }); // 이름만 변경
const updated2 = updateUser(1, { email: "kim@example.com", age: 31 }); // 이메일과 나이 변경
const updated3 = updateUser(1, {}); // 아무것도 변경 안 함
```

**2. 설정 객체**
```typescript
interface DatabaseConfig {
  host: string;
  port: number;
  username: string;
  password: string;
  database: string;
}

// 기본 설정
const defaultConfig: DatabaseConfig = {
  host: "localhost",
  port: 5432,
  username: "admin",
  password: "password",
  database: "mydb"
};

// 사용자 설정으로 덮어쓰기
function createConnection(userConfig: Partial<DatabaseConfig>) {
  const config = {
    ...defaultConfig,
    ...userConfig
  };
  
  console.log(`Connecting to ${config.host}:${config.port}...`);
  return config;
}

// 사용
createConnection({ host: "192.168.1.100" }); // host만 변경
createConnection({ port: 3306, database: "testdb" }); // port와 database 변경
createConnection({}); // 기본 설정 사용
```

**3. 폼 상태 관리**
```typescript
interface FormData {
  username: string;
  email: string;
  password: string;
  confirmPassword: string;
}

class FormState {
  private data: Partial<FormData> = {};
  
  // 필드 업데이트
  updateField(field: keyof FormData, value: string) {
    this.data[field] = value;
  }
  
  // 현재 상태 확인
  getState(): Partial<FormData> {
    return { ...this.data };
  }
  
  // 유효성 검사
  isValid(): boolean {
    // 모든 필드가 채워졌는지 확인
    return (
      this.data.username !== undefined &&
      this.data.email !== undefined &&
      this.data.password !== undefined &&
      this.data.confirmPassword !== undefined
    );
  }
}

// 사용
const form = new FormState();
form.updateField("username", "hong");
form.updateField("email", "hong@example.com");
console.log(form.getState()); // { username: "hong", email: "hong@example.com" }
console.log(form.isValid()); // false (password가 없음)
```

### 중첩 객체와 Partial
```typescript
interface Address {
  street: string;
  city: string;
  zipCode: string;
}

interface User {
  id: number;
  name: string;
  address: Address;
}

// ⚠️ 주의: Partial은 1단계만 선택적으로 만듭니다
type PartialUser = Partial<User>;

/*
type PartialUser = {
  id?: number;
  name?: string;
  address?: Address;  // ⚠️ Address 자체는 여전히 필수 속성들을 가짐
}
*/

const user: PartialUser = {
  address: {
    street: "서울시",
    // ❌ 에러! city와 zipCode는 필수
  }
};

// 해결 방법: 중첩된 객체도 Partial로 감싸기
type DeepPartialUser = {
  id?: number;
  name?: string;
  address?: Partial<Address>;
};

const user2: DeepPartialUser = {
  address: {
    street: "서울시"
    // ✅ OK! city와 zipCode 생략 가능
  }
};
```

### 💡 Partial 사용 팁
```typescript
// ✅ 좋은 사용 예
// 1. 업데이트 함수
function updateUser(id: number, updates: Partial<User>) { }

// 2. 선택적 설정
function configure(options: Partial<Config>) { }

// 3. 단계별 데이터 수집
const formData: Partial<FormData> = {};

// ❌ 피해야 할 사용
// 1. 모든 것을 Partial로 만들지 마세요
type BadUser = Partial<User>; // 모든 필드가 선택적 - 타입 안전성 저하

// 2. 필수 필드가 있는 경우
// id는 항상 필요한데 Partial로 만들면 안 됩니다
function getUser(id: number): Partial<User> {
  // id는 항상 있어야 하는데 반환 타입에서 선택적...
}
```

---

## Required<T>

### 개념

`Required<T>`는 타입 T의 **모든 선택적 속성을 필수**로 만듭니다. `Partial`의 정반대입니다.

**문법:**
```typescript
type Required<T> = {
  [P in keyof T]-?: T[P];
};
```

> 💡 **-?**: 물음표를 제거하여 필수 속성으로 만듭니다.

### 기본 사용법
```typescript
interface User {
  id: number;
  name?: string;
  email?: string;
  age?: number;
}

// 모든 속성이 필수가 됩니다
type RequiredUser = Required<User>;

/*
결과:
type RequiredUser = {
  id: number;
  name: string;      // ? 제거됨
  email: string;     // ? 제거됨
  age: number;       // ? 제거됨
}
*/

const user1: RequiredUser = {
  id: 1,
  name: "홍길동",
  email: "hong@example.com",
  age: 30
}; // ✅ OK

const user2: RequiredUser = {
  id: 1,
  name: "홍길동"
  // ❌ 에러! email과 age가 필수입니다
};
```

### 실전 활용 예제

**1. API 응답 검증**
```typescript
interface ApiResponse {
  status?: number;
  message?: string;
  data?: any;
}

// API 응답을 검증하는 함수
function validateResponse(response: ApiResponse): response is Required<ApiResponse> {
  return (
    response.status !== undefined &&
    response.message !== undefined &&
    response.data !== undefined
  );
}

// 사용
async function fetchData() {
  const response: ApiResponse = await fetch("/api/data").then(r => r.json());
  
  if (validateResponse(response)) {
    // 이제 response는 Required<ApiResponse> 타입
    console.log(response.status); // ✅ 안전하게 접근 가능
    console.log(response.message);
    console.log(response.data);
  } else {
    console.error("Invalid response");
  }
}
```

**2. 설정 완성도 체크**
```typescript
interface Config {
  host?: string;
  port?: number;
  username?: string;
  password?: string;
}

// 기본 설정 (일부만 있음)
const partialConfig: Config = {
  host: "localhost"
};

// 완전한 설정 만들기
function completeConfig(config: Config): Required<Config> {
  return {
    host: config.host ?? "localhost",
    port: config.port ?? 3000,
    username: config.username ?? "admin",
    password: config.password ?? "password"
  };
}

// 사용
const fullConfig = completeConfig(partialConfig);
console.log(fullConfig.port); // ✅ 반드시 존재함
```

**3. 폼 제출**
```typescript
interface FormData {
  username?: string;
  email?: string;
  password?: string;
}

class Form {
  private data: FormData = {};
  
  updateField(field: keyof FormData, value: string) {
    this.data[field] = value;
  }
  
  // 제출할 때는 모든 필드가 필수
  submit(): Required<FormData> | null {
    if (this.isComplete()) {
      return this.data as Required<FormData>;
    }
    return null;
  }
  
  private isComplete(): boolean {
    return (
      this.data.username !== undefined &&
      this.data.email !== undefined &&
      this.data.password !== undefined
    );
  }
}

// 사용
const form = new Form();
form.updateField("username", "hong");
form.updateField("email", "hong@example.com");

const result = form.submit();
if (result === null) {
  console.log("모든 필드를 입력해주세요");
} else {
  // result는 Required<FormData> 타입
  console.log(`제출: ${result.username}, ${result.email}, ${result.password}`);
}
```

### Partial과 Required 조합
```typescript
interface User {
  id: number;
  name: string;
  email?: string;
  phone?: string;
}

// 시나리오: 업데이트 시에는 일부만, 생성 시에는 전부
type UpdateUser = Partial<User>; // 모든 필드 선택적
type CreateUser = Required<User>; // 모든 필드 필수

function createUser(data: CreateUser) {
  // 모든 필드가 반드시 있어야 함
  console.log(data.email); // ✅ string (undefined 아님)
  console.log(data.phone); // ✅ string (undefined 아님)
}

function updateUser(id: number, data: UpdateUser) {
  // 일부 필드만 있어도 됨
  console.log(data.email); // string | undefined
}

// 사용
createUser({
  id: 1,
  name: "홍길동",
  email: "hong@example.com",
  phone: "010-1234-5678"
}); // ✅ 모든 필드 필수

updateUser(1, {
  name: "김철수"
}); // ✅ 일부만 업데이트
```

---

## 13.4 Readonly<T>

### 개념

`Readonly<T>`는 타입 T의 **모든 속성을 읽기 전용**으로 만듭니다.

**문법:**
```typescript
type Readonly<T> = {
  readonly [P in keyof T]: T[P];
};
```

### 기본 사용법
```typescript
interface User {
  id: number;
  name: string;
  email: string;
}

// 모든 속성이 읽기 전용이 됩니다
type ReadonlyUser = Readonly<User>;

/*
결과:
type ReadonlyUser = {
  readonly id: number;
  readonly name: string;
  readonly email: string;
}
*/

const user: ReadonlyUser = {
  id: 1,
  name: "홍길동",
  email: "hong@example.com"
};

console.log(user.name); // ✅ 읽기 OK

user.name = "김철수"; // ❌ 에러! 읽기 전용 속성입니다
```

### 실전 활용 예제

**1. 불변 상태 관리**
```typescript
interface State {
  count: number;
  message: string;
}

class Store {
  private state: State = {
    count: 0,
    message: "Hello"
  };
  
  // 상태를 읽기 전용으로 반환
  getState(): Readonly<State> {
    return this.state;
  }
  
  // 상태 업데이트는 메서드를 통해서만
  increment() {
    this.state = {
      ...this.state,
      count: this.state.count + 1
    };
  }
  
  setMessage(message: string) {
    this.state = {
      ...this.state,
      message
    };
  }
}

// 사용
const store = new Store();
const state = store.getState();

console.log(state.count); // ✅ 읽기 OK

state.count = 100; // ❌ 에러! 직접 수정 불가
store.increment(); // ✅ 메서드를 통해서만 변경
```

**2. 설정 보호**
```typescript
interface DatabaseConfig {
  host: string;
  port: number;
  username: string;
  password: string;
}

class Database {
  private config: Readonly<DatabaseConfig>;
  
  constructor(config: DatabaseConfig) {
    // 생성 시 한 번만 설정
    this.config = Object.freeze(config);
  }
  
  getConfig(): Readonly<DatabaseConfig> {
    return this.config;
  }
  
  connect() {
    console.log(`Connecting to ${this.config.host}:${this.config.port}`);
    // this.config.host = "hacked"; // ❌ 에러! 읽기 전용
  }
}

// 사용
const db = new Database({
  host: "localhost",
  port: 5432,
  username: "admin",
  password: "secret"
});

const config = db.getConfig();
console.log(config.host); // ✅ 읽기 OK
config.port = 3306; // ❌ 에러! 수정 불가
```

**3. 함수 반환 값 보호**
```typescript
interface Point {
  x: number;
  y: number;
}

class Shape {
  private center: Point = { x: 0, y: 0 };
  
  // 중심점을 읽기 전용으로 반환
  getCenter(): Readonly<Point> {
    return this.center;
  }
  
  // 중심점 이동은 메서드를 통해서만
  moveTo(x: number, y: number) {
    this.center = { x, y };
  }
}

// 사용
const shape = new Shape();
const center = shape.getCenter();

console.log(center.x, center.y); // ✅ 읽기 OK

center.x = 100; // ❌ 에러! 직접 수정 불가
shape.moveTo(100, 200); // ✅ 메서드를 통해 이동
```

### Readonly vs const
```typescript
// const: 변수 재할당 방지
const user1 = { name: "홍길동" };
user1 = { name: "김철수" }; // ❌ 에러! 재할당 불가
user1.name = "김철수"; // ✅ OK (속성은 변경 가능)

// Readonly: 속성 변경 방지
const user2: Readonly<{ name: string }> = { name: "홍길동" };
user2 = { name: "김철수" }; // ❌ 에러! const이므로 재할당 불가
user2.name = "김철수"; // ❌ 에러! Readonly이므로 속성 변경 불가

// 💡 함께 사용하면 완전한 불변성
const user3: Readonly<{ name: string }> = Object.freeze({ name: "홍길동" });
```

### 주의사항: 얕은(Shallow) 읽기 전용
```typescript
interface User {
  id: number;
  name: string;
  address: {
    city: string;
    street: string;
  };
}

const user: Readonly<User> = {
  id: 1,
  name: "홍길동",
  address: {
    city: "서울",
    street: "강남구"
  }
};

user.name = "김철수"; // ❌ 에러! 1단계 속성은 읽기 전용

user.address.city = "부산"; // ✅ OK??? (중첩 객체는 읽기 전용이 아님!)

// 해결: DeepReadonly 타입 필요 (13.12에서 다룹니다)
```

---

## 13.5 Pick<T, K>

### 개념

`Pick<T, K>`는 타입 T에서 **특정 속성 K만 선택**하여 새로운 타입을 만듭니다.

**문법:**
```typescript
type Pick<T, K extends keyof T> = {
  [P in K]: T[P];
};
```

> 💡 **읽는 방법**: "T에서 K에 해당하는 속성들만 선택"

### 기본 사용법
```typescript
interface User {
  id: number;
  name: string;
  email: string;
  age: number;
  address: string;
}

// id와 name만 선택
type UserPreview = Pick<User, 'id' | 'name'>;

/*
결과:
type UserPreview = {
  id: number;
  name: string;
}
*/

const preview: UserPreview = {
  id: 1,
  name: "홍길동"
}; // ✅ OK

const preview2: UserPreview = {
  id: 1,
  name: "홍길동",
  email: "hong@example.com" // ❌ 에러! email은 없는 속성
};
```

### 실전 활용 예제

**1. API 응답 타입 재사용**
```typescript
interface User {
  id: number;
  username: string;
  email: string;
  password: string;
  createdAt: Date;
  updatedAt: Date;
}

// 로그인 응답: 민감한 정보 제외
type LoginResponse = Pick<User, 'id' | 'username' | 'email'>;

// 사용자 목록: 요약 정보만
type UserListItem = Pick<User, 'id' | 'username' | 'createdAt'>;

function login(username: string, password: string): LoginResponse {
  // 로그인 로직...
  return {
    id: 1,
    username: "hong",
    email: "hong@example.com"
    // password는 반환하지 않음!
  };
}

function getUserList(): UserListItem[] {
  return [
    { id: 1, username: "hong", createdAt: new Date() },
    { id: 2, username: "kim", createdAt: new Date() }
  ];
}
```

**2. 폼 데이터**
```typescript
interface Product {
  id: number;
  name: string;
  description: string;
  price: number;
  stock: number;
  createdAt: Date;
  updatedAt: Date;
}

// 상품 생성 폼: id, createdAt, updatedAt 제외
type CreateProductForm = Pick<Product, 'name' | 'description' | 'price' | 'stock'>;

// 상품 수정 폼: id는 필요, 날짜는 불필요
type UpdateProductForm = Pick<Product, 'id' | 'name' | 'description' | 'price' | 'stock'>;

function createProduct(data: CreateProductForm) {
  const product: Product = {
    id: Date.now(), // 자동 생성
    ...data,
    createdAt: new Date(),
    updatedAt: new Date()
  };
  return product;
}

function updateProduct(data: UpdateProductForm) {
  // 기존 상품 정보 가져오기
  const existing = getProductById(data.id);
  
  return {
    ...existing,
    ...data,
    updatedAt: new Date() // 수정 시간만 변경
  };
}
```

**3. 컴포넌트 Props**
```typescript
interface User {
  id: number;
  name: string;
  email: string;
  avatar: string;
  bio: string;
  followers: number;
  following: number;
}

// 사용자 카드: 요약 정보만
type UserCardProps = Pick<User, 'name' | 'avatar'>;

// 사용자 프로필: 상세 정보
type UserProfileProps = Pick<User, 'name' | 'email' | 'bio' | 'followers' | 'following'>;

function UserCard(props: UserCardProps) {
  return `
    <div>
      <img src="${props.avatar}" />
      <h3>${props.name}</h3>
    </div>
  `;
}

function UserProfile(props: UserProfileProps) {
  return `
    <div>
      <h1>${props.name}</h1>
      <p>${props.email}</p>
      <p>${props.bio}</p>
      <p>팔로워: ${props.followers} | 팔로잉: ${props.following}</p>
    </div>
  `;
}
```

### Pick과 다른 유틸리티 타입 조합
```typescript
interface User {
  id: number;
  name: string;
  email: string;
  age: number;
}

// Pick + Partial: 특정 필드만 선택하고 선택적으로
type PartialUserProfile = Partial<Pick<User, 'name' | 'email'>>;

/*
결과:
type PartialUserProfile = {
  name?: string;
  email?: string;
}
*/

// Pick + Readonly: 특정 필드만 선택하고 읽기 전용으로
type ReadonlyUserProfile = Readonly<Pick<User, 'id' | 'name'>>;

/*
결과:
type ReadonlyUserProfile = {
  readonly id: number;
  readonly name: string;
}
*/
```

