---
title: "[JS기초] 배열과 객체 다루기 - 3"
date: 2025-10-12 20:00:00 +0900
categories: [웹 개발, 자바스크립트]
tags: [웹개발, 자바스크립트, 배열, 객체]
pin: true
math: true
mermaid: true
---

> **학습 목표**
> - 객체 메서드를 활용할 수 있다
> 

---

## 객체 메서드

객체를 다루는 데 유용한 메서드들을 알아봅시다. 이 메서드들은 **Object**에 내장된 메서드로 모든 객체에서 사용할 수 있습니다.

### Object.keys() - 모든 키 가져오기
- **기능:** 객체의 모든 키(속성 이름)를 **배열**로 반환합니다.  
- **원본 변경:** 원본 객체를 변경하지 않습니다.  
- **반환값:** 키들로 이루어진 배열 (문자열 배열)
 
```javascript
const person = {
  name: "홍길동",
  age: 25,
  city: "서울"
};

const keys = Object.keys(person);
console.log(keys);  // ["name", "age", "city"]
```

**💡 언제 사용할까?**

1) 객체가 어떤 속성을 가지고 있는지 확인할 때

```javascript
const user = {
  username: "hong123",
  email: "hong@example.com",
  age: 25
};

console.log("이 객체의 속성들:", Object.keys(user));
// ["username", "email", "age"]
```

2) 객체의 속성 개수를 세고 싶을 때

```javascript
const product = {
  name: "노트북",
  price: 1000000,
  brand: "삼성"
};

const propertyCount = Object.keys(product).length;
console.log(\`속성 개수: \${propertyCount}\`);  // 속성 개수: 3
```

3) 객체의 모든 속성을 순회할 때

```javascript
const scores = {
  korean: 85,
  math: 90,
  english: 88
};

Object.keys(scores).forEach(subject => {
  console.log(\`\${subject}: \${scores[subject]}점\`);
});
// korean: 85점
// math: 90점
// english: 88점
```

**주의사항:**
- 반환되는 배열의 순서는 대부분 속성이 추가된 순서이지만, 항상 보장되는 것은 아닙니다
- 객체에 속성이 없으면 빈 배열 `[]`을 반환합니다

---

### Object.values() - 모든 값 가져오기
- **기능:** 객체의 모든 값을 **배열**로 반환합니다.  
- **원본 변경:** 원본 객체를 변경하지 않습니다.  
- **반환값:** 값들로 이루어진 배열

```javascript
const person = {
  name: "홍길동",
  age: 25,
  city: "서울"
};

const values = Object.values(person);
console.log(values);  // ["홍길동", 25, "서울"]
```

**언제 사용할까?**

1) 모든 값을 배열로 다루고 싶을 때

```javascript
const scores = {
  korean: 85,
  math: 90,
  english: 88
};

const allScores = Object.values(scores);
console.log(allScores);  // [85, 90, 88]

// 평균 계산
const average = allScores.reduce((sum, score) => sum + score, 0) / allScores.length;
console.log(\`평균: \${average}\`);  // 평균: 87.66666...
```

2) 특정 값이 포함되어 있는지 확인할 때

```javascript
const user = {
  name: "홍길동",
  age: 25,
  city: "서울"
};

const hasSeoul = Object.values(user).includes("서울");
console.log(hasSeoul);  // true
```

3) 값들을 변환하거나 필터링할 때

```javascript
const prices = {
  apple: 1000,
  banana: 1500,
  orange: 2000
};

// 모든 가격을 10% 할인
const discountedPrices = Object.values(prices).map(price => price * 0.9);
console.log(discountedPrices);  // [900, 1350, 1800]

// 2000원 이상인 가격만
const expensivePrices = Object.values(prices).filter(price => price >= 2000);
console.log(expensivePrices);  // [2000]
```

**주의사항:**
키 정보는 포함하지 않고 값만 가져오기 때문에 어떤 값이 어떤 키에 해당하는지 알 수 없습니다. 키와 값을 함께 다루려면 `Object.entries()`를 사용하세요.

```javascript
const data = { a: 1, b: 2, c: 3 };
const values = Object.values(data);  // [1, 2, 3]
// 1이 'a'의 값인지 알 수 없음!
```

---

### Object.entries() - 키-값 쌍 가져오기
- **기능:** 객체의 각 속성을 `[키, 값]` 형태의 배열로 만들고, 이들을 모아 2차원 배열로 반환합니다.  
- **원본 변경:** 원본 객체를 변경하지 않습니다.  
- **반환값:** `[키, 값]` 쌍들의 배열

```javascript
const person = {
  name: "홍길동",
  age: 25,
  city: "서울"
};

const entries = Object.entries(person);
console.log(entries);
// [["name", "홍길동"], ["age", 25], ["city", "서울"]]
```

**구조 이해하기:**

```javascript
const data = { a: 1, b: 2 };
const entries = Object.entries(data);

console.log(entries);        // [["a", 1], ["b", 2]]
console.log(entries[0]);     // ["a", 1]
console.log(entries[0][0]);  // "a" (첫 번째 키)
console.log(entries[0][1]);  // 1 (첫 번째 값)
```

**언제 사용할까?**

1) 키와 값을 함께 순회할 때 (가장 많이 사용)

```javascript
const scores = {
  korean: 85,
  math: 90,
  english: 88
};

// for...of와 구조 분해 할당 사용
for (let [subject, score] of Object.entries(scores)) {
  console.log(\`\${subject}: \${score}점\`);
}
// korean: 85점
// math: 90점
// english: 88점
```

(참고) 구조 분해 할당 없이 키와 값을 함께 순회하려면

```javascript
for (let entry of Object.entries(scores)) {
  const subject = entry[0];  // 키
  const score = entry[1];    // 값
  console.log(\`\${subject}: \${score}점\`);
}
```

2) 객체를 배열 메서드로 처리하고 싶을 때

```javascript
const prices = {
  apple: 1000,
  banana: 1500,
  orange: 2000
};

// 가격이 1500원 이상인 과일만 찾기
const expensive = Object.entries(prices)
  .filter(([fruit, price]) => price >= 1500);

console.log(expensive);
// [["banana", 1500], ["orange", 2000]]

// 다시 객체로 만들기
const expensiveObj = Object.fromEntries(expensive);
console.log(expensiveObj);
// { banana: 1500, orange: 2000 }
```

3) forEach로 객체 순회하기

```javascript
const user = {
  name: "홍길동",
  age: 25,
  city: "서울"
};

Object.entries(user).forEach(([key, value]) => {
  console.log(\`\${key}: \${value}\`);
});
// name: 홍길동
// age: 25
// city: 서울
```

**주의사항:**
- 2차원 배열이므로 구조가 복잡해 보일 수 있습니다
- 구조 분해 할당을 사용하면 훨씬 읽기 쉽습니다

```javascript
// 읽기 어려움
Object.entries(obj).forEach(entry => {
  console.log(entry[0], entry[1]);
});

// 읽기 쉬움
Object.entries(obj).forEach(([key, value]) => {
  console.log(key, value);
});
```

---

### Object.assign() - 객체 복사/병합
- **기능:** 하나 이상의 원본 객체로부터 대상 객체로 속성을 복사합니다.  
- **원본 변경:** **첫 번째 인자(대상 객체)가 변경**됩니다.  
- **반환값:** 변경된 대상 객체

**기본형:** 

`Object.assign(target, ...sources)`
- target: 속성을 복사받을 대상 객체
- sources: 복사할 원본 객체들

```javascript
const target = { a: 1 };
const source = { b: 2, c: 3 };

Object.assign(target, source);

console.log(target);  // { a: 1, b: 2, c: 3 }
console.log(source);  // { b: 2, c: 3 } (원본은 그대로)
```

**언제 사용할까?**

1) 객체 복사하기 (얕은 복사)

```javascript
const original = {
  name: "홍길동",
  age: 25
};

// 빈 객체에 original 복사
const copy = Object.assign({}, original);

console.log(copy);  // { name: "홍길동", age: 25 }

// 복사본 수정해도 원본에 영향 없음
copy.age = 30;
console.log(original.age);  // 25 (그대로)
console.log(copy.age);      // 30 (변경됨)
```

2) 여러 객체 병합하기

```javascript
const defaults = {
  volume: 50,
  brightness: 80
};

const userSettings = {
  volume: 70
};

// defaults와 userSettings 병합
const finalSettings = Object.assign({}, defaults, userSettings);

console.log(finalSettings);
// { volume: 70, brightness: 80 }
// volume은 userSettings의 값으로 덮어씌워짐
```

3) 객체에 속성 추가하기

```javascript
const person = {
  name: "홍길동"
};

Object.assign(person, {
  age: 25,
  city: "서울"
});

console.log(person);
// { name: "홍길동", age: 25, city: "서울" }
```

**중요한 주의사항:**

1) assing() 메서드의 첫 번째 인자가 변경됩니다.

따라서 원본을 보존하려면 빈 객체를 첫 번째 인자로 사용해야 합니다.

```javascript
const obj1 = { a: 1 };
const obj2 = { b: 2 };

const result = Object.assign({}, obj1, obj2);

console.log(result);  // { a: 1, b: 2 }
console.log(obj1);    // { a: 1 } (그대로)
console.log(obj2);    // { b: 2 } (그대로)
```

2) 같은 키가 있으면 나중 값으로 덮어씌워집니다

```javascript
const obj1 = { name: "홍길동", age: 25 };
const obj2 = { age: 30, city: "서울" };

const result = Object.assign({}, obj1, obj2);

console.log(result);
// { name: "홍길동", age: 30, city: "서울" }
// age는 obj2의 값(30)으로 덮어씌워짐
```

3) 얕은 복사만 가능합니다. 따라서 중첩 객체는 참조만 복사됩니다. 중첩 객체일 경우 전개 연산자를 사용하는 것이 더 간단합니다. 

```javascript
const original = {
  name: "홍길동",
  address: {
    city: "서울"
  }
};

const copy = Object.assign({}, original);

// 중첩된 객체는 참조가 복사됨!
copy.address.city = "부산";

console.log(original.address.city);  // "부산" (원본도 변경됨!)
console.log(copy.address.city);      // "부산"
```


```javascript
// Object.assign 사용
const copy1 = Object.assign({}, original);

// 전개 연산자 사용 (권장)
const copy2 = { ...original };

// 객체 병합
const merged = { ...obj1, ...obj2 };
```

---

### Object.fromEntries() - 배열을 객체로 변환

- **기능:** `[키, 값]` 쌍의 배열을 객체로 변환합니다. `Object.entries()`의 반대입니다.  
- **원본 변경:** 원본 배열을 변경하지 않습니다.  
- **반환값:** 새로운 객체

```javascript
const entries = [
  ["name", "홍길동"],
  ["age", 25],
  ["city", "서울"]
];

const person = Object.fromEntries(entries);
console.log(person);
// { name: "홍길동", age: 25, city: "서울" }
```

**언제 사용할까?**

1) Object.entries()와 함께 사용 (객체 변환)

```javascript
const prices = {
  apple: 1000,
  banana: 1500,
  orange: 2000
};

// 모든 가격을 10% 할인
const discounted = Object.fromEntries(
  Object.entries(prices).map(([fruit, price]) => {
    return [fruit, price * 0.9];
  })
);

console.log(discounted);
// { apple: 900, banana: 1350, orange: 1800 }
```

2) 특정 속성만 필터링하기

```javascript
const user = {
  name: "홍길동",
  password: "1234",
  age: 25,
  city: "서울"
};

// password를 제외한 나머지만
const safeUser = Object.fromEntries(
  Object.entries(user).filter(([key]) => key !== "password")
);

console.log(safeUser);
// { name: "홍길동", age: 25, city: "서울" }
```

3) Map을 객체로 변환

이 때  배열의 각 요소는 반드시 `[키, 값]` 형태여야 합니다. 그리고 키는 문자열로 변환됩니다

```javascript
const map = new Map([
  ["name", "홍길동"],
  ["age", 25]
]);

const obj = Object.fromEntries(map);
console.log(obj);  // { name: "홍길동", age: 25 }
```

---

### Object.hasOwn() - 속성 존재 확인

- **기능:** 객체가 특정 속성을 가지고 있는지 확인합니다.  
- **원본 변경:** 원본 객체를 변경하지 않습니다.  
- **반환값:** true 또는 false

```javascript
const person = {
  name: "홍길동",
  age: 25
};

console.log(Object.hasOwn(person, "name"));    // true
console.log(Object.hasOwn(person, "city"));    // false
console.log(Object.hasOwn(person, "age"));     // true
```

**언제 사용할까?**

1) 속성이 있는지 확인 후 사용

```javascript
const config = {
  theme: "dark",
  fontSize: 14
};

if (Object.hasOwn(config, "theme")) {
  console.log(\`테마: \${config.theme}\`);
}

if (Object.hasOwn(config, "language")) {
  console.log(\`언어: \${config.language}\`);
} else {
  console.log("언어 설정이 없습니다");
}
```

2) 안전한 속성 접근

```javascript
function getProperty(obj, key) {
  if (Object.hasOwn(obj, key)) {
    return obj[key];
  } else {
    return "속성이 없습니다";
  }
}

const user = { name: "홍길동" };
console.log(getProperty(user, "name"));  // "홍길동"
console.log(getProperty(user, "age"));   // "속성이 없습니다"
```

**속성 존재를 확인하는 다른 방법들과 비교:**

```javascript
const person = { name: "홍길동", age: 25 };

// 방법 1: in 연산자
console.log("name" in person);  // true

// 방법 2: Object.hasOwn() (권장)
console.log(Object.hasOwn(person, "name"));  // true

// 방법 3: undefined 체크 (권장하지 않음)
console.log(person.name !== undefined);  // true
```

**Object.hasOwn()을 사용하는 이유:** `in` 연산자는 상속된 속성까지 확인하는 반면 `Object.hasOwn()`은 객체 자신의 속성만 확인합니다 (더 정확)

---

## 📊 객체 메서드 요약표

| 메서드 | 반환값 | 주요 용도 | 예시 |
|--------|--------|----------|------|
| **Object.keys()** | 키 배열 | 모든 속성 이름 가져오기 | `["name", "age"]` |
| **Object.values()** | 값 배열 | 모든 속성 값 가져오기 | `["홍길동", 25]` |
| **Object.entries()** | [키,값] 쌍 배열 | 키와 값 함께 순회 | `[["name","홍길동"],["age",25]]` |
| **Object.assign()** | 대상 객체 | 객체 복사/병합 | `{ ...obj1, ...obj2 }` |
| **Object.fromEntries()** | 객체 | 배열을 객체로 변환 | `{ name: "홍길동" }` |
| **Object.hasOwn()** | true/false | 속성 존재 확인 | `true` |

**💡 기억하기:**
- **keys**: 키만
- **values**: 값만
- **entries**: 키와 값 모두
- **assign**: 합치기
- **fromEntries**: 배열 → 객체
- **hasOwn**: 있는지 확인

---
