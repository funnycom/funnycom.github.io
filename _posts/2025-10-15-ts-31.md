---
title: "[Projects] 날씨 대시보드 앱 - 2"
date: 2025-10-15 16:50:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS]
pin: true
math: true
mermaid: true
published: false
---

> **프로젝트 목표**
> - 실시간 외부 API를 마스터한다
> - 데이터 캐싱 전략을 학습한다
> - 비동기 처리 패턴을 심화한다
> - 사용자 경험(UX)을 고려한 설계를 한다
> - 완전히 자율적으로 프로젝트를 진행한다

---

## Part 2: AI와 함께 구현하기 (2-3시간)

Part 1에서 완성한 설계를 바탕으로 실제 코드를 작성해봅시다.
이번에는 **가장 자율적**으로 진행하되, 필요할 때만 AI의 도움을 받습니다.

### 🚀 시작하기 전에

**[ ] 준비 사항 확인**
```
□ Part 1의 설계 문서를 옆에 두고 참고
□ API 키 준비 완료
□ 코드 에디터 실행
□ 프로젝트 폴더 생성 (weather-dashboard/)
□ TypeScript 환경 설정 완료
```

**프로젝트 초기 설정:**
```bash
mkdir weather-dashboard
cd weather-dashboard
npm init -y
npm install typescript @types/node --save-dev
npx tsc --init
```

---

## 📝 Step 1: 타입 정의

### 스스로 먼저 작성해보기

**[ ] 5분 동안 스스로 작성:**
```typescript
// src/types/weather.types.ts

// 1분 동안 생각해보고 작성
// 힌트: Part 1의 API 응답 구조 참고

export interface WeatherResponse {
  // API 원시 응답 구조
}

export interface WeatherInfo {
  // 날씨 기본 정보
}

export interface MainInfo {
  // 온도, 습도 등
}

export interface WindInfo {
  // 바람 정보
}
```

### AI에게 검증 요청

**프롬프트 (스스로 작성한 후):**
```
"OpenWeather API의 Current Weather Data 응답 구조를 
TypeScript 인터페이스로 정의해줘.

필요한 필드:
- weather: 날씨 상태 배열
- main: 온도, 습도 등
- wind: 풍속, 풍향
- name: 도시명
- dt: 타임스탬프

조건:
- 정확한 타입 지정
- JSDoc 주석
- 선택적 속성은 ?로 표시

내가 작성한 것과 비교할게."
```

**[ ] AI 응답 분석:**
```
받은 코드와 내 코드 비교:

같은 점:
- _______________________________________

다른 점:
- _______________________________________

더 나은 방법:
- _______________________________________

최종 선택:
- _______________________________________
```

### 캐시 타입 정의

**프롬프트:**
```
"제네릭 캐시를 위한 타입을 정의해줘.

interface CacheEntry<T> {
  data: T;
  timestamp: number;  // Date.now()
  expiresAt: number;  // timestamp + TTL
}

이게 맞는지 확인하고, 개선점이 있으면 알려줘."
```

---

## 📝 Step 2: CacheManager 구현

캐시는 다른 클래스에 의존하지 않으므로 먼저 구현합니다.

### 스스로 기본 구조 작성

**[ ] 10분 동안 스스로 작성:**
```typescript
// src/cache/CacheManager.ts

interface CacheEntry<T> {
  data: T;
  timestamp: number;
  expiresAt: number;
}

export class CacheManager<T> {
  private cache: Map<string, CacheEntry<T>>;
  private defaultTTL: number;

  constructor(defaultTTL: number = 300000) {
    // 5분 = 300000ms
    this.cache = new Map();
    this.defaultTTL = defaultTTL;
  }

  set(key: string, data: T, ttl?: number): void {
    // 여기를 구현해보세요
  }

  get(key: string): T | null {
    // 여기를 구현해보세요
  }

  has(key: string): boolean {
    // 여기를 구현해보세요
  }

  private isExpired(entry: CacheEntry<T>): boolean {
    // 여기를 구현해보세요
  }
}
```

### AI에게 부분적 도움 요청

**막히는 부분만 질문:**
```
"CacheManager의 set 메서드를 구현하고 있어.

요구사항:
1. key, data, 선택적 ttl 받기
2. 현재 시간 기록
3. 만료 시간 계산 (timestamp + ttl)
4. Map에 저장

내 구현:
[여기에 내가 작성한 코드]

이게 맞는지 확인하고, 개선점 알려줘.
전체 코드는 주지 말고 힌트만."
```

**프롬프트 (전체 검증):**
```
"CacheManager 클래스를 완성해줘.

이미 구현한 부분:
- 기본 구조
- constructor

구현 필요:
- set(key, data, ttl?): void
- get(key): T | null
  → 만료 확인 후 반환
  → 만료되었으면 삭제하고 null
- has(key): boolean
- isExpired(entry): boolean
- clear(): void
- size(): number

조건:
- 제네릭 사용
- 만료된 항목 자동 제거
- JSDoc 주석

간결하고 명확하게 작성해줘."
```

### 테스트 작성 (스스로)

**[ ] CacheManager 테스트:**
```typescript
// src/cache/CacheManager.test.ts
// 또는 index.ts에서 직접 테스트

import { CacheManager } from "./cache/CacheManager";

// 스스로 테스트 코드 작성
console.log("=== CacheManager 테스트 ===\n");

const cache = new CacheManager<string>(5000); // 5초 TTL

// 테스트 1: 저장 및 조회
cache.set("city1", "Seoul Weather Data");
console.log("저장 후 조회:", cache.get("city1")); // 데이터 있어야 함

// 테스트 2: 만료 확인
// 6초 대기 후 조회하면 null이어야 함
setTimeout(() => {
  console.log("만료 후 조회:", cache.get("city1")); // null이어야 함
}, 6000);

// 더 많은 테스트 직접 작성
```

---

## 📝 Step 3: WeatherApiService 구현

### API 호출 기본 구조

**프롬프트 (구체적으로):**
```
"WeatherApiService 클래스를 구현해줘.

구조:
class WeatherApiService {
  private baseUrl = "https://api.openweathermap.org/data/2.5";
  private apiKey: string;
  
  constructor(apiKey: string) { }
  
  async getCurrentWeather(city: string): Promise<WeatherResponse> { }
  
  private async fetchWeather(endpoint: string, params: object): Promise<any> {
    // fetch 래핑
    // 에러 처리
  }
}

요구사항:
1. fetch로 API 호출
2. URL 파라미터 생성 (city, appid, units=metric)
3. 에러 처리:
   - 네트워크 에러
   - HTTP 상태 코드 에러 (400, 401, 404, 500)
   - JSON 파싱 에러
4. 각 에러에 명확한 메시지

조건:
- async/await
- 타입 안전성
- JSDoc 주석
- 에러를 throw하지 말고 명확한 에러 메시지 로깅
"
```

### 에러 처리 강화

**프롬프트 (에러 처리 부분만):**
```
"WeatherApiService의 에러 처리를 개선해줘.

현재 구현:
[AI가 준 코드 또는 내 코드]

개선 사항:
1. 커스텀 에러 클래스
   - WeatherApiError extends Error
   - statusCode, city 속성 포함

2. 상태 코드별 처리
   - 401: "Invalid API key"
   - 404: "City not found: {city}"
   - 429: "API rate limit exceeded"
   - 500: "Weather service unavailable"

3. 재시도 로직 (선택적)
   - 최대 3회
   - 지수 백오프

재시도는 간단히 구현하고, 나중에 개선할 수 있게 해줘."
```

### API 테스트

**[ ] 실제 API로 테스트:**
```typescript
// 스스로 작성
import { WeatherApiService } from "./services/WeatherApiService";

async function testApi() {
  const apiKey = "YOUR_ACTUAL_API_KEY"; // 실제 키 사용
  const service = new WeatherApiService(apiKey);

  console.log("=== API 테스트 ===\n");

  // 테스트 1: 정상 케이스
  try {
    console.log("1. 서울 날씨 조회");
    const data = await service.getCurrentWeather("Seoul");
    console.log("성공:", data);
  } catch (error) {
    console.error("실패:", error);
  }

  // 테스트 2: 존재하지 않는 도시
  try {
    console.log("\n2. 잘못된 도시명");
    await service.getCurrentWeather("InvalidCityName123");
  } catch (error) {
    console.error("예상된 에러:", error);
  }

  // 테스트 3: 잘못된 API 키 (키를 임의로 바꿔서)
  // 더 많은 테스트 직접 작성
}

testApi();
```

---

## 📝 Step 4: WeatherData 클래스 구현

### 데이터 변환 클래스

**프롬프트:**
```
"WeatherData 클래스를 구현해줘.

역할: API 응답을 사용하기 쉬운 형태로 변환

class WeatherData {
  constructor(
    public city: string,
    public temperature: number,
    public feelsLike: number,
    public condition: string,      // "Clear", "Rain" 등
    public description: string,    // "clear sky", "light rain" 등
    public humidity: number,
    public windSpeed: number,
    public tempMin: number,
    public tempMax: number,
    public timestamp: Date
  ) {}
  
  // 정적 팩토리 메서드
  static fromApiResponse(response: WeatherResponse): WeatherData {
    // API 응답을 WeatherData로 변환
  }
  
  // 유틸리티 메서드들
  getTemperatureF(): number { }
  getWeatherEmoji(): string { }
  getFormattedInfo(): string { }
  isDataFresh(maxAgeMinutes: number): boolean { }
}

조건:
- fromApiResponse에서 필요한 데이터 추출
- 온도는 반올림 (Math.round)
- 날씨 이모지 매핑 (Clear→☀️, Rain→🌧️, Clouds→☁️ 등)
- 보기 좋은 포맷 (예: "서울 | ☀️ 맑음 | 5°C (체감: 2°C)")
- JSDoc 주석
"
```

### 날씨 이모지 매핑 (스스로 추가)

**[ ] 날씨 상태별 이모지:**
```typescript
// getWeatherEmoji() 구현
// 스스로 작성해보세요

private static weatherEmojis: Record<string, string> = {
  Clear: "☀️",
  Clouds: "☁️",
  Rain: "🌧️",
  // 더 추가...
};
```

---

## 📝 Step 5: WeatherDashboard 통합

이제 모든 것을 하나로 묶습니다.

### 기본 구조

**프롬프트 (간략히):**
```
"WeatherDashboard 클래스의 기본 구조만 만들어줘.

class WeatherDashboard {
  private apiService: WeatherApiService;
  private cache: CacheManager<WeatherData>;
  private favorites: Set<string>;
  
  constructor(apiKey: string, cacheTTL = 300000) {
    // 초기화
  }
}

의존성 주입으로 구현하고, 
메서드는 시그니처만 정의해줘. 구현은 내가 할게."
```

### getWeather 메서드 (스스로 먼저)

**[ ] 10분 동안 스스로 구현:**
```typescript
async getWeather(
  city: string, 
  useCache: boolean = true
): Promise<WeatherData | null> {
  // 로직 순서:
  // 1. useCache가 true면 캐시 확인
  // 2. 캐시에 있고 유효하면 반환
  // 3. 없으면 API 호출
  // 4. 성공하면 캐시에 저장
  // 5. 실패하면 null 반환
  
  // 여기를 구현해보세요
}
```

**프롬프트 (검증 요청):**
```
"내가 구현한 getWeather 메서드를 리뷰해줘.

[내 코드 붙여넣기]

확인할 점:
- 로직 순서가 맞는가?
- 에러 처리가 적절한가?
- 개선할 점은?

전체 코드를 다시 주지 말고, 피드백만 줘."
```

### 다중 도시 조회

**프롬프트:**
```
"getMultipleWeather 메서드를 구현해줘.

async getMultipleWeather(cities: string[]): Promise<WeatherData[]> {
  // Promise.all 또는 Promise.allSettled 사용
  // 일부 도시 실패해도 성공한 것은 반환
}

요구사항:
1. Promise.allSettled 사용 (일부 실패 허용)
2. 성공한 것만 필터링
3. 실패한 도시는 로그에 기록
4. 병렬 처리로 빠르게

간결하게 구현해줘."
```

### 즐겨찾기 기능 (스스로)

**[ ] 즐겨찾기 메서드 구현:**
```typescript
// 스스로 구현
addFavorite(city: string): void {
  // Set에 추가
}

removeFavorite(city: string): boolean {
  // Set에서 제거, 성공 여부 반환
}

getFavorites(): string[] {
  // Set을 배열로 변환
}

isFavorite(city: string): boolean {
  // 즐겨찾기 여부 확인
}

// localStorage에 저장 (선택)
private saveFavorites(): void { }
private loadFavorites(): void { }
```

### 대시보드 출력

**프롬프트:**
```
"printDashboard 메서드를 구현해줘.

출력 형식:
```
=== 날씨 대시보드 ===

📍 서울
   ☀️ 맑음 (Clear sky)
   온도: 5°C (체감: 2°C)
   습도: 45% | 풍속: 12 km/h
   최저/최고: 1°C / 8°C
   업데이트: 2분 전

📍 도쿄
   ...

⭐ 즐겨찾기: 서울, 도쿄
💾 캐시: 2개 도시
🔄 마지막 업데이트: 방금 전
```

조건:
- 현재 가진 모든 날씨 데이터 출력
- 보기 좋게 포맷
- 업데이트 시간 표시 (상대 시간: "2분 전")
"
```

### 통계 기능

**[ ] 통계 메서드 (스스로):**
```typescript
interface DashboardStats {
  totalCities: number;
  cachedCities: number;
  favoritesCount: number;
  lastUpdate: Date;
  cacheHitRate?: number;
}

getStatistics(): DashboardStats {
  // 통계 계산
}

printStatistics(): void {
  // 통계 출력
}
```

---

## 📝 Step 6: 통합 테스트

### 전체 시나리오 테스트

**[ ] 스스로 작성:**
```typescript
// src/index.ts

import { WeatherDashboard } from "./dashboard/WeatherDashboard";

async function main() {
  console.log("=== 날씨 대시보드 앱 ===\n");
  
  const apiKey = "YOUR_API_KEY";
  const dashboard = new WeatherDashboard(apiKey);
  
  // 시나리오 1: 단일 도시 조회
  console.log("1. 서울 날씨 조회");
  const seoul = await dashboard.getWeather("Seoul");
  if (seoul) {
    console.log(seoul.getFormattedInfo());
  }
  
  // 시나리오 2: 캐시 테스트
  console.log("\n2. 캐시 테스트 (같은 도시 다시 조회)");
  const seoulCached = await dashboard.getWeather("Seoul");
  // 빠르게 반환되어야 함
  
  // 시나리오 3: 여러 도시
  console.log("\n3. 여러 도시 동시 조회");
  const cities = ["Seoul", "Tokyo", "New York", "London"];
  const weatherData = await dashboard.getMultipleWeather(cities);
  console.log(`${weatherData.length}개 도시 조회 완료`);
  
  // 시나리오 4: 즐겨찾기
  console.log("\n4. 즐겨찾기 추가");
  dashboard.addFavorite("Seoul");
  dashboard.addFavorite("Tokyo");
  
  // 시나리오 5: 대시보드 출력
  console.log("\n5. 전체 대시보드");
  dashboard.printDashboard();
  
  // 시나리오 6: 통계
  console.log("\n6. 통계");
  dashboard.printStatistics();
  
  // 시나리오 7: 잘못된 도시
  console.log("\n7. 에러 처리 테스트");
  await dashboard.getWeather("InvalidCity123");
}

main().catch(console.error);
```

### 디버깅 체크리스트

**[ ] 발생 가능한 문제들:**
```
□ API 키 오류
  → 확인: API 키가 올바른가?
  
□ CORS 에러 (브라우저에서만)
  → Node.js 환경에서는 문제없음
  
□ 캐시가 작동하지 않음
  → console.log로 캐시 히트 확인
  
□ Promise 에러
  → async/await 제대로 사용했는가?
  
□ TypeScript 컴파일 에러
  → 타입이 정확한가?
  
□ 날씨 데이터 파싱 에러
  → API 응답 구조 변경되었나?
```

---

## 📝 Step 7: 고급 기능 추가

기본 기능이 완성되었다면 고급 기능을 추가해봅시다.

### 자동 새로고침

**[ ] 스스로 구현:**
```typescript
class WeatherDashboard {
  private refreshInterval?: NodeJS.Timeout;
  
  startAutoRefresh(intervalMs: number = 600000): void {
    // 10분마다 자동 새로고침
    this.refreshInterval = setInterval(async () => {
      console.log("\n🔄 자동 새로고침...");
      // 즐겨찾기 도시들 다시 조회
    }, intervalMs);
  }
  
  stopAutoRefresh(): void {
    if (this.refreshInterval) {
      clearInterval(this.refreshInterval);
    }
  }
}
```

### 5일 예보 (선택)

**프롬프트 (원하면):**
```
"WeatherApiService에 5일 예보 기능을 추가해줘.

async getForecast(city: string): Promise<ForecastResponse> {
  // /forecast 엔드포인트 사용
}

interface ForecastResponse {
  list: ForecastItem[];
  city: { name: string };
}

interface ForecastItem {
  dt: number;
  main: { temp, humidity };
  weather: [...];
  dt_txt: string;  // "2024-01-15 12:00:00"
}

간단히 구현하고, 일별로 그룹핑하는 메서드도 추가해줘."
```

### 온도 단위 변환

**[ ] 스스로 추가:**
```typescript
class WeatherDashboard {
  private temperatureUnit: "C" | "F" = "C";
  
  setTemperatureUnit(unit: "C" | "F"): void {
    this.temperatureUnit = unit;
  }
  
  // printDashboard에서 현재 단위에 맞게 표시
}
```

### localStorage 영속성

**[ ] 즐겨찾기 저장:**
```typescript
private saveFavorites(): void {
  // localStorage.setItem("favorites", JSON.stringify([...this.favorites]));
}

private loadFavorites(): void {
  // const saved = localStorage.getItem("favorites");
  // if (saved) this.favorites = new Set(JSON.parse(saved));
}

// constructor에서 loadFavorites() 호출
// addFavorite, removeFavorite에서 saveFavorites() 호출
```

---

## 🐛 Step 8: 디버깅 및 최적화

### 성능 측정

**[ ] 성능 체크:**
```typescript
class PerformanceMonitor {
  private static measurements: Map<string, number[]> = new Map();
  
  static start(label: string): number {
    return Date.now();
  }
  
  static end(label: string, startTime: number): void {
    const duration = Date.now() - startTime;
    
    if (!this.measurements.has(label)) {
      this.measurements.set(label, []);
    }
    this.measurements.get(label)!.push(duration);
    
    console.log(`⏱️ ${label}: ${duration}ms`);
  }
  
  static report(): void {
    console.log("\n📊 === 성능 리포트 ===");
    this.measurements.forEach((durations, label) => {
      const avg = durations.reduce((a, b) => a + b) / durations.length;
      console.log(`${label}: 평균 ${avg.toFixed(0)}ms (${durations.length}회)`);
    });
  }
}

// 사용
const start = PerformanceMonitor.start("API 호출");
await service.getCurrentWeather("Seoul");
PerformanceMonitor.end("API 호출", start);
```

### 캐시 효율 측정

**[ ] 캐시 히트율 계산:**
```typescript
class CacheManager<T> {
  private hits: number = 0;
  private misses: number = 0;
  
  get(key: string): T | null {
    const entry = this.cache.get(key);
    
    if (entry && !this.isExpired(entry)) {
      this.hits++;
      return entry.data;
    }
    
    this.misses++;
    return null;
  }
  
  getHitRate(): number {
    const total = this.hits + this.misses;
    return total === 0 ? 0 : (this.hits / total) * 100;
  }
  
  getStats() {
    return {
      hits: this.hits,
      misses: this.misses,
      hitRate: this.getHitRate().toFixed(1) + "%",
      size: this.cache.size
    };
  }
}
```

### 메모리 누수 방지

**[ ] 캐시 크기 제한:**
```typescript
class CacheManager<T> {
  private maxSize: number;
  
  constructor(defaultTTL: number, maxSize: number = 100) {
    this.maxSize = maxSize;
  }
  
  set(key: string, data: T, ttl?: number): void {
    // 크기 제한 체크
    if (this.cache.size >= this.maxSize) {
      // 가장 오래된 항목 삭제 (LRU)
      const oldestKey = this.cache.keys().next().value;
      this.cache.delete(oldestKey);
    }
    
    // ... 저장 로직
  }
}
```

---

## ✅ Part 2 완료 체크리스트

**[ ] 필수 구현:**
```
□ CacheManager 클래스 완성
□ WeatherApiService 완성
  □ getCurrentWeather 작동
  □ 에러 처리 작동
□ WeatherData 클래스 완성
  □ fromApiResponse 작동
  □ 포맷팅 메서드 작동
□ WeatherDashboard 완성
  □ getWeather 작동 (캐싱 포함)
  □ getMultipleWeather 작동
  □ 즐겨찾기 기능 작동
  □ printDashboard 작동
□ 통합 테스트 완료
  □ 실제 API로 테스트
  □ 캐시 동작 확인
  □ 에러 케이스 확인
```

**[ ] 추가 기능 (선택):**
```
□ 5일 예보
□ 자동 새로고침
□ 온도 단위 변환
□ localStorage 영속성
□ 성능 모니터링
□ 캐시 통계
```

**[ ] 코드 품질:**
```
□ 모든 클래스에 JSDoc 주석
□ 타입 안전성 확보
□ 에러 처리 완료
□ 테스트 코드 작성
□ 성능 최적화
```

---

## 💡 구현 중 자주 하는 실수

### 1. API 키 하드코딩
```typescript
// ❌ 나쁜 예
const apiKey = "abc123def456"; // 코드에 직접

// ✅ 좋은 예
const apiKey = process.env.WEATHER_API_KEY || "fallback-key";
// 또는 생성자로 받기
```

### 2. 에러를 무시
```typescript
// ❌ 나쁜 예
try {
  await fetchWeather();
} catch (error) {
  // 아무것도 안 함
}

// ✅ 좋은 예
try {
  await fetchWeather();
} catch (error) {
  console.error("날씨 조회 실패:", error);
  return null; // 또는 적절한 처리
}
```

### 3. 캐시 만료 확인 안 함
```typescript
// ❌ 나쁜 예
get(key: string): T | null {
  return this.cache.get(key)?.data || null;
}

// ✅ 좋은 예
get(key: string): T | null {
  const entry = this.cache.get(key);
  if (entry && !this.isExpired(entry)) {
    return entry.data;
  }
  return null;
}
```

### 4. Promise를 await 안 함
```typescript
// ❌ 나쁜 예
async function test() {
  const data = service.getCurrentWeather("Seoul"); // await 빠짐!
  console.log(data); // Promise 객체가 출력됨
}

// ✅ 좋은 예
async function test() {
  const data = await service.getCurrentWeather("Seoul");
  console.log(data); // 실제 데이터
}
```

### 5. 타입 단언 남용
```typescript
// ❌ 나쁜 예
const data = await response.json() as WeatherResponse;
// 응답이 정말 WeatherResponse인지 확인 안 함

// ✅ 좋은 예
const data = await response.json();
if (isWeatherResponse(data)) {
  // 타입 가드로 확인
  return data;
}
throw new Error("Invalid response format");
```

---



## 🎯 Part 2 마무리

**축하합니다! Part 2를 완료했습니다!**

### 구현 완료 확인

**[ ] 최종 체크:**
```
실행 테스트:
□ npm run dev 실행 성공
□ API 호출 성공
□ 캐시 동작 확인
□ 여러 도시 조회 성공
□ 에러 처리 확인
□ 대시보드 출력 정상

코드 품질:
□ TypeScript 컴파일 에러 없음
□ 모든 Promise에 await 사용
□ 타입 안전성 확보
□ 적절한 에러 처리
□ 코드 주석 작성
```

### 구현 시간 기록
```
Part 2 소요 시간:

Step 1 (타입 정의): _____분
Step 2 (CacheManager): _____분
Step 3 (WeatherApiService): _____분
Step 4 (WeatherData): _____분
Step 5 (WeatherDashboard): _____분
Step 6 (통합 테스트): _____분
Step 7 (고급 기능): _____분
Step 8 (디버깅): _____분

총 소요 시간: _____시간
```

### AI 활용 후기

**[ ] AI 협업 평가:**
```
AI에게 요청한 횟수: _____회

가장 도움된 요청:
"_______________________________________"

스스로 해결한 부분:
- _______________________________________
- _______________________________________

어려웠던 부분:
- _______________________________________
  → 해결 방법: _________________________

다음에 개선할 점:
- _______________________________________
- _______________________________________
```

### 코드 리뷰 (스스로)

**[ ] 자기 코드 리뷰:**
```
잘한 점:
1. _______________________________________
2. _______________________________________
3. _______________________________________

아쉬운 점:
1. _______________________________________
   → 개선 방법: _________________________
   
2. _______________________________________
   → 개선 방법: _________________________

리팩토링이 필요한 부분:
- _______________________________________
- _______________________________________
```

---

## 🚀 다음 단계 안내

**이제 Part 3로 진행합니다!**

### Part 3 미리보기

**Part 3: 스스로 개선하기**

완전 자율 도전 과제들:

**Level 1: 기본 개선**
- 여러 API 지원 (WeatherAPI, Tomorrow.io 등)
- 위치 기반 자동 조회
- 검색 히스토리
- 날씨 알림 시스템

**Level 2: 중급 도전**
- 비교 기능 (여러 도시 비교)
- 날씨 추천 (옷차림, 활동)
- 그래프/차트 (온도 변화)
- 데이터 내보내기 (CSV, JSON)

**Level 3: 고급 도전**
- 날씨 예측 분석
- 머신러닝 통합
- 웹 UI 구현 (HTML/CSS)
- 실시간 알림 서버

---

## 💾 코드 백업

**[ ] GitHub 저장:**
```bash
# Git 초기화
git init

# .gitignore 생성
echo "node_modules/" >> .gitignore
echo ".env" >> .gitignore
echo "dist/" >> .gitignore

# 커밋
git add .
git commit -m "feat: Weather Dashboard - Part 2 complete"

# GitHub에 푸시 (저장소 생성 후)
git remote add origin YOUR_REPO_URL
git push -u origin main
```

**[ ] README 작성:**
```markdown
# 날씨 대시보드

실시간 날씨 정보를 제공하는 TypeScript 애플리케이션

## 기능

- ✅ 실시간 날씨 조회
- ✅ 여러 도시 동시 조회
- ✅ 데이터 캐싱 (5분)
- ✅ 즐겨찾기 관리
- ✅ 에러 처리 및 재시도

## 기술 스택

- TypeScript
- OpenWeather API
- 캐싱 시스템
- 비동기 처리 (async/await)

## 설치 및 실행

\`\`\`bash
npm install
npm run dev
\`\`\`

## 사용 예시

\`\`\`typescript
const dashboard = new WeatherDashboard(API_KEY);
const weather = await dashboard.getWeather("Seoul");
dashboard.printDashboard();
\`\`\`

## 배운 점

- 실시간 API 연동
- 캐싱 전략 설계
- 비동기 에러 처리
- ...
```

---

## 📊 성과 측정

### 기술적 성장

**[ ] 프로젝트 3 후 자가 평가 (1-5점):**
```
프로젝트 시작 전 → 프로젝트 완료 후

API 통합: ___/5 → ___/5 (변화: +___)
캐싱 전략: ___/5 → ___/5 (변화: +___)
비동기 처리: ___/5 → ___/5 (변화: +___)
에러 처리: ___/5 → ___/5 (변화: +___)
자율성: ___/5 → ___/5 (변화: +___)
문제 해결: ___/5 → ___/5 (변화: +___)
```

### 프로젝트 비교
```
┌─────────────────────────────────────────────────┐
│         3개 프로젝트 완성도 비교                │
├──────────────┬─────────┬─────────┬─────────────┤
│              │ Todo 앱 │ 도서관  │  날씨 앱    │
├──────────────┼─────────┼─────────┼─────────────┤
│ 완성도       │  ___/10 │  ___/10 │   ___/10    │
│ 코드 품질    │  ___/10 │  ___/10 │   ___/10    │
│ 자율성       │  ___/10 │  ___/10 │   ___/10    │
│ 만족도       │  ___/10 │  ___/10 │   ___/10    │
└──────────────┴─────────┴─────────┴─────────────┘

전체적인 성장:
- 가장 성장한 부분: _______________________
- 가장 어려웠던 부분: _____________________
- 가장 뿌듯한 순간: _______________________
```

---

## 🎓 학습 정리

### Part 2에서 배운 핵심 개념

**1. API 통합 패턴**
```
✅ fetch 래핑
✅ 에러 처리 계층화
✅ 재시도 로직
✅ 타임아웃 처리
✅ 응답 타입 안전성
```

**2. 캐싱 전략**
```
✅ TTL (Time-To-Live)
✅ Cache-Aside 패턴
✅ 만료 확인
✅ 캐시 무효화
✅ 성능 측정
```

**3. 비동기 프로그래밍**
```
✅ async/await 패턴
✅ Promise.all vs Promise.allSettled
✅ 병렬 vs 순차 처리
✅ 에러 전파
✅ 타임아웃 구현
```

**4. 설계 패턴**
```
✅ 의존성 주입
✅ 팩토리 메서드
✅ 제네릭 활용
✅ 레이어드 아키텍처
✅ 에러 복구 전략
```

### 실전 팁 정리

**API 호출 시:**
```
1. 항상 에러 처리
2. 타임아웃 설정
3. 재시도 로직 고려
4. 응답 검증
5. 로깅 추가
```

**캐싱 시:**
```
1. 적절한 TTL 설정
2. 만료 확인 필수
3. 캐시 크기 제한
4. 히트율 측정
5. 무효화 전략
```

**비동기 코드:**
```
1. 모든 Promise에 await
2. try-catch 사용
3. 병렬 처리 활용 (Promise.all)
4. 일부 실패 허용 (Promise.allSettled)
5. 로딩 상태 관리
```

---

## 🎉 Part 2 완료!

**여러분은 이제:**

- ✅ 실시간 외부 API를 통합할 수 있습니다
- ✅ 효과적인 캐싱 시스템을 구현할 수 있습니다
- ✅ 복잡한 비동기 로직을 처리할 수 있습니다
- ✅ 프로덕션 수준의 에러 처리를 할 수 있습니다
- ✅ 90% 자율적으로 프로젝트를 진행할 수 있습니다

### Part 3 예고

**완전 자율 도전의 시간!**

Part 3에서는:
- AI의 도움 없이 스스로 기능 추가
- 창의적인 아이디어 구현
- 코드 최적화 및 리팩토링
- 포트폴리오 완성

**준비되셨나요?**

---

## 📝 Part 2 회고

**[ ] 5분 회고:**
```
1. 가장 재미있었던 부분:
   _________________________________________

2. 가장 어려웠던 부분:
   _________________________________________

3. 가장 많이 배운 개념:
   _________________________________________

4. AI 활용에서 개선된 점:
   _________________________________________

5. 다음 프로젝트에 적용하고 싶은 것:
   _________________________________________

6. 다른 사람에게 공유하고 싶은 팁:
   _________________________________________
```

---

## 🔗 유용한 리소스

### 학습 자료

**API 관련:**
```
- OpenWeather API 문서: https://openweathermap.org/api
- REST API 디자인 가이드
- HTTP 상태 코드 완벽 가이드
```

**캐싱:**
```
- 캐싱 전략 패턴
- Redis 캐싱 (고급)
- CDN 캐싱 개념
```

**비동기:**
```
- JavaScript Promises 완벽 가이드
- async/await 패턴
- 에러 처리 베스트 프랙티스
```

### 다음 단계 학습

**추천 주제:**
```
1. 웹 소켓 (실시간 통신)
2. GraphQL (REST 대안)
3. 인증/인가 (OAuth, JWT)
4. 데이터베이스 연동
5. 배포 (Docker, AWS)
```

---

## ⏭️ Part 3로 이동

**명령어:**
```
"Part 3 작성해줘"
```

**Part 3 내용:**
- Level 1, 2, 3 자율 도전 과제
- 포트폴리오 정리
- 최종 프로젝트 완성
- 성장 평가

**이제 Part 3로 넘어갑시다! 🚀**

---

**Part 2 완료 시간:** _____________

**자기 평가:**
```
구현 완성도: ___/10
코드 품질: ___/10
자율성: ___/10
만족도: ___/10

총평:
_________________________________________
_________________________________________
_________________________________________
```

**다음 목표:**
```
□ Part 3 도전 과제 선택
□ 고급 기능 구현
□ 코드 리팩토링
□ 포트폴리오 완성
□ 최종 회고 작성
```

---

**END OF PART 2**
