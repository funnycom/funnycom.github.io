---
title: "[TS] 데코레이터 - 1"
date: 2025-10-23 15:55:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS, 데코레이터, decorator]
pin: true
math: true
mermaid: true
---

## 🎯 학습 목표

* 데코레이터의 개념과 동작 원리를 이해한다
* TypeScript에서 데코레이터 기능을 활성화할 수 있다
* 클래스 데코레이터로 클래스를 수정하고 확장할 수 있다
* 메서드 데코레이터로 메서드 동작을 변경할 수 있다
* 프로퍼티 데코레이터로 속성에 메타데이터를 추가할 수 있다
* 매개변수 데코레이터로 파라미터 정보를 추출할 수 있다
* 데코레이터 팩토리로 설정 가능한 데코레이터를 만들 수 있다
* 실전에서 유효성 검사, 로깅, 의존성 주입을 구현할 수 있다

---

## 16.1 데코레이터란?

### 데코레이터의 개념

데코레이터는 **클래스, 메서드, 프로퍼티 등에 추가 기능을 붙이는** 특별한 선언입니다.

**데코레이터가 없다면?**
```typescript
// 로깅 기능을 수동으로 추가
class UserService {
  getUser(id: number) {
    console.log(`getUser called with id: ${id}`);  // 로깅 코드
    const startTime = Date.now();
    
    // 실제 로직
    const user = { id, name: "홍길동" };
    
    const endTime = Date.now();
    console.log(`getUser took ${endTime - startTime}ms`);  // 로깅 코드
    return user;
  }

  createUser(name: string) {
    console.log(`createUser called with name: ${name}`);  // 로깅 코드
    const startTime = Date.now();
    
    // 실제 로직
    const user = { id: Date.now(), name };
    
    const endTime = Date.now();
    console.log(`createUser took ${endTime - startTime}ms`);  // 로깅 코드
    return user;
  }
  
  // 모든 메서드에 같은 로깅 코드 반복... 😱
}
```

**데코레이터를 사용하면:**
```typescript
// 로깅 데코레이터 정의 (한 번만)
function Log(target: any, propertyKey: string, descriptor: PropertyDescriptor) {
  const originalMethod = descriptor.value;
  
  descriptor.value = function(...args: any[]) {
    console.log(`${propertyKey} called with:`, args);
    const startTime = Date.now();
    
    const result = originalMethod.apply(this, args);
    
    const endTime = Date.now();
    console.log(`${propertyKey} took ${endTime - startTime}ms`);
    return result;
  };
}

// 간단하게 적용! ✨
class UserService {
  @Log  // 데코레이터 적용
  getUser(id: number) {
    return { id, name: "홍길동" };
  }

  @Log  // 데코레이터 적용
  createUser(name: string) {
    return { id: Date.now(), name };
  }
  
  // 로깅 코드 없이도 자동으로 로깅됨!
}
```

**데코레이터의 장점:**
```
✅ 코드 재사용
   - 공통 기능을 한 번만 정의
   - 여러 곳에서 재사용

✅ 관심사 분리 (Separation of Concerns)
   - 핵심 로직과 부가 기능 분리
   - 코드 가독성 향상

✅ 선언적 프로그래밍
   - @Log, @Validate처럼 의도가 명확
   - 코드가 자기 설명적(self-documenting)

✅ 메타프로그래밍
   - 런타임에 동작 변경 가능
   - 리플렉션과 함께 강력한 기능
```

**데코레이터 종류:**
```typescript
// 1. 클래스 데코레이터
@Component
class MyClass {}

// 2. 메서드 데코레이터
class MyClass {
  @Log
  myMethod() {}
}

// 3. 프로퍼티 데코레이터
class MyClass {
  @Required
  name: string;
}

// 4. 접근자 데코레이터
class MyClass {
  @Configurable
  get value() {}
}

// 5. 매개변수 데코레이터
class MyClass {
  myMethod(@Inject('service') service: any) {}
}
```

### 실험적 기능 활성화

데코레이터는 아직 **실험적 기능**(Experimental)이므로 활성화가 필요합니다.

**tsconfig.json 설정:**
```json
{
  "compilerOptions": {
    "target": "ES5",                          // 또는 ES2015 이상
    "experimentalDecorators": true,           // 데코레이터 활성화 ✅
    "emitDecoratorMetadata": true            // 메타데이터 방출 (선택)
  }
}
```

**설정 설명:**
```json
{
  "experimentalDecorators": true,
  // 데코레이터 문법을 사용할 수 있게 함
  // 없으면 에러: "Experimental support for decorators is a feature..."
  
  "emitDecoratorMetadata": true
  // 타입 정보를 런타임에서 사용할 수 있게 메타데이터 생성
  // reflect-metadata 라이브러리와 함께 사용
  // 의존성 주입 등에 필요
}
```

**reflect-metadata 설치 (선택):**
```bash
# 메타데이터 리플렉션이 필요한 경우
npm install reflect-metadata

# 타입 정의도 포함되어 있음
```
```typescript
// 📁 app.ts 최상단에 import
import 'reflect-metadata';

// 이제 런타임에 타입 정보 사용 가능
```

**확인:**
```typescript
// 데코레이터 테스트
function Test(target: any) {
  console.log('Decorator working!');
}

@Test
class MyClass {}

// 컴파일 및 실행
// tsc app.ts
// node app.js
// 출력: Decorator working!
```

---

## 16.2 클래스 데코레이터

### 기본 문법

클래스 데코레이터는 **클래스 선언 직전에 적용**되며, 클래스 생성자를 수정하거나 확장할 수 있습니다.

**문법:**
```typescript
function ClassDecorator(constructor: Function) {
  // constructor: 데코레이팅되는 클래스의 생성자
}

@ClassDecorator
class MyClass {}
```

**기본 예제:**
```typescript
// 간단한 로깅 데코레이터
function LogClass(constructor: Function) {
  console.log(`Class created: ${constructor.name}`);
}

@LogClass
class User {
  constructor(public name: string) {}
}

// 출력: Class created: User

const user = new User("홍길동");
```

**클래스 수정:**
```typescript
// 클래스에 속성 추가
function AddTimestamp<T extends { new(...args: any[]): {} }>(constructor: T) {
  return class extends constructor {
    createdAt = new Date();
    
    constructor(...args: any[]) {
      super(...args);
      console.log(`Instance created at ${this.createdAt}`);
    }
  };
}

@AddTimestamp
class User {
  constructor(public name: string) {}
}

const user = new User("홍길동");
console.log((user as any).createdAt);  // Date 객체
```

**타입 안전한 클래스 데코레이터:**
```typescript
// 제네릭을 사용한 타입 안전 데코레이터
type Constructor<T = {}> = new (...args: any[]) => T;

function Serializable<T extends Constructor>(Base: T) {
  return class extends Base {
    serialize() {
      return JSON.stringify(this);
    }
    
    static deserialize(json: string) {
      return JSON.parse(json);
    }
  };
}

@Serializable
class User {
  constructor(
    public id: number,
    public name: string
  ) {}
}

const user = new User(1, "홍길동");
const json = user.serialize();  // ✅ serialize 메서드 사용 가능
console.log(json);  // {"id":1,"name":"홍길동"}

const restored = User.deserialize(json);  // ✅ static 메서드도 사용 가능
```

### 클래스 메타데이터 추가

**예제 1: 컴포넌트 등록**
```typescript
// 컴포넌트 레지스트리
const componentRegistry = new Map<string, any>();

function Component(name: string) {
  return function<T extends Constructor>(constructor: T) {
    // 컴포넌트 등록
    componentRegistry.set(name, constructor);
    
    return class extends constructor {
      componentName = name;
    };
  };
}

@Component('UserCard')
class UserCard {
  render() {
    return '<div>User Card</div>';
  }
}

@Component('ProductCard')
class ProductCard {
  render() {
    return '<div>Product Card</div>';
  }
}

// 컴포넌트 찾기
function getComponent(name: string) {
  return componentRegistry.get(name);
}

const UserCardClass = getComponent('UserCard');
const card = new UserCardClass!();
console.log(card.render());  // <div>User Card</div>
```

**예제 2: 싱글톤 패턴**
```typescript
function Singleton<T extends Constructor>(constructor: T) {
  let instance: T | null = null;
  
  return class extends constructor {
    constructor(...args: any[]) {
      if (instance) {
        return instance as any;
      }
      super(...args);
      instance = this as any;
    }
  };
}

@Singleton
class Database {
  constructor(public connectionString: string) {
    console.log('Database instance created');
  }
  
  connect() {
    console.log(`Connecting to ${this.connectionString}`);
  }
}

const db1 = new Database('localhost:5432');
// 출력: Database instance created

const db2 = new Database('localhost:3306');
// 아무것도 출력 안 됨 (같은 인스턴스 반환)

console.log(db1 === db2);  // true (싱글톤!)
```

**예제 3: 프리징(불변 객체)**
```typescript
function Frozen<T extends Constructor>(constructor: T) {
  return class extends constructor {
    constructor(...args: any[]) {
      super(...args);
      Object.freeze(this);  // 인스턴스를 불변으로 만듦
    }
  };
}

@Frozen
class Config {
  constructor(
    public apiUrl: string,
    public timeout: number
  ) {}
}

const config = new Config('https://api.example.com', 5000);
console.log(config.apiUrl);  // https://api.example.com

config.apiUrl = 'https://other.com';  // ❌ 런타임 에러! (strict mode)
console.log(config.apiUrl);  // 여전히 https://api.example.com
```

**예제 4: 메타데이터 저장**
```typescript
import 'reflect-metadata';

function Entity(tableName: string) {
  return function<T extends Constructor>(constructor: T) {
    // 메타데이터 저장
    Reflect.defineMetadata('tableName', tableName, constructor);
    
    return constructor;
  };
}

@Entity('users')
class User {
  constructor(
    public id: number,
    public name: string
  ) {}
}

// 메타데이터 읽기
const tableName = Reflect.getMetadata('tableName', User);
console.log(tableName);  // users

// ORM에서 활용
function getTableName(target: any): string {
  return Reflect.getMetadata('tableName', target) || 'default_table';
}

console.log(getTableName(User));  // users
```

---

## 16.3 메서드 데코레이터

### 메서드 수정 및 확장

메서드 데코레이터는 **메서드의 동작을 수정하거나 확장**할 수 있습니다.

**문법:**
```typescript
function MethodDecorator(
  target: any,                      // 클래스의 prototype (인스턴스 메서드) 또는 생성자 (정적 메서드)
  propertyKey: string | symbol,    // 메서드 이름
  descriptor: PropertyDescriptor    // 메서드의 속성 설명자
) {
  // descriptor.value: 원본 메서드
  // descriptor.writable: 쓰기 가능 여부
  // descriptor.enumerable: 열거 가능 여부
  // descriptor.configurable: 설정 가능 여부
}

class MyClass {
  @MethodDecorator
  myMethod() {}
}
```

**PropertyDescriptor:**
```typescript
interface PropertyDescriptor {
  value?: any;              // 메서드 함수 자체
  writable?: boolean;       // 덮어쓰기 가능 여부
  enumerable?: boolean;     // for...in 루프에 나타날지
  configurable?: boolean;   // 삭제나 수정 가능 여부
  get?(): any;             // getter
  set?(v: any): void;      // setter
}
```

**기본 예제:**
```typescript
function ReadOnly(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
) {
  descriptor.writable = false;  // 메서드를 읽기 전용으로
}

class MathUtils {
  @ReadOnly
  add(a: number, b: number): number {
    return a + b;
  }
}

const math = new MathUtils();
console.log(math.add(2, 3));  // 5

// 메서드 덮어쓰기 시도
math.add = function() { return 0; };  // ❌ 런타임 에러! (strict mode)
```

### 로깅 데코레이터

**예제 1: 기본 로깅**
```typescript
function Log(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
) {
  const originalMethod = descriptor.value;
  
  descriptor.value = function(...args: any[]) {
    console.log(`Calling ${propertyKey} with:`, args);
    const result = originalMethod.apply(this, args);
    console.log(`${propertyKey} returned:`, result);
    return result;
  };
  
  return descriptor;
}

class Calculator {
  @Log
  add(a: number, b: number): number {
    return a + b;
  }
  
  @Log
  multiply(a: number, b: number): number {
    return a * b;
  }
}

const calc = new Calculator();
calc.add(5, 3);
// 출력:
// Calling add with: [5, 3]
// add returned: 8

calc.multiply(4, 2);
// 출력:
// Calling multiply with: [4, 2]
// multiply returned: 8
```

**예제 2: 성능 측정**
```typescript
function Measure(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
) {
  const originalMethod = descriptor.value;
  
  descriptor.value = function(...args: any[]) {
    const start = performance.now();
    const result = originalMethod.apply(this, args);
    const end = performance.now();
    
    console.log(`${propertyKey} took ${(end - start).toFixed(2)}ms`);
    return result;
  };
  
  return descriptor;
}

class DataService {
  @Measure
  processLargeData(size: number): number[] {
    const data: number[] = [];
    for (let i = 0; i < size; i++) {
      data.push(Math.random());
    }
    return data;
  }
}

const service = new DataService();
service.processLargeData(1000000);
// 출력: processLargeData took 45.23ms
```

**예제 3: 에러 처리**
```typescript
function Catch(errorType: any) {
  return function(
    target: any,
    propertyKey: string,
    descriptor: PropertyDescriptor
  ) {
    const originalMethod = descriptor.value;
    
    descriptor.value = async function(...args: any[]) {
      try {
        return await originalMethod.apply(this, args);
      } catch (error) {
        if (error instanceof errorType) {
          console.error(`${propertyKey} caught error:`, error.message);
          return null;
        }
        throw error;
      }
    };
    
    return descriptor;
  };
}

class UserService {
  @Catch(Error)
  async getUser(id: number) {
    if (id < 0) {
      throw new Error('Invalid user ID');
    }
    return { id, name: 'User' };
  }
}

const userService = new UserService();

await userService.getUser(1);    // ✅ OK
await userService.getUser(-1);   // 에러 로그 출력, null 반환
```

**예제 4: 재시도 로직**
```typescript
function Retry(times: number) {
  return function(
    target: any,
    propertyKey: string,
    descriptor: PropertyDescriptor
  ) {
    const originalMethod = descriptor.value;
    
    descriptor.value = async function(...args: any[]) {
      let lastError: any;
      
      for (let i = 0; i < times; i++) {
        try {
          return await originalMethod.apply(this, args);
        } catch (error) {
          lastError = error;
          console.log(`${propertyKey} attempt ${i + 1} failed, retrying...`);
          await new Promise(resolve => setTimeout(resolve, 1000));
        }
      }
      
      throw lastError;
    };
    
    return descriptor;
  };
}

class ApiService {
  @Retry(3)
  async fetchData(url: string) {
    const response = await fetch(url);
    if (!response.ok) {
      throw new Error('Fetch failed');
    }
    return response.json();
  }
}

const api = new ApiService();
await api.fetchData('https://api.example.com/data');
// 실패 시 최대 3번 재시도
```

**예제 5: 캐싱**
```typescript
function Memoize(
  target: any,
  propertyKey: string,
  descriptor: PropertyDescriptor
) {
  const originalMethod = descriptor.value;
  const cache = new Map<string, any>();
  
  descriptor.value = function(...args: any[]) {
    const key = JSON.stringify(args);
    
    if (cache.has(key)) {
      console.log(`${propertyKey} cache hit for:`, args);
      return cache.get(key);
    }
    
    console.log(`${propertyKey} cache miss for:`, args);
    const result = originalMethod.apply(this, args);
    cache.set(key, result);
    return result;
  };
  
  return descriptor;
}

class FibonacciCalculator {
  @Memoize
  calculate(n: number): number {
    if (n <= 1) return n;
    return this.calculate(n - 1) + this.calculate(n - 2);
  }
}

const fib = new FibonacciCalculator();
console.log(fib.calculate(10));  // 첫 호출: 캐시 미스
console.log(fib.calculate(10));  // 두 번째: 캐시 히트
```

