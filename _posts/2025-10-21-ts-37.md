---
title: "[TS] 유틸리티 타입 - 4"
date: 2025-10-21 18:00:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS, 유틸리티타입]
pin: true
math: true
mermaid: true
published: false
---


> **학습목표**
> - 유틸리티의 필요성을 이해한다
> - 타입을 추출하고 변형할 수 있다.
> - 유니온 타입을 조작할 수 있다.
> - 함수 타입을 다룰 수 있다.
> - 고급 패턴을 적용할 수 있다.

## 13.12 실전 활용 패턴

이제 배운 유틸리티 타입들을 조합하여 실전에서 자주 사용되는 패턴들을 알아봅시다.

### 패턴 1: DeepPartial - 중첩 객체를 모두 선택적으로
```typescript
// 문제: Partial은 1단계만 선택적으로 만듭니다
interface User {
  id: number;
  name: string;
  address: {
    city: string;
    street: string;
    zipCode: string;
  };
  settings: {
    theme: 'light' | 'dark';
    notifications: {
      email: boolean;
      push: boolean;
    };
  };
}

// ❌ Partial은 address와 settings 내부는 여전히 필수
type PartialUser = Partial<User>;

const user1: PartialUser = {
  address: {
    city: "서울"
    // ❌ 에러! street와 zipCode가 필요함
  }
};

// ✅ 해결: DeepPartial 구현
type DeepPartial<T> = {
  [P in keyof T]?: T[P] extends object
    ? DeepPartial<T[P]>
    : T[P];
};

type DeepPartialUser = DeepPartial<User>;

const user2: DeepPartialUser = {
  address: {
    city: "서울"
    // ✅ OK! 모든 중첩 속성이 선택적
  },
  settings: {
    notifications: {
      email: true
      // push는 생략 가능
    }
  }
}; // ✅ OK

// 실전 사용: 설정 업데이트
function updateUserSettings(userId: number, updates: DeepPartial<User['settings']>) {
  // 중첩된 일부 설정만 업데이트 가능
  console.log(`Updating settings for user ${userId}:`, updates);
}

updateUserSettings(1, {
  theme: 'dark'
  // notifications는 생략
});

updateUserSettings(1, {
  notifications: {
    email: false
    // push는 생략
  }
});
```

### 패턴 2: DeepReadonly - 중첩 객체를 모두 읽기 전용으로
```typescript
// 문제: Readonly는 1단계만 읽기 전용으로 만듭니다
interface Config {
  api: {
    baseUrl: string;
    timeout: number;
    endpoints: {
      users: string;
      posts: string;
    };
  };
  features: {
    enableAuth: boolean;
    enableCache: boolean;
  };
}

// ❌ Readonly는 api와 features 내부는 변경 가능
const config1: Readonly<Config> = {
  api: {
    baseUrl: "https://api.example.com",
    timeout: 5000,
    endpoints: {
      users: "/users",
      posts: "/posts"
    }
  },
  features: {
    enableAuth: true,
    enableCache: true
  }
};

config1.api.timeout = 10000; // ✅ OK??? (중첩 객체는 변경 가능)

// ✅ 해결: DeepReadonly 구현
type DeepReadonly<T> = {
  readonly [P in keyof T]: T[P] extends object
    ? DeepReadonly<T[P]>
    : T[P];
};

const config2: DeepReadonly<Config> = {
  api: {
    baseUrl: "https://api.example.com",
    timeout: 5000,
    endpoints: {
      users: "/users",
      posts: "/posts"
    }
  },
  features: {
    enableAuth: true,
    enableCache: true
  }
};

config2.api.timeout = 10000; // ❌ 에러! 모든 중첩 속성이 읽기 전용
config2.api.endpoints.users = "/api/users"; // ❌ 에러!
```

### 패턴 3: RequiredKeys와 OptionalKeys - 필수/선택적 키 추출
```typescript
interface User {
  id: number;          // 필수
  name: string;        // 필수
  email?: string;      // 선택적
  phone?: string;      // 선택적
  age?: number;        // 선택적
}

// 필수 키만 추출
type RequiredKeys<T> = {
  [K in keyof T]-?: {} extends Pick<T, K> ? never : K;
}[keyof T];

type UserRequiredKeys = RequiredKeys<User>;
// 결과: 'id' | 'name'

// 선택적 키만 추출
type OptionalKeys<T> = {
  [K in keyof T]-?: {} extends Pick<T, K> ? K : never;
}[keyof T];

type UserOptionalKeys = OptionalKeys<User>;
// 결과: 'email' | 'phone' | 'age'

// 실전 사용: 필수 필드만 받는 함수
type RequiredFields<T> = Pick<T, RequiredKeys<T>>;
type OptionalFields<T> = Pick<T, OptionalKeys<T>>;

function createUser(required: RequiredFields<User>, optional?: OptionalFields<User>) {
  return {
    ...required,
    ...optional
  };
}

// 사용
const user = createUser(
  { id: 1, name: "홍길동" }, // 필수 필드
  { email: "hong@example.com" } // 선택적 필드
);
```

### 패턴 4: Mutable - Readonly 반대 (읽기 전용 제거)
```typescript
interface ReadonlyUser {
  readonly id: number;
  readonly name: string;
  readonly email: string;
}

// Readonly를 제거하는 유틸리티 타입
type Mutable<T> = {
  -readonly [P in keyof T]: T[P];
};

type MutableUser = Mutable<ReadonlyUser>;

/*
결과:
type MutableUser = {
  id: number;
  name: string;
  email: string;
}
*/

const user: MutableUser = {
  id: 1,
  name: "홍길동",
  email: "hong@example.com"
};

user.name = "김철수"; // ✅ OK (이제 변경 가능)

// 실전 사용: 불변 데이터를 변경 가능하게 복사
function cloneAsMutable<T>(obj: T): Mutable<T> {
  return { ...obj } as Mutable<T>;
}

const readonlyUser: ReadonlyUser = {
  id: 1,
  name: "홍길동",
  email: "hong@example.com"
};

const mutableCopy = cloneAsMutable(readonlyUser);
mutableCopy.name = "김철수"; // ✅ OK
```

### 패턴 5: PickByType - 특정 타입의 속성만 선택
```typescript
interface User {
  id: number;
  name: string;
  email: string;
  age: number;
  isActive: boolean;
  createdAt: Date;
}

// 특정 타입의 키만 추출
type PickByType<T, ValueType> = {
  [K in keyof T as T[K] extends ValueType ? K : never]: T[K];
};

// 숫자 타입 속성만 추출
type NumberFields = PickByType<User, number>;

/*
결과:
type NumberFields = {
  id: number;
  age: number;
}
*/

// 문자열 타입 속성만 추출
type StringFields = PickByType<User, string>;

/*
결과:
type StringFields = {
  name: string;
  email: string;
}
*/

// 실전 사용: 숫자 필드 통계
function getNumberFieldStats(user: User): Record<keyof PickByType<User, number>, number> {
  return {
    id: user.id,
    age: user.age
  };
}

const stats = getNumberFieldStats({
  id: 1,
  name: "홍길동",
  email: "hong@example.com",
  age: 30,
  isActive: true,
  createdAt: new Date()
});

console.log(stats); // { id: 1, age: 30 }
```

### 패턴 6: OmitByType - 특정 타입의 속성 제외
```typescript
// 특정 타입의 키를 제외
type OmitByType<T, ValueType> = {
  [K in keyof T as T[K] extends ValueType ? never : K]: T[K];
};

interface User {
  id: number;
  name: string;
  email: string;
  createdAt: Date;
  updatedAt: Date;
}

// Date 타입 제외
type UserWithoutDates = OmitByType<User, Date>;

/*
결과:
type UserWithoutDates = {
  id: number;
  name: string;
  email: string;
}
*/

// 실전 사용: 직렬화 (Date 제외)
function serializeUser(user: User): UserWithoutDates {
  const { createdAt, updatedAt, ...rest } = user;
  return rest;
}
```

### 패턴 7: PartialBy - 특정 키만 선택적으로
```typescript
// 특정 키만 선택적으로 만들기
type PartialBy<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;

interface User {
  id: number;
  name: string;
  email: string;
  age: number;
}

// email과 age만 선택적으로
type UserWithOptionalContact = PartialBy<User, 'email' | 'age'>;

/*
결과:
type UserWithOptionalContact = {
  id: number;
  name: string;
  email?: string;
  age?: number;
}
*/

const user1: UserWithOptionalContact = {
  id: 1,
  name: "홍길동"
  // email과 age는 생략 가능
}; // ✅ OK

const user2: UserWithOptionalContact = {
  id: 2,
  name: "김철수",
  email: "kim@example.com"
  // age만 생략
}; // ✅ OK

// 실전 사용: 업데이트 함수
function updateUser(
  id: number,
  updates: PartialBy<User, 'id'>
) {
  // id는 필수, 나머지는 선택적
  console.log(`Updating user ${id}:`, updates);
}

updateUser(1, {
  id: 1,
  name: "홍길동"
  // email과 age는 생략 가능
});
```

### 패턴 8: RequiredBy - 특정 키만 필수로
```typescript
// 특정 키만 필수로 만들기
type RequiredBy<T, K extends keyof T> = Omit<T, K> & Required<Pick<T, K>>;

interface User {
  id?: number;
  name?: string;
  email?: string;
  age?: number;
}

// id와 name만 필수로
type UserWithRequiredId = RequiredBy<User, 'id' | 'name'>;

/*
결과:
type UserWithRequiredId = {
  id: number;
  name: string;
  email?: string;
  age?: number;
}
*/

const user: UserWithRequiredId = {
  id: 1,
  name: "홍길동"
  // email과 age는 생략 가능
}; // ✅ OK
```

### 패턴 9: FunctionPropertyNames - 함수 속성 키 추출
```typescript
interface User {
  id: number;
  name: string;
  getName: () => string;
  getAge: () => number;
  setName: (name: string) => void;
}

// 함수 타입의 키만 추출
type FunctionPropertyNames<T> = {
  [K in keyof T]: T[K] extends Function ? K : never;
}[keyof T];

type UserMethods = FunctionPropertyNames<User>;
// 결과: 'getName' | 'getAge' | 'setName'

// 함수가 아닌 속성 키만 추출
type NonFunctionPropertyNames<T> = {
  [K in keyof T]: T[K] extends Function ? never : K;
}[keyof T];

type UserData = NonFunctionPropertyNames<User>;
// 결과: 'id' | 'name'

// 실전 사용: 메서드와 데이터 분리
type Methods<T> = Pick<T, FunctionPropertyNames<T>>;
type Data<T> = Pick<T, NonFunctionPropertyNames<T>>;

type UserMethods2 = Methods<User>;
/*
{
  getName: () => string;
  getAge: () => number;
  setName: (name: string) => void;
}
*/

type UserData2 = Data<User>;
/*
{
  id: number;
  name: string;
}
*/
```

### 패턴 10: Promisify - 함수를 Promise 반환으로
```typescript
// 함수의 반환 타입을 Promise로 감싸기
type Promisify<T extends (...args: any[]) => any> = (
  ...args: Parameters<T>
) => Promise<ReturnType<T>>;

// 동기 함수
function getUser(id: number): User {
  return {
    id,
    name: "홍길동",
    email: "hong@example.com",
    age: 30,
    isActive: true,
    createdAt: new Date()
  };
}

// 비동기 버전으로 변환
type AsyncGetUser = Promisify<typeof getUser>;
// (id: number) => Promise<User>

const getUserAsync: AsyncGetUser = async (id) => {
  // 비동기 작업...
  await new Promise(resolve => setTimeout(resolve, 1000));
  return getUser(id);
};

// 사용
getUserAsync(1).then(user => {
  console.log(user.name); // "홍길동"
});
```

### 패턴 11: Awaited - Promise 언래핑 (TypeScript 4.5+)
```typescript
// Promise의 결과 타입 추출
type Awaited<T> = T extends Promise<infer U> ? U : T;

async function fetchUser(): Promise<User> {
  const response = await fetch('/api/user');
  return response.json();
}

// Promise를 벗긴 타입
type UserFromPromise = Awaited<ReturnType<typeof fetchUser>>;
// 결과: User

// 중첩 Promise도 처리
type DoublePromise = Promise<Promise<User>>;
type UnwrappedUser = Awaited<DoublePromise>;
// 결과: User (완전히 언래핑됨)

// 실전 사용
async function processUsers() {
  const users: Awaited<ReturnType<typeof fetchUser>>[] = [];
  
  for (let i = 0; i < 3; i++) {
    const user = await fetchUser();
    users.push(user);
  }
  
  return users;
}
```

### 패턴 12: 유틸리티 타입 조합 실전 예제
```typescript
interface Article {
  id: number;
  title: string;
  content: string;
  author: {
    id: number;
    name: string;
    email: string;
  };
  tags: string[];
  published: boolean;
  createdAt: Date;
  updatedAt: Date;
}

// 1. 생성 시: id, 날짜 제외, author는 id만
type CreateArticleInput = Omit<Article, 'id' | 'createdAt' | 'updatedAt'> & {
  author: Pick<Article['author'], 'id'>;
};

/*
{
  title: string;
  content: string;
  author: { id: number };
  tags: string[];
  published: boolean;
}
*/

// 2. 수정 시: 일부 필드만 선택적으로
type UpdateArticleInput = {
  id: number;
} & Partial<Omit<CreateArticleInput, 'author'>>;

/*
{
  id: number;
  title?: string;
  content?: string;
  tags?: string[];
  published?: boolean;
}
*/

// 3. 목록 표시: 요약 정보만
type ArticleListItem = Pick<Article, 'id' | 'title' | 'author' | 'published' | 'createdAt'> & {
  author: Pick<Article['author'], 'name'>;
};

/*
{
  id: number;
  title: string;
  author: { name: string };
  published: boolean;
  createdAt: Date;
}
*/

// 4. 상세 표시: 민감한 정보 제외
type ArticleDetail = Omit<Article, 'author'> & {
  author: Omit<Article['author'], 'email'>;
};

/*
{
  id: number;
  title: string;
  content: string;
  author: { id: number; name: string };
  tags: string[];
  published: boolean;
  createdAt: Date;
  updatedAt: Date;
}
*/

// 사용 예시
function createArticle(input: CreateArticleInput): Article {
  return {
    id: Date.now(),
    ...input,
    author: {
      id: input.author.id,
      name: "작성자명", // DB에서 조회
      email: "author@example.com" // DB에서 조회
    },
    createdAt: new Date(),
    updatedAt: new Date()
  };
}

function updateArticle(input: UpdateArticleInput): Article {
  // 기존 게시글 가져오기
  const existing = getArticleById(input.id);
  
  return {
    ...existing,
    ...input,
    updatedAt: new Date()
  };
}

function getArticleList(): ArticleListItem[] {
  // 목록 조회
  return [];
}

function getArticleDetail(id: number): ArticleDetail {
  // 상세 조회
  const article = getArticleById(id);
  const { email, ...authorWithoutEmail } = article.author;
  
  return {
    ...article,
    author: authorWithoutEmail
  };
}
```

---

## 💡 유틸리티 타입 사용 팁

### 1. 적절한 유틸리티 타입 선택
```typescript
// ✅ 좋은 예
type UpdateUser = Partial<User>; // 모든 필드 선택적
type CreateUser = Omit<User, 'id'>; // id만 제외
type UserPreview = Pick<User, 'id' | 'name'>; // 2개만 선택

// ❌ 나쁜 예
type UpdateUser = Omit<User, never>; // Partial을 사용하세요
type CreateUser = Pick<User, 'name' | 'email' | 'age'>; // Omit이 더 간단
```

### 2. 타입 별칭으로 재사용
```typescript
// ✅ 좋은 예: 타입에 의미 있는 이름 부여
type UserId = User['id'];
type UserEmail = User['email'];
type CreateUserInput = Omit<User, 'id' | 'createdAt'>;

// ❌ 나쁜 예: 인라인으로 계속 사용
function updateUser(updates: Partial<Pick<User, 'name' | 'email'>>) {
  // 복잡하고 재사용 불가
}
```

### 3. 조합 순서에 주의
```typescript
// 순서가 중요합니다!
type A = Partial<Pick<User, 'name' | 'email'>>;
// 먼저 Pick으로 선택, 그 다음 Partial

type B = Pick<Partial<User>, 'name' | 'email'>;
// 먼저 Partial로 모두 선택적, 그 다음 Pick
// 결과는 같지만 A가 더 효율적
```

### 4. 과도한 중첩 피하기
```typescript
// ❌ 나쁜 예: 읽기 어려움
type ComplexType = Partial<Omit<Pick<Required<User>, 'name' | 'email' | 'age'>, 'age'>>;

// ✅ 좋은 예: 단계별로 분리
type RequiredUser = Required<User>;
type SelectedFields = Pick<RequiredUser, 'name' | 'email' | 'age'>;
type WithoutAge = Omit<SelectedFields, 'age'>;
type FinalType = Partial<WithoutAge>;

// 또는
type FinalType2 = Partial<Omit<Pick<Required<User>, 'name' | 'email' | 'age'>, 'age'>>;
// → 중간 타입 사용: Partial<{ name: string; email: string }>
```

---

## 📝 연습 문제

### 문제 1: 사용자 관리 시스템
```typescript
interface User {
  id: number;
  username: string;
  email: string;
  password: string;
  profile: {
    firstName: string;
    lastName: string;
    age: number;
    avatar?: string;
  };
  roles: ('user' | 'admin' | 'moderator')[];
  createdAt: Date;
  lastLoginAt?: Date;
}

// TODO: 다음 타입들을 유틸리티 타입으로 정의하세요

// 1. 회원가입 입력 (id, createdAt, lastLoginAt 제외)
type SignupInput = /* 여기에 작성 */;

// 2. 프로필 업데이트 (profile의 일부만 수정 가능)
type UpdateProfileInput = /* 여기에 작성 */;

// 3. 공개 프로필 (password, email 제외)
type PublicProfile = /* 여기에 작성 */;

// 4. 로그인 응답 (password 제외)
type LoginResponse = /* 여기에 작성 */;
```

### 문제 2: API 함수 타입
```typescript
// 다음 함수들의 타입을 ReturnType과 Parameters로 추출하세요

async function fetchUsers(page: number, limit: number) {
  return [
    { id: 1, name: "User 1" },
    { id: 2, name: "User 2" }
  ];
}

function createUser(name: string, email: string, age: number) {
  return {
    id: Date.now(),
    name,
    email,
    age,
    createdAt: new Date()
  };
}

// TODO: 타입 추출
type FetchUsersParams = /* 여기에 작성 */;
type FetchUsersReturn = /* 여기에 작성 */;
type CreateUserParams = /* 여기에 작성 */;
type CreateUserReturn = /* 여기에 작성 */;
```

### 문제 3: 커스텀 유틸리티 타입
```typescript
// TODO: 다음 유틸리티 타입들을 구현하세요

// 1. 모든 속성을 string으로 변환
type Stringify<T> = /* 여기에 작성 */;

interface User {
  id: number;
  name: string;
  age: number;
}

type StringUser = Stringify<User>;
// 결과: { id: string; name: string; age: string; }

// 2. 특정 타입의 속성만 선택적으로
type PartialByType<T, ValueType> = /* 여기에 작성 */;

type UserWithOptionalNumbers = PartialByType<User, number>;
// 결과: { id?: number; name: string; age?: number; }
```

---

## 🎯 Chapter 13 정리

### 핵심 유틸리티 타입
```
Partial<T>        → 모든 속성을 선택적으로
Required<T>       → 모든 속성을 필수로
Readonly<T>       → 모든 속성을 읽기 전용으로
Pick<T, K>        → 특정 속성만 선택
Omit<T, K>        → 특정 속성 제외
Record<K, T>      → 키-값 쌍의 타입
Exclude<T, U>     → 유니온에서 제외
Extract<T, U>     → 유니온에서 추출
NonNullable<T>    → null/undefined 제거
ReturnType<T>     → 함수 반환 타입
Parameters<T>     → 함수 매개변수 타입
```

### 언제 무엇을 사용할까?
```
업데이트 함수     → Partial
생성 함수         → Omit (자동 생성 필드 제외)
읽기 전용 데이터  → Readonly
API 응답         → Pick / Omit (민감한 정보 제외)
설정 객체         → Record
타입 필터링       → Exclude / Extract
함수 래핑         → Parameters / ReturnType
```

### 다음 Chapter 예고

**Chapter 14: 고급 타입 기법**

- 조건부 타입으로 동적 타입 생성
- 매핑 타입으로 타입 변환
- 템플릿 리터럴 타입
- 타입 가드 심화

유틸리티 타입을 마스터했다면, 이제 더 강력한 타입 시스템의 세계로!
