---
title: "[TS] 모듈 시스템 - 1"
date: 2025-10-23 14:55:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS, 모듈]
pin: true
math: true
mermaid: true
published: false
---

## 🎯 학습 목표

* 모듈의 개념과 필요성을 이해한다
* ES Modules의 import/export 문법을 사용할 수 있다
* default export와 named export의 차이를 안다
* CommonJS와 ES Modules의 차이를 이해한다
* 모듈 해석 전략(상대/절대 경로)을 이해한다
* 타입 선언 파일(.d.ts)을 작성하고 사용할 수 있다
* DefinitelyTyped를 활용하여 타입을 설치할 수 있다
* 네임스페이스와 모듈의 차이를 이해한다

---

## 15.1 모듈이란?

### 모듈의 필요성

모듈은 **코드를 파일 단위로 분리**하여 관리하는 방법입니다. 

**모듈이 없다면?**
```typescript
// 하나의 거대한 파일 (app.ts)
// 사용자 관련 코드
interface User {
  id: number;
  name: string;
  email: string;
}

function createUser(name: string, email: string): User {
  return {
    id: Date.now(),
    name,
    email
  };
}

function validateEmail(email: string): boolean {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
}

// 상품 관련 코드
interface Product {
  id: number;
  title: string;
  price: number;
}

function createProduct(title: string, price: number): Product {
  return {
    id: Date.now(),
    title,
    price
  };
}

// 주문 관련 코드
interface Order {
  id: number;
  userId: number;
  productId: number;
  quantity: number;
}

function createOrder(userId: number, productId: number, quantity: number): Order {
  return {
    id: Date.now(),
    userId,
    productId,
    quantity
  };
}

// ... 코드가 계속 늘어남 (수천 줄) 😱
```

**문제점:**
```
1. 유지보수 어려움
   - 한 파일에 모든 코드가 있어 찾기 힘듦
   - 수정 시 다른 코드에 영향 확인이 어려움

2. 이름 충돌
   - 전역 스코프에서 변수/함수 이름이 충돌할 수 있음
   - id라는 변수를 여러 곳에서 사용하면 덮어씀

3. 재사용 불가
   - 다른 프로젝트에서 일부 코드만 가져다 쓰기 어려움

4. 의존성 관리 어려움
   - 어떤 함수가 어떤 함수에 의존하는지 파악 불가

5. 팀 협업 어려움
   - 여러 사람이 동시에 같은 파일 수정 시 충돌
```

**모듈을 사용하면:**
```typescript
// user.ts
export interface User {
  id: number;
  name: string;
  email: string;
}

export function createUser(name: string, email: string): User {
  return { id: Date.now(), name, email };
}

export function validateEmail(email: string): boolean {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
}

// product.ts
export interface Product {
  id: number;
  title: string;
  price: number;
}

export function createProduct(title: string, price: number): Product {
  return { id: Date.now(), title, price };
}

// order.ts
import { User } from './user';
import { Product } from './product';

export interface Order {
  id: number;
  userId: number;
  productId: number;
  quantity: number;
}

export function createOrder(
  user: User,
  product: Product,
  quantity: number
): Order {
  return {
    id: Date.now(),
    userId: user.id,
    productId: product.id,
    quantity
  };
}

// app.ts
import { createUser } from './user';
import { createProduct } from './product';
import { createOrder } from './order';

const user = createUser("홍길동", "hong@example.com");
const product = createProduct("노트북", 1000000);
const order = createOrder(user, product, 1);
```

**장점:**
```
✅ 코드 분리와 구조화
   - 관련 코드끼리 묶어서 관리
   - 파일이 작고 명확해짐

✅ 캡슐화
   - export하지 않은 것은 외부에서 접근 불가
   - 내부 구현 숨기기

✅ 재사용성
   - 필요한 모듈만 가져다 쓰기
   - 다른 프로젝트에서도 사용 가능

✅ 의존성 명확화
   - import 문으로 의존 관계 파악
   - 순환 의존성 감지 가능

✅ 이름 충돌 방지
   - 각 모듈은 독립적인 스코프
   - 같은 이름 사용해도 충돌 없음
```

### JavaScript 모듈 역사

**1. 모듈이 없던 시절 (~ 2009년)**
```html
<!-- index.html -->
<script src="utils.js"></script>
<script src="user.js"></script>
<script src="product.js"></script>
<script src="app.js"></script>

<!-- 문제: 
  1. 로딩 순서가 중요함
  2. 전역 스코프 오염
  3. 의존성 관리 어려움
-->
```

**2. CommonJS (2009년, Node.js)**
```javascript
// user.js
module.exports = {
  createUser: function(name) {
    return { name: name };
  }
};

// app.js
const user = require('./user');
const newUser = user.createUser('홍길동');
```

- Node.js에서 채택
- 동기적 로딩 (서버 환경에 적합)
- 브라우저에서는 직접 사용 불가

**3. AMD (Asynchronous Module Definition, 2010년)**
```javascript
// user.js
define(['dependency'], function(dependency) {
  return {
    createUser: function(name) {
      return { name: name };
    }
  };
});

// app.js
require(['user'], function(user) {
  const newUser = user.createUser('홍길동');
});
```

- 브라우저 환경 지원
- 비동기 로딩
- RequireJS가 구현

**4. UMD (Universal Module Definition, 2011년)**
```javascript
(function (root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD
    define(['dependency'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS
    module.exports = factory(require('dependency'));
  } else {
    // 전역 변수
    root.MyModule = factory(root.Dependency);
  }
}(this, function (dependency) {
  return {
    createUser: function(name) {
      return { name: name };
    }
  };
}));
```

- CommonJS와 AMD 모두 지원
- 복잡하고 장황함

**5. ES Modules (2015년, ES6)**
```javascript
// user.js
export function createUser(name) {
  return { name };
}

// app.js
import { createUser } from './user.js';
const newUser = createUser('홍길동');
```

- JavaScript 표준 모듈 시스템
- 정적 분석 가능
- Tree shaking 지원
- 브라우저와 Node.js 모두 지원 (Node.js 12+)

**현재 (2024년):**
```
✅ ES Modules가 표준
   - TypeScript는 ES Modules 사용
   - 최신 브라우저 네이티브 지원
   - Node.js도 ES Modules 지원 (확장자 .mjs 또는 package.json의 "type": "module")

⚠️ CommonJS는 레거시
   - 기존 Node.js 프로젝트에서 여전히 많이 사용
   - 점차 ES Modules로 전환 중

💡 TypeScript 개발자는 ES Modules 사용!
```

---

## 15.2 ES Modules

### import와 export

**기본 문법:**
```typescript
// 📁 math.ts
// export: 내보내기
export function add(a: number, b: number): number {
  return a + b;
}

export function subtract(a: number, b: number): number {
  return a - b;
}

// 📁 app.ts
// import: 가져오기
import { add, subtract } from './math';

console.log(add(5, 3));       // 8
console.log(subtract(5, 3));  // 2
```

### default export

**개념:** 모듈에서 **하나의 주요 값**을 내보낼 때 사용합니다.

**문법:**
```typescript
// 📁 user.ts
// default export (파일당 1개만 가능)
export default interface User {
  id: number;
  name: string;
  email: string;
}

// 📁 app.ts
// default import (이름은 자유롭게 지정)
import User from './user';
import MyUser from './user';  // ✅ OK (이름 변경 가능)

const user: User = {
  id: 1,
  name: "홍길동",
  email: "hong@example.com"
};
```

**함수 default export:**
```typescript
// 📁 logger.ts
export default function log(message: string): void {
  console.log(`[LOG] ${message}`);
}

// 📁 app.ts
import log from './logger';
import myLog from './logger';  // ✅ 다른 이름으로 import 가능

log("Hello");
myLog("World");
```

**클래스 default export:**
```typescript
// 📁 database.ts
export default class Database {
  private connection: string;

  constructor(connectionString: string) {
    this.connection = connectionString;
  }

  connect(): void {
    console.log(`Connecting to ${this.connection}`);
  }
}

// 📁 app.ts
import Database from './database';
import DB from './database';  // ✅ 다른 이름 가능

const db = new Database("localhost:5432");
db.connect();
```

**익명 함수/클래스 default export:**
```typescript
// 📁 calculator.ts
export default class {
  add(a: number, b: number): number {
    return a + b;
  }
}

// 📁 greeter.ts
export default function(name: string): string {
  return `Hello, ${name}!`;
}

// 📁 app.ts
import Calculator from './calculator';
import greet from './greeter';

const calc = new Calculator();
console.log(calc.add(1, 2));  // 3
console.log(greet("홍길동"));   // Hello, 홍길동!
```

### named export

**개념:** 모듈에서 **여러 값**을 내보낼 때 사용합니다.

**기본 사용:**
```typescript
// 📁 utils.ts
// 선언과 동시에 export
export function formatDate(date: Date): string {
  return date.toISOString().split('T')[0];
}

export function formatTime(date: Date): string {
  return date.toTimeString().split(' ')[0];
}

export const APP_NAME = "My App";
export const VERSION = "1.0.0";

// 📁 app.ts
// 필요한 것만 import
import { formatDate, APP_NAME } from './utils';

console.log(formatDate(new Date()));
console.log(APP_NAME);
```

**선언 후 export:**
```typescript
// 📁 constants.ts
const API_URL = "https://api.example.com";
const TIMEOUT = 5000;
const MAX_RETRIES = 3;

// 나중에 한번에 export
export { API_URL, TIMEOUT, MAX_RETRIES };

// 📁 app.ts
import { API_URL, TIMEOUT } from './constants';
```

**이름 변경하여 export (as):**
```typescript
// 📁 math.ts
function addNumbers(a: number, b: number): number {
  return a + b;
}

function multiplyNumbers(a: number, b: number): number {
  return a * b;
}

// 다른 이름으로 export
export {
  addNumbers as add,
  multiplyNumbers as multiply
};

// 📁 app.ts
import { add, multiply } from './math';

console.log(add(2, 3));       // 5
console.log(multiply(2, 3));  // 6
```

**이름 변경하여 import (as):**
```typescript
// 📁 user.ts
export function validate(email: string): boolean {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
}

// 📁 product.ts
export function validate(price: number): boolean {
  return price > 0;
}

// 📁 app.ts
// 이름 충돌 방지
import { validate as validateEmail } from './user';
import { validate as validatePrice } from './product';

console.log(validateEmail("test@example.com"));  // true
console.log(validatePrice(100));                 // true
```

**전체 import (*):**
```typescript
// 📁 math.ts
export function add(a: number, b: number): number {
  return a + b;
}

export function subtract(a: number, b: number): number {
  return a - b;
}

export function multiply(a: number, b: number): number {
  return a * b;
}

// 📁 app.ts
// 모든 export를 하나의 객체로
import * as Math from './math';

console.log(Math.add(2, 3));       // 5
console.log(Math.subtract(5, 2));  // 3
console.log(Math.multiply(2, 3));  // 6
```

### default와 named export 혼합
```typescript
// 📁 user.ts
// default export
export default interface User {
  id: number;
  name: string;
  email: string;
}

// named exports
export function createUser(name: string, email: string): User {
  return {
    id: Date.now(),
    name,
    email
  };
}

export function validateUser(user: User): boolean {
  return user.name.length > 0 && user.email.includes('@');
}

export const USER_ROLE = {
  ADMIN: 'admin',
  USER: 'user'
} as const;

// 📁 app.ts
// 혼합 import
import User, { createUser, validateUser, USER_ROLE } from './user';

const user = createUser("홍길동", "hong@example.com");
console.log(validateUser(user));  // true
console.log(USER_ROLE.ADMIN);     // "admin"
```

### re-export

**개념:** 다른 모듈의 export를 **다시 export**하여 단일 진입점을 만듭니다.

**기본 re-export:**
```typescript
// 📁 models/user.ts
export interface User {
  id: number;
  name: string;
}

// 📁 models/product.ts
export interface Product {
  id: number;
  title: string;
  price: number;
}

// 📁 models/order.ts
export interface Order {
  id: number;
  userId: number;
  productId: number;
}

// 📁 models/index.ts
// 모든 모델을 한 곳에서 export
export { User } from './user';
export { Product } from './product';
export { Order } from './order';

// 📁 app.ts
// 여러 파일 대신 index에서 한번에 import
import { User, Product, Order } from './models';
// import { User } from './models/user';
// import { Product } from './models/product';
// import { Order } from './models/order';
// 위 3줄 대신 1줄로!
```

**전체 re-export:**
```typescript
// 📁 utils/string.ts
export function capitalize(str: string): string {
  return str.charAt(0).toUpperCase() + str.slice(1);
}

export function trim(str: string): string {
  return str.trim();
}

// 📁 utils/number.ts
export function round(num: number, decimals: number): number {
  return Math.round(num * Math.pow(10, decimals)) / Math.pow(10, decimals);
}

// 📁 utils/index.ts
// 모든 것을 re-export
export * from './string';
export * from './number';

// 📁 app.ts
import { capitalize, trim, round } from './utils';

console.log(capitalize("hello"));  // "Hello"
console.log(round(3.14159, 2));    // 3.14
```

**이름 변경하여 re-export:**
```typescript
// 📁 api/user-api.ts
export function get(id: number) {
  return { id, name: "User" };
}

export function create(data: any) {
  return { id: 1, ...data };
}

// 📁 api/product-api.ts
export function get(id: number) {
  return { id, title: "Product" };
}

export function create(data: any) {
  return { id: 1, ...data };
}

// 📁 api/index.ts
// 이름 충돌 방지를 위해 이름 변경
export {
  get as getUser,
  create as createUser
} from './user-api';

export {
  get as getProduct,
  create as createProduct
} from './product-api';

// 📁 app.ts
import { getUser, createUser, getProduct, createProduct } from './api';

const user = getUser(1);
const product = getProduct(1);
```

**default export re-export:**
```typescript
// 📁 components/Button.ts
export default class Button {
  render() {
    return '<button>Click</button>';
  }
}

// 📁 components/Input.ts
export default class Input {
  render() {
    return '<input />';
  }
}

// 📁 components/index.ts
// default를 named로 변환하여 re-export
export { default as Button } from './Button';
export { default as Input } from './Input';

// 📁 app.ts
import { Button, Input } from './components';

const button = new Button();
const input = new Input();
```

### 💡 export 스타일 가이드

**✅ 권장사항:**
```typescript
// 1. 선언과 동시에 export (명확함)
export interface User {
  id: number;
  name: string;
}

export function createUser(name: string): User {
  return { id: Date.now(), name };
}

// 2. 파일 하나에 여러 관련된 export
// 📁 user.ts
export interface User { /* ... */ }
export function createUser() { /* ... */ }
export function validateUser() { /* ... */ }

// 3. index.ts로 진입점 만들기
// 📁 models/index.ts
export * from './user';
export * from './product';
export * from './order';

// 4. named export 선호 (명시적)
import { User, createUser } from './user';  // ✅ 명확

// 5. 하나의 주요 기능만 있다면 default
// 📁 Database.ts
export default class Database { /* ... */ }
```

**❌ 피해야 할 것:**
```typescript
// 1. default와 named 혼합 (혼란스러움)
export default class User { }
export const USER_ROLE = { };
export function createUser() { }
// → 모두 named로 통일 권장

// 2. default export에 익명 사용 (디버깅 어려움)
export default function() { }  // ❌ 이름 없음
export default function greet() { }  // ✅ 이름 있음

// 3. 순환 의존성
// 📁 a.ts
import { b } from './b';
export const a = b + 1;

// 📁 b.ts
import { a } from './a';  // ❌ 순환 의존성!
export const b = a + 1;
```

---

계속해서 15.3 CommonJS를 작성할까요?
