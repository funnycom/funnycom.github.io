---
title: "[TS] 기본 타입 시스템 - 3"
date: 2025-10-13 17:30:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS]
pin: true
math: true
mermaid: true
---

> **학습 목표**
> - TypeScript의 기본 타입들을 이해하고 사용할 수 있다
> - 배열과 튜플의 차이를 안다
> - 객체 타입을 정의할 수 있다
> - any, unknown, void, never의 용도를 이해한다
> - 유니온 타입과 리터럴 타입을 활용할 수 있다

---

## ✏️ 직접 해보기

### 과제 1: 사용자 정보 타입 만들기

다양한 타입을 사용하여 사용자 정보를 관리하는 프로그램을 만들어보세요.

**요구사항:**
- 이름, 나이, 이메일, 전화번호(선택적) 포함
- 사용자 역할: "admin", "user", "guest" 중 하나
- 활성 상태: true 또는 false
- 가입일: Date 타입

```typescript
// 여기를 완성하세요
type UserRole = // ...
type User = {
  // ...
};

const user: User = {
  // ...
};
```

#### 정답 1: 사용자 정보 타입 만들기

```typescript
// 사용자 역할 (리터럴 타입)
type UserRole = "admin" | "user" | "guest";

// 사용자 타입
type User = {
  name: string;
  age: number;
  email: string;
  phone?: string;           // 선택적 속성
  role: UserRole;
  isActive: boolean;
  joinedAt: Date;
};

// 사용자 생성
const user1: User = {
  name: "홍길동",
  age: 25,
  email: "hong@example.com",
  phone: "010-1234-5678",
  role: "user",
  isActive: true,
  joinedAt: new Date("2024-01-15")
};

const user2: User = {
  name: "김철수",
  age: 30,
  email: "kim@example.com",
  // phone은 선택적이므로 생략 가능
  role: "admin",
  isActive: true,
  joinedAt: new Date("2023-06-10")
};

// 사용자 정보 출력 함수
function printUserInfo(user: User): void {
  console.log("=== 사용자 정보 ===");
  console.log(\`이름: \${user.name}\`);
  console.log(\`나이: \${user.age}세\`);
  console.log(\`이메일: \${user.email}\`);
  console.log(\`전화번호: \${user.phone || "없음"}\`);
  console.log(\`역할: \${user.role}\`);
  console.log(\`상태: \${user.isActive ? "활성" : "비활성"}\`);
  console.log(\`가입일: \${user.joinedAt.toLocaleDateString()}\`);
}

printUserInfo(user1);
console.log();
printUserInfo(user2);
```

---

### 과제 2: 성적표 시스템

유니온 타입과 리터럴 타입을 사용하여 성적표 시스템을 만들어보세요.

**요구사항:**
- 학점: "A", "B", "C", "D", "F" 중 하나
- 과목 정보: 과목명, 점수, 학점 포함
- 점수에 따라 학점을 계산하는 함수 작성
- 여러 과목의 평균 점수 계산

```typescript
// 여기를 완성하세요
type Grade = // ...

type Subject = {
  // ...
};

function calculateGrade(score: number): Grade {
  // 90점 이상: A
  // 80점 이상: B
  // 70점 이상: C
  // 60점 이상: D
  // 60점 미만: F
}

function calculateAverage(subjects: Subject[]): number {
  // ...
}
```

#### 정답 2: 성적표 시스템

```typescript
// 학점 타입 (리터럴 타입)
type Grade = "A" | "B" | "C" | "D" | "F";

// 과목 타입
type Subject = {
  name: string;
  score: number;
  grade: Grade;
};

// 점수로 학점 계산
function calculateGrade(score: number): Grade {
  if (score >= 90) return "A";
  if (score >= 80) return "B";
  if (score >= 70) return "C";
  if (score >= 60) return "D";
  return "F";
}

// 과목 생성 함수
function createSubject(name: string, score: number): Subject {
  return {
    name,
    score,
    grade: calculateGrade(score)
  };
}

// 평균 점수 계산
function calculateAverage(subjects: Subject[]): number {
  const total = subjects.reduce((sum, subject) => sum + subject.score, 0);
  return total / subjects.length;
}

// 학점별 개수 세기
function countGrades(subjects: Subject[]): Record<Grade, number> {
  const counts: Record<Grade, number> = {
    A: 0,
    B: 0,
    C: 0,
    D: 0,
    F: 0
  };
  
  subjects.forEach(subject => {
    counts[subject.grade]++;
  });
  
  return counts;
}

// 성적표 출력
function printReport(subjects: Subject[]): void {
  console.log("=== 성적표 ===");
  subjects.forEach(subject => {
    console.log(\`\${subject.name}: \${subject.score}점 (\${subject.grade}학점)\`);
  });
  
  const average = calculateAverage(subjects);
  const averageGrade = calculateGrade(average);
  
  console.log(\`\n평균 점수: \${average.toFixed(2)}점\`);
  console.log(\`평균 학점: \${averageGrade}\`);
  
  console.log("\n=== 학점 분포 ===");
  const gradeCounts = countGrades(subjects);
  for (const [grade, count] of Object.entries(gradeCounts)) {
    if (count > 0) {
      console.log(\`\${grade}학점: \${count}과목\`);
    }
  }
}

// 테스트
const subjects: Subject[] = [
  createSubject("국어", 85),
  createSubject("수학", 92),
  createSubject("영어", 78),
  createSubject("과학", 95),
  createSubject("사회", 88)
];

printReport(subjects);

// 출력 예시:
// === 성적표 ===
// 국어: 85점 (B학점)
// 수학: 92점 (A학점)
// 영어: 78점 (C학점)
// 과학: 95점 (A학점)
// 사회: 88점 (B학점)
//
// 평균 점수: 87.60점
// 평균 학점: B
//
// === 학점 분포 ===
// A학점: 2과목
// B학점: 2과목
// C학점: 1과목

// 추가 기능: 특정 학점 이상인 과목만 필터링
function filterByGrade(subjects: Subject[], minGrade: Grade): Subject[] {
  const gradeOrder: Grade[] = ["A", "B", "C", "D", "F"];
  const minIndex = gradeOrder.indexOf(minGrade);
  
  return subjects.filter(subject => {
    const subjectIndex = gradeOrder.indexOf(subject.grade);
    return subjectIndex <= minIndex;
  });
}

console.log("\n=== B학점 이상 과목 ===");
const goodGrades = filterByGrade(subjects, "B");
goodGrades.forEach(subject => {
  console.log(\`\${subject.name}: \${subject.grade}학점\`);
});
```

**설명:**
- `Grade` 타입: 리터럴 타입으로 5가지 학점만 허용
- `calculateGrade`: 점수에 따라 학점을 반환
- `calculateAverage`: reduce를 사용하여 평균 계산
- `Record<Grade, number>`: 각 학점별 개수를 저장하는 객체 타입
- 유니온 타입의 안정성 덕분에 잘못된 학점을 사용할 수 없음

---

### 과제 3: 도형 넓이 계산기

교차 타입과 판별 유니온을 사용하여 도형 넓이 계산기를 만들어보세요.

**요구사항:**
- Circle, Square 타입 정의 (kind 속성으로 구분)
- 각 도형은 색상(color) 속성도 가짐
- 넓이 계산 함수 작성

```typescript
// 여기를 완성하세요
```

#### 정답 3: 도형 넓이 계산기

```typescript
// 공통 속성
type ColoredShape = {
  color: string;
};

// 원
type Circle = ColoredShape & {
  kind: "circle";
  radius: number;
};

// 정사각형
type Square = ColoredShape & {
  kind: "square";
  side: number;
};

// 직사각형
type Rectangle = ColoredShape & {
  kind: "rectangle";
  width: number;
  height: number;
};

// 모든 도형
type Shape = Circle | Square | Rectangle;

// 넓이 계산 함수
function calculateArea(shape: Shape): number {
  switch (shape.kind) {
    case "circle":
      return Math.PI * shape.radius ** 2;
    case "square":
      return shape.side ** 2;
    case "rectangle":
      return shape.width * shape.height;
  }
}

// 도형 정보 출력 함수
function printShapeInfo(shape: Shape): void {
  const area = calculateArea(shape);
  console.log(\`도형: \${shape.kind}\`);
  console.log(\`색상: \${shape.color}\`);
  console.log(\`넓이: \${area.toFixed(2)}\n\`);
}

// 테스트
const circle: Circle = {
  kind: "circle",
  color: "빨강",
  radius: 5
};

const square: Square = {
  kind: "square",
  color: "파랑",
  side: 10
};

const rectangle: Rectangle = {
  kind: "rectangle",
  color: "초록",
  width: 8,
  height: 12
};

console.log("=== 도형 넓이 계산 ===");
printShapeInfo(circle);
printShapeInfo(square);
printShapeInfo(rectangle);

// 모든 도형을 배열로 관리
const shapes: Shape[] = [circle, square, rectangle];

const totalArea = shapes.reduce((sum, shape) => sum + calculateArea(shape), 0);
console.log(\`전체 도형의 총 넓이: \${totalArea.toFixed(2)}\`);
```

---

## ✅ 체크포인트

이번 챕터를 마치기 전에 다음 항목을 확인해보세요:

- [ ] 원시 타입(string, number, boolean 등)을 이해한다
- [ ] 배열 타입을 선언할 수 있다
- [ ] 튜플과 배열의 차이를 안다
- [ ] 객체 타입을 정의할 수 있다
- [ ] 선택적 속성과 읽기 전용 속성을 사용할 수 있다
- [ ] any와 unknown의 차이를 이해한다
- [ ] void, never, undefined의 차이를 안다
- [ ] 리터럴 타입을 활용할 수 있다
- [ ] 유니온 타입과 교차 타입을 사용할 수 있다
- [ ] 타입 가드를 이용하여 타입을 좁힐 수 있다

---

### 자가 진단 퀴즈

**1. 다음 중 올바른 배열 타입 선언은?**

```typescript
A. const numbers: number = [1, 2, 3];
B. const numbers: number[] = [1, 2, 3];
C. const numbers: array = [1, 2, 3];
D. const numbers: [number] = [1, 2, 3];
```

**정답: B**

- A: ❌ number는 배열 타입이 아님
- B: ✅ number[] 또는 Array<number>로 선언
- C: ❌ array는 유효한 타입이 아님
- D: ❌ [number]는 튜플 타입 (요소가 1개만 있는 배열)

---

**2. 다음 중 튜플에 대한 설명으로 올바른 것은?**

A. 튜플은 길이가 가변적이다  
B. 튜플의 모든 요소는 같은 타입이어야 한다  
C. 튜플은 각 위치마다 다른 타입을 가질 수 있다  
D. 튜플은 객체의 다른 이름이다

**정답: C**

튜플은 고정된 길이와 각 위치마다 정해진 타입을 가지는 배열입니다.

```typescript
const person: [string, number] = ["홍길동", 25];
```

---

**3. any와 unknown의 차이점은?**

A. any는 타입 체크를 하지 않고, unknown은 타입 체크를 한다  
B. 둘은 완전히 같다  
C. unknown이 더 위험하다  
D. any는 최신 버전에서만 사용 가능하다

**정답: A**

`any`는 타입 체크를 완전히 끄지만, `unknown`은 사용하기 전에 타입을 확인해야 합니다. `unknown`이 더 안전한 타입입니다.

---

**4. 다음 코드의 출력 결과는?**

```typescript
type Status = "pending" | "success" | "error";
let status: Status = "pending";
status = "loading";
```

A. "loading"  
B. "pending"  
C. 컴파일 에러  
D. undefined

**정답: C (컴파일 에러)**

`Status` 타입은 "pending", "success", "error" 중 하나만 허용하므로, "loading"을 할당하려고 하면 컴파일 에러가 발생합니다.

---

**5. 유니온 타입 A | B는 무엇을 의미하는가?**

A. A와 B 둘 다 만족해야 함  
B. A 또는 B 중 하나를 만족하면 됨  
C. A만 만족하면 됨  
D. 타입 에러

**정답: B**

유니온 타입(`|`)은 "또는(OR)"의 의미로, 둘 중 하나의 타입이면 됩니다.

교차 타입(`&`)은 "그리고(AND)"의 의미로, 모든 타입을 만족해야 합니다.

---

## 💡 학습 팁

### 타입 시스템 마스터하기

**1. 작은 것부터 시작하기**

복잡한 타입을 만들기 전에 간단한 타입부터 연습하세요.

```typescript
// 1단계: 기본 타입
const name: string = "홍길동";

// 2단계: 배열
const names: string[] = ["홍길동", "김철수"];

// 3단계: 객체
const user: { name: string; age: number } = {
  name: "홍길동",
  age: 25
};

// 4단계: 유니온 타입
const id: string | number = "user-123";

// 5단계: 복잡한 조합
type User = {
  id: string | number;
  name: string;
  email?: string;
};
```

**2. 타입 에러 메시지 읽는 연습**

TypeScript의 에러 메시지는 매우 친절합니다. 꼼꼼히 읽으면 문제를 쉽게 해결할 수 있습니다.

```typescript
const user: { name: string; age: number } = {
  name: "홍길동"
  // Error: Property 'age' is missing in type '{ name: string; }' 
  //        but required in type '{ name: string; age: number; }'.
};
// → age 속성이 없다는 것을 명확히 알려줌
```

**3. 타입 좁히기 패턴 익히기**

유니온 타입을 안전하게 사용하는 패턴을 익히세요.

```typescript
function process(value: string | number) {
  // typeof 가드
  if (typeof value === "string") {
    console.log(value.toUpperCase());
  }
  
  // in 가드
  if ("length" in value) {
    console.log(value.length);
  }
}
```

**4. 실전 예제로 연습하기**

책에서 배운 타입을 자신의 프로젝트에 적용해보세요. 예를 들어:
- Todo 앱의 할 일 타입 정의
- 쇼핑몰의 상품 타입 정의
- API 응답 타입 정의

  ---

### AI 활용 팁

**효과적인 질문 방법:**

```
1. "튜플과 배열의 차이를 예제와 함께 설명해줘"
   → 개념 비교 요청

2. "다음 코드의 타입 에러를 고쳐줘: [코드]"
   → 구체적인 코드와 함께 질문

3. "API 응답 타입을 유니온 타입으로 만드는 방법을 알려줘"
   → 실전 활용법 질문

4. "readonly와 const의 차이가 뭐야?"
   → 헷갈리는 개념 질문
```

---

## 🎯 다음 챕터 미리보기

Chapter 8에서는 **함수 타입**을 배웁니다!
- 함수 매개변수와 반환 타입
- 선택적 매개변수와 기본 매개변수
- 함수 오버로딩
- 콜백 함수와 고차 함수

**미리 준비하기:**
- Chapter 7의 모든 과제를 완성하세요
- 유니온 타입과 타입 가드를 복습하세요
- Part 0의 함수 개념을 복습하세요

---

TypeScript의 기본 타입 시스템을 모두 배웠습니다! 이제 여러분은:
- ✅ 모든 기본 타입을 사용할 수 있다
- ✅ 배열과 튜플을 구분할 수 있다
- ✅ 안전한 타입을 선택할 수 있다 (unknown > any)
- ✅ 유니온과 교차 타입을 활용할 수 있다

다음 챕터에서는 함수에 타입을 적용하는 방법을 배워봅시다! 💪

</details>

---
