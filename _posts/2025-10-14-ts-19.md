---
title: "[TS] HTTP와 API 다루기 - 4"
date: 2025-10-14 23:30:00 +0900
categories: [웹 개발, 타입스크립트]
tags: [웹개발, 타입스크립트, TS, http, api]
pin: true
math: true
mermaid: true
---

> **학습 목표**
> - HTTP 프로토콜이 무엇인지 이해한다
> - REST API의 개념과 설계 원칙을 안다
> - TypeScript에서 API를 호출하고 응답을 처리할 수 있다
> - async/await를 사용하여 비동기 코드를 작성할 수 있다
> - 타입 안전한 API 통신을 구현할 수 있다
> - 에러 처리와 로딩 상태를 관리할 수 있다

--- 

## ✏️ 직접 해보기

### 과제 1: JSONPlaceholder Todo API

JSONPlaceholder의 Todo API를 사용하여 할 일 관리 시스템을 만들어보세요.

**요구사항:**
- Todo 타입 정의
- 모든 할 일 조회
- 새 할 일 생성
- 할 일 완료 상태 변경
- 할 일 삭제
- 에러 처리 포함

```typescript
// Todo 타입
interface Todo {
  userId: number;
  id: number;
  title: string;
  completed: boolean;
}

// 여기를 완성하세요
class TodoAPI {
  private baseUrl = "https://jsonplaceholder.typicode.com";
  
  // TODO: 메서드 구현
}
```

---

#### 정답 1: JSONPlaceholder Todo API

<details>
<summary>정답 보기</summary>

```typescript
interface Todo {
  userId: number;
  id: number;
  title: string;
  completed: boolean;
}

class TodoAPI {
  private baseUrl = "https://jsonplaceholder.typicode.com";
  
  // 모든 할 일 조회
  async getAllTodos(): Promise<Todo[]> {
    try {
      const response = await fetch(\`\${this.baseUrl}/todos\`);
      
      if (!response.ok) {
        throw new Error(\`HTTP error! status: \${response.status}\`);
      }
      
      const todos: Todo[] = await response.json();
      return todos;
    } catch (error) {
      console.error("할 일을 가져올 수 없습니다:", error);
      return [];
    }
  }
  
  // 특정 사용자의 할 일 조회
  async getUserTodos(userId: number): Promise<Todo[]> {
    try {
      const response = await fetch(
        \`\${this.baseUrl}/todos?userId=\${userId}\`
      );
      
      if (!response.ok) {
        throw new Error(\`HTTP error! status: \${response.status}\`);
      }
      
      const todos: Todo[] = await response.json();
      return todos;
    } catch (error) {
      console.error("할 일을 가져올 수 없습니다:", error);
      return [];
    }
  }
  
  // 특정 할 일 조회
  async getTodo(todoId: number): Promise<Todo | null> {
    try {
      const response = await fetch(\`\${this.baseUrl}/todos/\${todoId}\`);
      
      if (!response.ok) {
        throw new Error(\`HTTP error! status: \${response.status}\`);
      }
      
      const todo: Todo = await response.json();
      return todo;
    } catch (error) {
      console.error("할 일을 가져올 수 없습니다:", error);
      return null;
    }
  }
  
  // 새 할 일 생성
  async createTodo(todo: Omit<Todo, "id">): Promise<Todo | null> {
    try {
      const response = await fetch(\`\${this.baseUrl}/todos\`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify(todo)
      });
      
      if (!response.ok) {
        throw new Error(\`HTTP error! status: \${response.status}\`);
      }
      
      const newTodo: Todo = await response.json();
      console.log("✅ 할 일이 생성되었습니다:", newTodo);
      return newTodo;
    } catch (error) {
      console.error("할 일 생성 실패:", error);
      return null;
    }
  }
  
  // 할 일 완료 상태 변경
  async toggleTodo(todoId: number): Promise<Todo | null> {
    try {
      // 먼저 현재 상태 가져오기
      const currentTodo = await this.getTodo(todoId);
      
      if (!currentTodo) {
        throw new Error("할 일을 찾을 수 없습니다");
      }
      
      // 완료 상태 반전
      const response = await fetch(\`\${this.baseUrl}/todos/\${todoId}\`, {
        method: "PATCH",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify({
          completed: !currentTodo.completed
        })
      });
      
      if (!response.ok) {
        throw new Error(\`HTTP error! status: \${response.status}\`);
      }
      
      const updatedTodo: Todo = await response.json();
      console.log(
        updatedTodo.completed ? "✅ 완료 처리" : "⬜ 미완료 처리",
        updatedTodo
      );
      return updatedTodo;
    } catch (error) {
      console.error("할 일 수정 실패:", error);
      return null;
    }
  }
  
  // 할 일 수정
  async updateTodo(
    todoId: number,
    updates: Partial<Todo>
  ): Promise<Todo | null> {
    try {
      const response = await fetch(\`\${this.baseUrl}/todos/\${todoId}\`, {
        method: "PATCH",
        headers: {
          "Content-Type": "application/json"
        },
        body: JSON.stringify(updates)
      });
      
      if (!response.ok) {
        throw new Error(\`HTTP error! status: \${response.status}\`);
      }
      
      const updatedTodo: Todo = await response.json();
      console.log("✏️ 할 일이 수정되었습니다:", updatedTodo);
      return updatedTodo;
    } catch (error) {
      console.error("할 일 수정 실패:", error);
      return null;
    }
  }
  
  // 할 일 삭제
  async deleteTodo(todoId: number): Promise<boolean> {
    try {
      const response = await fetch(\`\${this.baseUrl}/todos/\${todoId}\`, {
        method: "DELETE"
      });
      
      if (!response.ok) {
        throw new Error(\`HTTP error! status: \${response.status}\`);
      }
      
      console.log(\`🗑️ 할 일 \${todoId}이(가) 삭제되었습니다.\`);
      return true;
    } catch (error) {
      console.error("할 일 삭제 실패:", error);
      return false;
    }
  }
  
  // 통계
  async getStatistics(userId: number): Promise<void> {
    const todos = await this.getUserTodos(userId);
    
    const total = todos.length;
    const completed = todos.filter(t => t.completed).length;
    const pending = total - completed;
    const completionRate = total > 0 ? (completed / total * 100).toFixed(1) : 0;
    
    console.log(\`\n=== 사용자 \${userId}의 할 일 통계 ===\`);
    console.log(\`전체: \${total}개\`);
    console.log(\`완료: \${completed}개\`);
    console.log(\`미완료: \${pending}개\`);
    console.log(\`완료율: \${completionRate}%\`);
  }
}

// 테스트
async function testTodoAPI() {
  const todoAPI = new TodoAPI();
  
  console.log("=== Todo API 테스트 ===\n");
  
  // 1. 모든 할 일 조회 (처음 5개만)
  console.log("1. 모든 할 일 조회");
  const allTodos = await todoAPI.getAllTodos();
  console.log(\`총 \${allTodos.length}개의 할 일\`);
  allTodos.slice(0, 3).forEach(todo => {
    console.log(\`  \${todo.completed ? "✅" : "⬜"} \${todo.title}\`);
  });
  
  // 2. 특정 사용자의 할 일
  console.log("\n2. 사용자 1의 할 일 (처음 5개)");
  const userTodos = await todoAPI.getUserTodos(1);
  userTodos.slice(0, 5).forEach(todo => {
    console.log(\`  \${todo.completed ? "✅" : "⬜"} \${todo.title}\`);
  });
  
  // 3. 새 할 일 생성
  console.log("\n3. 새 할 일 생성");
  const newTodo = await todoAPI.createTodo({
    userId: 1,
    title: "TypeScript 공부하기",
    completed: false
  });
  
  if (newTodo) {
    // 4. 할 일 완료 처리
    console.log("\n4. 할 일 완료 처리");
    await todoAPI.toggleTodo(newTodo.id);
    
    // 5. 할 일 수정
    console.log("\n5. 할 일 수정");
    await todoAPI.updateTodo(newTodo.id, {
      title: "TypeScript와 API 마스터하기"
    });
    
    // 6. 할 일 삭제
    console.log("\n6. 할 일 삭제");
    await todoAPI.deleteTodo(newTodo.id);
  }
  
  // 7. 통계
  await todoAPI.getStatistics(1);
}

// 실행
testTodoAPI();
```

</details>

---

### 과제 2: 날씨 앱 만들기

OpenWeather API를 사용하여 날씨 정보를 조회하는 앱을 만들어보세요.

**요구사항:**
- 도시 이름으로 현재 날씨 조회
- 여러 도시 날씨 비교
- 섭씨/화씨 변환
- 에러 처리

```typescript
// 여기를 완성하세요
class WeatherApp {
  // API 키는 https://openweathermap.org 에서 발급받으세요
  private apiKey = "YOUR_API_KEY";
  
  // TODO: 구현
}
```

---

#### 정답 2: 날씨 앱

<details>
<summary>정답 보기</summary>

```typescript
interface WeatherData {
  coord: { lon: number; lat: number };
  weather: Array<{
    id: number;
    main: string;
    description: string;
    icon: string;
  }>;
  main: {
    temp: number;
    feels_like: number;
    temp_min: number;
    temp_max: number;
    pressure: number;
    humidity: number;
  };
  wind: {
    speed: number;
    deg: number;
  };
  clouds: {
    all: number;
  };
  name: string;
  dt: number;
}

class WeatherApp {
  private apiKey: string;
  private baseUrl = "https://api.openweathermap.org/data/2.5";
  
  constructor(apiKey: string) {
    this.apiKey = apiKey;
  }
  
  // 켈빈을 섭씨로 변환
  private kelvinToCelsius(kelvin: number): number {
    return Math.round((kelvin - 273.15) * 10) / 10;
  }
  
  // 켈빈을 화씨로 변환
  private kelvinToFahrenheit(kelvin: number): number {
    return Math.round(((kelvin - 273.15) * 9/5 + 32) * 10) / 10;
  }
  
  // 풍속 m/s를 km/h로 변환
  private msToKmh(ms: number): number {
    return Math.round(ms * 3.6 * 10) / 10;
  }
  
  // 날씨 이모지
  private getWeatherEmoji(main: string): string {
    const emojiMap: Record<string, string> = {
      Clear: "☀️",
      Clouds: "☁️",
      Rain: "🌧️",
      Drizzle: "🌦️",
      Thunderstorm: "⛈️",
      Snow: "❄️",
      Mist: "🌫️",
      Fog: "🌫️"
    };
    return emojiMap[main] || "🌤️";
  }
  
  // 도시 이름으로 날씨 가져오기
  async getWeatherByCity(city: string): Promise<WeatherData | null> {
    try {
      const url = \`\${this.baseUrl}/weather?q=\${encodeURIComponent(city)}&appid=\${this.apiKey}\`;
      const response = await fetch(url);
      
      if (!response.ok) {
        if (response.status === 404) {
          console.error(\`❌ 도시 "\${city}"을(를) 찾을 수 없습니다.\`);
        } else if (response.status === 401) {
          console.error("❌ API 키가 유효하지 않습니다.");
        } else {
          console.error(\`❌ HTTP error! status: \${response.status}\`);
        }
        return null;
      }
      
      const data: WeatherData = await response.json();
      return data;
    } catch (error) {
      console.error("❌ 날씨 정보를 가져올 수 없습니다:", error);
      return null;
    }
  }
  
  // 좌표로 날씨 가져오기
  async getWeatherByCoords(
    lat: number,
    lon: number
  ): Promise<WeatherData | null> {
    try {
      const url = \`\${this.baseUrl}/weather?lat=\${lat}&lon=\${lon}&appid=\${this.apiKey}\`;
      const response = await fetch(url);
      
      if (!response.ok) {
        throw new Error(\`HTTP error! status: \${response.status}\`);
      }
      
      const data: WeatherData = await response.json();
      return data;
    } catch (error) {
      console.error("❌ 날씨 정보를 가져올 수 없습니다:", error);
      return null;
    }
  }
  
  // 날씨 정보 출력
  printWeather(data: WeatherData, unit: "C" | "F" = "C"): void {
    const emoji = this.getWeatherEmoji(data.weather[0].main);
    const temp = unit === "C" 
      ? this.kelvinToCelsius(data.main.temp)
      : this.kelvinToFahrenheit(data.main.temp);
    const feelsLike = unit === "C"
      ? this.kelvinToCelsius(data.main.feels_like)
      : this.kelvinToFahrenheit(data.main.feels_like);
    const tempMin = unit === "C"
      ? this.kelvinToCelsius(data.main.temp_min)
      : this.kelvinToFahrenheit(data.main.temp_min);
    const tempMax = unit === "C"
      ? this.kelvinToCelsius(data.main.temp_max)
      : this.kelvinToFahrenheit(data.main.temp_max);
    
    const unitSymbol = unit === "C" ? "°C" : "°F";
    
    console.log(\`\n\${emoji} === \${data.name} 날씨 ===\`);
    console.log(\`날씨: \${data.weather[0].main} (\${data.weather[0].description})\`);
    console.log(\`현재 온도: \${temp}\${unitSymbol}\`);
    console.log(\`체감 온도: \${feelsLike}\${unitSymbol}\`);
    console.log(\`최저/최고: \${tempMin}\${unitSymbol} / \${tempMax}\${unitSymbol}\`);
    console.log(\`습도: \${data.main.humidity}%\`);
    console.log(\`풍속: \${this.msToKmh(data.wind.speed)} km/h\`);
    console.log(\`구름: \${data.clouds.all}%\`);
  }
  
  // 여러 도시 날씨 비교
  async compareCities(cities: string[]): Promise<void> {
    console.log("\n🌍 === 도시별 날씨 비교 ===");
    
    const weatherData = await Promise.all(
      cities.map(city => this.getWeatherByCity(city))
    );
    
    const validData = weatherData.filter(
      (data): data is WeatherData => data !== null
    );
    
    if (validData.length === 0) {
      console.log("조회된 날씨 정보가 없습니다.");
      return;
    }
    
    // 테이블 형식으로 출력
    console.log("\n도시명\t\t날씨\t\t온도\t\t습도");
    console.log("─".repeat(60));
    
    validData.forEach(data => {
      const emoji = this.getWeatherEmoji(data.weather[0].main);
      const temp = this.kelvinToCelsius(data.main.temp);
      const cityName = data.name.padEnd(15);
      const weather = \`\${emoji} \${data.weather[0].main}\`.padEnd(15);
      
      console.log(\`\${cityName}\t\${weather}\t\${temp}°C\t\t\${data.main.humidity}%\`);
    });
    
    // 가장 더운 도시
    const hottestCity = validData.reduce((prev, current) =>
      current.main.temp > prev.main.temp ? current : prev
    );
    
    // 가장 추운 도시
    const coldestCity = validData.reduce((prev, current) =>
      current.main.temp < prev.main.temp ? current : prev
    );
    
    console.log(\`\n🔥 가장 더운 도시: \${hottestCity.name} (\${this.kelvinToCelsius(hottestCity.main.temp)}°C)\`);
    console.log(\`❄️ 가장 추운 도시: \${coldestCity.name} (\${this.kelvinToCelsius(coldestCity.main.temp)}°C)\`);
  }
  
  // 날씨 조건 확인
  isGoodWeather(data: WeatherData): boolean {
    const temp = this.kelvinToCelsius(data.main.temp);
    const main = data.weather[0].main;
    
    return (
      temp >= 15 &&
      temp <= 28 &&
      main !== "Rain" &&
      main !== "Thunderstorm" &&
      main !== "Snow"
    );
  }
  
  // 날씨 추천
  getRecommendation(data: WeatherData): string {
    const temp = this.kelvinToCelsius(data.main.temp);
    const main = data.weather[0].main;
    
    if (main === "Rain" || main === "Thunderstorm") {
      return "🌂 우산을 챙기세요!";
    }
    
    if (main === "Snow") {
      return "⛄ 따뜻하게 입으세요!";
    }
    
    if (temp < 0) {
      return "🧥 매우 춥습니다. 두껍게 입으세요!";
    }
    
    if (temp < 10) {
      return "🧥 쌀쌀합니다. 외투를 챙기세요!";
    }
    
    if (temp >= 10 && temp < 20) {
      return "👕 날씨가 선선합니다.";
    }
    
    if (temp >= 20 && temp < 28) {
      return "😊 완벽한 날씨입니다!";
    }
    
    if (temp >= 28) {
      return "🥵 더워요! 수분 섭취하세요!";
    }
    
    return "좋은 하루 되세요!";
  }
}

// 테스트
async function testWeatherApp() {
  // API 키를 실제 키로 교체하세요
  const app = new WeatherApp("YOUR_API_KEY");
  
  console.log("=== 날씨 앱 테스트 ===");
  
  // 1. 서울 날씨 (섭씨)
  const seoulWeather = await app.getWeatherByCity("Seoul");
  if (seoulWeather) {
    app.printWeather(seoulWeather, "C");
    console.log(\`💡 추천: \${app.getRecommendation(seoulWeather)}\`);
    console.log(\`좋은 날씨? \${app.isGoodWeather(seoulWeather) ? "✅" : "❌"}\`);
  }
  
  // 2. 뉴욕 날씨 (화씨)
  const nyWeather = await app.getWeatherByCity("New York");
  if (nyWeather) {
    app.printWeather(nyWeather, "F");
  }
  
  // 3. 여러 도시 비교
  await app.compareCities([
    "Seoul",
    "Tokyo",
    "Beijing",
    "Bangkok",
    "Singapore"
  ]);
}

// 실행 (API 키가 있을 때만)
// testWeatherApp();

// API 키 없이 테스트하려면 JSONPlaceholder 사용
console.log("\n💡 OpenWeather API 키가 필요합니다.");
console.log("https://openweathermap.org 에서 무료로 발급받으세요!");
```

</details>

---
